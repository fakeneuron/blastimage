---
title: consolidate imagegenFs path-walk loops
status: sidequest
priority: Low
pickup: next-chat
created: 2026-08-09
parent: BI-036
---

# BI-041 | consolidate imagegenFs path-walk loops

[← PLAN.md](../../PLAN.md) · 📌 Sidequest (filed 2026-08-09)

## Idea

`readImagegenFile` (`lib/imagegenFs.ts:210-218`) and `writeImagegenTextFile` (:257-274) each split a relative path, walk parent dirs via `getDirectoryHandle`, then grab the leaf with `getFileHandle` — near-identical loops differing only in `{ create: true }` and the empty-path failure mode (throw vs. `Result` error). Extract a shared resolver. Surfaced during BI-036, deferred there as a behavioral refactor of the FSA seam, outside a config task's remit.

## Resume anchor

BI-036 (`noUncheckedIndexedAccess`) had just closed and committed as `1647ca0`; PLAN.md was otherwise empty, and this was the one deferred item that task surfaced.
