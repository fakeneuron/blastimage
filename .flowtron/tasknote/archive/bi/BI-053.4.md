---
title: round-view-filter
status: completed
tags: []
created: 2026-09-20
due:
related-tasks: [BI-EPIC-053, BI-053.3, BI-047, BI-026]
blocked-by:
  - BI-053.3
touches:
  - lib/types.ts
  - lib/workspace.ts
  - lib/workspace.test.ts
  - lib/useWorkspace.ts
  - lib/useWorkspace.test.ts
  - components/Sidebar.tsx
  - components/Sidebar.test.tsx
  - components/TaskDetail.tsx
  - components/TaskDetail.test.tsx
  - components/BulkReviewPane.tsx
  - components/BulkReviewPane.test.tsx
  - components/Workspace.tsx
  - components/Workspace.test.tsx
  - e2e/imagegen-link-load.spec.ts
---

# BI-053.4 | round-view-filter

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-053]] [[BI-053.3]] [[BI-047]] [[BI-026]]

## 🎯 Goal

Persist `Session.currentRound` as the project's round view-filter, highlight it in the sidebar, and have TaskDetail / BulkReviewPane / approve-iterate render that round's iteration instead of always the latest.

## ✅ Acceptance

- [x] `Session.currentRound` is an optional field; `isSession` stays structural; `SCHEMA_VERSION` stays 1 — `npx vitest run lib/workspace.test.ts lib/storage.test.ts` and `grep -n 'SCHEMA_VERSION =' lib/types.ts`
- [x] `useWorkspace` owns the field (commit + persist), exposes it in place of `loadedRound`, and does not leak project A's view into project B on create/switch; switching back restores the stored value — `npx vitest run lib/useWorkspace.test.ts`
- [x] `TaskDetail` / `BulkReviewPane` render the iteration touching `currentRound` (fallback latest) via a shared `lib/workspace.ts` selector, not `iterations.at(-1)` — `npx vitest run lib/workspace.test.ts components/TaskDetail.test.tsx components/BulkReviewPane.test.tsx`
- [x] Sidebar rounds list is driven from `.3` summaries, highlights the current round, and chip click selects the view (does not BI-043-replace) — `npx vitest run components/Sidebar.test.tsx components/Workspace.test.tsx`
- [x] Approve / iterate round fallbacks read `session.currentRound` instead of `loadedRound` — `npx vitest run lib/useWorkspace.test.ts`
- [x] Typecheck and lint clean on the touched files — `npx tsc --noEmit` · `npm run lint`
- [x] e2e: linked fixture auto-selects r1 (chip current); a two-round copy toggles the review grid — `npx playwright test e2e/imagegen-link-load.spec.ts` (webServer blocked by Next lockfile on :3003, same as .3; covered by live :3003 pass)
- [x] Round highlight and review-grid swap are visible in the running app — `👁️` (live :3003: r1 highlighted after link; ↻ kept view on r1 while ingesting r2; r2 chip swapped grid to Cool hero; r1 restored Warm; one Hero banner throughout)

## 🧩 Subtasks

- [x] `lib/types.ts` — optional `currentRound?: number | null` on `Session` (BI-047 JSDoc pattern); no `SCHEMA_VERSION` bump
- [x] `lib/workspace.ts` — `setCurrentRound` mutator; export `visibleIteration(task, currentRound)` (touching iteration, else latest); keep `iterationTouchesRound` private
- [x] `lib/useWorkspace.ts` — drop `loadedRound` state; expose `currentRound` from the session + `setCurrentRound`; loadRound(n) and no-arg (when unset) write the field; approve/iterate fallbacks use it; create/switch do not leak
- [x] `components/Workspace.tsx` — autoload drops the `loadedRound !== null` skip (per-session one-shot remains); wire `setCurrentRound` / `currentRound` / summaries
- [x] `components/Sidebar.tsx` — rounds list from summaries, highlight `aria-current`, chip click selects the view; ↻ stays no-arg refresh
- [x] `components/TaskDetail.tsx` + `BulkReviewPane.tsx` — take `currentRound`, render `visibleIteration`
- [x] Tests: selector + persist/switch + chip highlight/select + review-grid swap + approve fallback; e2e current-chip + two-round toggle

## 🔗 Related

- [[BI-EPIC-053]] — parent epic (session-round-navigation)
- [[BI-053.3]] — blocked-by: round summaries + auto-ingest (predecessor, closed)
- [[BI-047]] — optional Session field + structural `isSession` (no schema bump)
- [[BI-026]] — in-memory `loadedRound` autoload this field replaces
- [[BI-053.5]] — follow-up: sidebar restructure (consumes the rounds list this child introduces)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Next Sequential child of BI-EPIC-053; .3 is closed. `loadedRound` is still unpersisted `useState`, TaskDetail L99 and BulkReviewPane L48 still render `iterations.at(-1)`, and rN chips still BI-043-replace. The .1 defect ("can't toggle rounds") is live. Scope matches the PLAN line.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; when the grep returns more than a handful of notes (~3 is a fair line), prefer handing the reading to a **probe**; log relevant findings in Discovery Notes before re-interpreting the task; an absent or empty `archive/<area>/` is a prompt to re-check `<area>` against the README table before logging "no prior tasknotes" — a derived-and-wrong folder is indistinguishable from a genuinely empty one

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit (omit only on a task with no file deliverable)

**Discovery Notes:**

**Read set (HEAD):** `lib/types.ts` `Session` (optional `imagegenRoot`, no `currentRound`); `lib/storage.ts` `isSession` structural (no field list beyond id/name/schemaVersion/tasks/refLibrary/timestamps); `SCHEMA_VERSION = 1`; `lib/workspace.ts` private `iterationTouchesRound` / `upsertIterationForRound`, exported `sessionRoundNumbers`, `bindImagegenRoot` as the optional-field mutator shape; `lib/useWorkspace.ts` `loadedRound` useState L377, reset only in `switchSession` L562 (not `createSession`), approve/unapprove/iterate fallbacks `roundNumberFromImageUrl(...) ?? loadedRound` (L830/887/962), `loadRound(n)` replace + `setLoadedRound`, no-arg ingest-missing + `setLoadedRound(latest)`; `components/Workspace.tsx` autoload skips when `loadedRound !== null`; `components/Sidebar.tsx` rN chips call `onLoadRound(n)`, no highlight; `components/TaskDetail.tsx` L99 and `BulkReviewPane.tsx` L48 `iterations.at(-1)`; labels use `iteration.index + 1` (not the terminal round number). `.3` already exposes `roundSummaries` on the hook.

**Assumptions (no clarifications needed):**
- rN chip click is the view filter: `setCurrentRound(n)`, not `loadRound(n)`. Explicit `loadRound(n)` stays on the hook (BI-043 replace) for programmatic/test use; ↻ stays no-arg ingest-missing. Losing chip-driven replace is the point of "view filter" from .1; a dedicated "reload this round from disk" control is out of scope (file later if wanted).
- `currentRound` lives on `Session` like `imagegenRoot` (BI-047): optional, no schema bump, not on `SessionMeta`. "Resets on switch" means project B does not inherit project A's view; switching *back* restores the stored value. `createSession` starts unset.
- Display fallback when `currentRound` is absent or that task has no matching iteration: latest (`iterations.at(-1)`).
- No-arg `loadRound()` sets `currentRound` to the latest on-disk round only when it is currently unset — a ↻ that ingests r3 while viewing r1 stays on r1.
- Autoload must still ingest missing rounds when `currentRound` is already set (otherwise remount would skip .3's refresh). Drop the `loadedRound !== null` skip; keep the per-session-id one-shot.
- Chip accessible name stays `Load round rN` (e2e + BI-035.3). Highlight via `aria-current="true"`. Counts from summaries go on `title`. Sidebar regroup / copy sweep stays .5.
- Docs that describe chip click as ingest are .5's sweep; this child does not rewrite REVIEW-LOOP / ADOPT / WORKFLOW.

**Best Practices Review:** Extend established seams — optional Session field + structural `isSession` (BI-047), pure mutator + selector in `lib/workspace.ts` (`.1` called this out: one definition for TaskDetail, BulkReviewPane, and the approve/iterate fallbacks), orchestration only in `useWorkspace` (BI-049: no parallel hook). `iterationTouchesRound` stays private; export `visibleIteration(task, currentRound)` (and `setCurrentRound`). No new hook. Deferred: chip-driven BI-043 replace; toast on ↻ ingest-vs-skip (.3); sidebar regroup (.5); `approved/` orphan cleanup (BI-043).

**Archive skim (load-bearing):** BI-047 — optional field, no schema bump, `isSession` unchanged, mutator in `workspace.ts`, hook owns commit/switch. BI-026 — `loadedRound !== null` skip so autoload never overrides a manual load; after this child the skip is wrong (ingest-missing is safe) and the per-session ref remains. BI-043 — replace-on-reload stays the rewrite path; chips no longer call it. BI-053.1 — `.4`'s core is `at(-1)`; selector belongs in `workspace.ts`. BI-053.3 — `roundSummaries` handoff; skip-already-ingested; chips currently still replace; counts-in-the-list deferred here. BI-049 — add orchestration in `useWorkspace`, do not split. BI-050 — render-time unlink clear of round listings stays.

**Drift check:** PLAN line matches HEAD. `loadedRound` still useState. `at(-1)` still the review panes. `isSession` still structural. `roundSummaries` exists as .3 promised. `.1` line numbers for `useWorkspace` (L1053 etc.) are stale after .2/.3; the symbols are the same. No SPEC contract. Fan-out Sequential after closed `.3`. The chip-click → view-filter reading is the filed "view filter" / "replaces `loadedRound`", not a re-scope.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

Pattern: BI-047 optional Session field + structural `isSession` (no schema bump); pure `setCurrentRound` / `visibleIteration` in `lib/workspace.ts`; `useWorkspace` owns commit/persist (BI-049). Dropped in-memory `loadedRound`. Chip click is `setCurrentRound` (view filter); `loadRound(n)` still BI-043-replaces but is no longer the chip handler. Autoload dropped the `loadedRound !== null` skip so remount still ingest-missing. No-arg `loadRound()` writes `currentRound` to latest only when unset.

Minimal: no merge-by-URL; no dedicated "reload this round" control. Deferred: sidebar regroup / chip copy (.5); toast on ↻ ingest-vs-skip (.3); `approved/` orphan cleanup (BI-043).

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

```
npx vitest run lib/workspace.test.ts lib/storage.test.ts lib/useWorkspace.test.ts components/TaskDetail.test.tsx components/BulkReviewPane.test.tsx components/Sidebar.test.tsx components/Workspace.test.tsx
  → 0  (237 passed)
npx tsc --noEmit  → 0
npx eslint <touched>  → 0
grep -n 'SCHEMA_VERSION =' lib/types.ts  → 0  (line 18: = 1)
npm test  → 0  (600 passed)
npx playwright test e2e/imagegen-link-load.spec.ts  → 1
    Another next dev server is already running on :3003 (Next lockfile). Covered instead by live :3003 browser pass.
```

Live :3003: link fixture → r1 chip `aria-current=true`, title `r1 · 1 task · 2 images`, grid label `Round r1`. Re-link a two-round copy → ↻ ingested r2 without leaving r1. Click r2 → Cool hero images, `Round r2`, one Hero banner. Click r1 → Warm hero restored.

No avoidable duplication: one `visibleIteration` selector; chip highlight is `aria-current` + existing border classes. `loadedRound` fully removed. Public surface: `setCurrentRound` / `visibleIteration` / `Session.currentRound` / hook `currentRound`+`setCurrentRound` (replaces `loadedRound`). Docs that still name chip click as ingest are .5.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed (standalone → top of `## Completed`; epic child → kept nested beneath its active parent — see SPEC/plan-filing.md §"`## Completed` archive convention" if unclear), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, the `touches:` scope reconciliation (`git diff --name-only` vs declared; name undeclared paths), and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Rounds are a persisted view filter. `Session.currentRound` survives reload and project switch-back; the sidebar highlights the current chip; TaskDetail / BulkReviewPane / approve-iterate fallbacks show that round's iteration instead of always the latest. Chip click no longer BI-043-replaces.

**Doc-drift:** `README.md`, `AGENTS.md`, `CLAUDE.md`, `VISION.md`, `docs/ADOPT.md`, `docs/WORKFLOW.md`, `docs/REVIEW-LOOP.md`, `docs/GROK-AGENT.md` — no change (chip-as-view copy is .5). `.flowtron/PLAN.md` — `.4` stubbed.

**Scope:** Declared 14 paths, changed 14. Plus tasknote + PLAN (excluded).

**Maintainability:** one selector for "which iteration to show"; `loadedRound` useState is gone.

**Archived:** 2026-09-20

