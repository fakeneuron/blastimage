---
title: doc-currency-polish audit
status: completed
tags: []
created: 2026-08-09
due:
related-tasks: [BI-EPIC-035, BI-035.2, BI-035.3, BI-035.4, BI-035.5]
---

# BI-035.N | doc-currency-polish audit

[← PLAN.md](../../PLAN.md) · ✅ Completed · 🔗 [[BI-EPIC-035]]

## 🎯 Goal

Verify the completed BI-EPIC-035 (`doc-currency-polish`) cohort sits coherently in the codebase: cumulative doc-drift sweep across `.flowtron/tasknote/README.md` §"AI-referenced docs", naming/style consistency across the cohort's deliverables, and follow-up filings for any miss.

## ✅ Acceptance

- [x] **Doc-drift sweep (fixed line, per SPEC/epic.md §"Audit acceptance — fixed doc-drift line")** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the specific update. Always present; surfaces cumulative slice-local staleness that per-task Phase 4 closures can miss.
- [x] Cohort coherence inventory: each implementation child's deliverables read against the others (naming consistency, style parity, no contradictory cross-refs)
- [x] No regressions surfaced in earlier-shipped cohort children's surfaces
- [x] Audit findings recorded in Implementation Notes; misses cited as candidates for `/ft-file-followup <NEW-ID>` filing (filed AFTER audit closure to preserve `/ft-file-followup`'s filing-discipline gate)
- [ ] Single `chore: BI-035.N — audit BI-EPIC-035` commit lands
- [x] PLAN.md line for `BI-035.N` flipped to stub form `Completed 2026-08-09.`
- [x] Tasknote moved to `.flowtron/tasknote/archive/bi/BI-035.N.md`
- [x] Parent-flip prompt surfaced after audit closure — user confirmed flipping `BI-EPIC-035` to `Completed` and moving the cohort to `## Completed`

## 🧩 Subtasks

- [x] Inventory cohort children's archived tasknotes — read each implementation child's Final Summary + Implementation Notes; capture deliverables in Discovery Notes
- [x] Walk `.flowtron/tasknote/README.md` §"AI-referenced docs" entries — fixed doc-drift sweep
- [x] Cohort coherence pass — naming consistency, style parity, no contradictory cross-refs across the cohort's deliverables
- [x] Surface audit findings in Implementation Notes; cite each miss as a `/ft-file-followup <NEW-ID>` candidate
- [x] Phase 4: flip `BI-035.N` PLAN line to stub form + archive tasknote
- [x] Parent-flip: user confirmed Yes; parent stubbed + cohort moved to `## Completed`

## 🔗 Related

- [[BI-EPIC-035]] — parent epic (doc-currency-polish)
- [[BI-035.2]] — vision-mode-rewrite (closed)
- [[BI-035.3]] — sidebar-aria-labels (closed)
- [[BI-035.4]] — review-sheet-src-escape (closed)
- [[BI-035.5]] — lightbox-focus-management (closed)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** All four implementation children (`BI-035.2`–`.5`) are closed and nested under still-active `BI-EPIC-035` in `## Medium`. Terminal audit `.N` is the correct next step per SPEC/epic.md. Epic founding theme ("Accessibility is modal-complete, chrome-incomplete") is known half-wrong from child notes — audit is the right place to triage that.

- [x] Read relevant source files

- [x] **Best Practices Review** — N/A for audit verification pass (no new module surface unless an inline fix lands). Cohort deliverables span docs (`VISION.md`), presentational a11y (`Sidebar`, `Lightbox`), and pure lib string builder (`buildReviewSheetHtml`). No new abstraction introduced by the cohort; `Lightbox` deliberately deferred shared `useFocusTrap` extraction.

- [x] **Archive skim** — Cohort archives read end-to-end: `BI-035.2`, `BI-035.3`, `BI-035.4`, `BI-035.5`. Prior related (non-cohort): `BI-028` (hosted-mode removal content for VISION), `BI-021.4` (review-sheet origin), `BI-027` / `TEST-002.4` (Lightbox foundation + focus-gap filing). No contradictory prior decisions.

- [x] **Drift check** — All cited deliverable paths still match HEAD:
  - `VISION.md` — two-mode table + BI-028 persistence paragraph present; "mocked for immediate use" absent
  - `components/Sidebar.tsx` — 13 `aria-label`s + Project `htmlFor`/`id`
  - `lib/storage.ts:590` — `src="${escapeHtml(src)}"`
  - `components/Lightbox.tsx` — focus trap + restore + controls inside dialog
  - Sibling modals still `aria-modal` + `autoFocus` only (no trap/restore)
  - `eslint.config.mjs` / `package.json` — no `eslint-plugin-jsx-a11y`

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  **No clarifications needed.** Assumptions:
  1. Audit is verification-only; large misses file as post-closure follow-ups, not inline multi-file a11y work.
  2. `ImportBuilder` shares the same focus gap as the three handed-over modals — fold into one follow-up, not a separate ticket.
  3. Adding `VISION.md` to the AI-referenced docs list is an audit judgment call (not Acceptance-required); default leave-off if CLAUDE/README already carry the same product facts.
  4. Parent-flip is operator-gated after audit closure (SPEC/epic.md).

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

### Cohort inventory (from archived Final Summaries)

| Child | Shortname | Deliverables | Tests |
|---|---|---|---|
| **BI-035.2** | vision-mode-rewrite | `VISION.md` rewrite: Adopter/In-app modes, mock claim removed, BI-028 + Neon/R2 seam | doc-only |
| **BI-035.3** | sidebar-aria-labels | `components/Sidebar.tsx` (+labels), `Sidebar.test.tsx` (5 → 13) | naming tests + mutation pass |
| **BI-035.4** | review-sheet-src-escape | `lib/storage.ts` one-line `escapeHtml(src)`, `storage.test.ts` breakout case | 52/52 storage |
| **BI-035.5** | lightbox-focus-management | `components/Lightbox.tsx` focus trap/restore + DOM control move, `Lightbox.test.tsx` (23 → 33) | focus describe + mutation pass |

### Handed-forward items (children → audit triage)

1. **Sibling-modal focus gap** (BI-035.5 + BI-035.3): `FeedbackModal`, `IterateModal`, `DeleteTaskModal` declare `aria-modal="true"` with only `autoFocus` — no trap, no restore. Audit also found `ImportBuilder` in the same class (Escape yes; no trap/restore/`autoFocus`).
2. **No `eslint-plugin-jsx-a11y`** (BI-035.3): lint stack is `next/core-web-vitals` + `next/typescript` only — chrome naming regresses silently.
3. **`VISION.md` on AI-referenced list?** (BI-035.2 deferred): optional; audit decides.

### Founding-theme correction

Epic PLAN long-description cites audit-repo theme *"Accessibility is modal-complete, chrome-incomplete"*. Cohort proved chrome was incomplete (Sidebar fixed) **and** modals were not focus-complete (Lightbox fixed; three + ImportBuilder remain). Theme is half-wrong going forward — follow-ups own the residual modal gap.

Discovery surfaced no significant deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — N/A for audit (verification over existing cohort deliverables; no new code surface).

- [x] **Minimal refactor gate** — no inline code fix; residual misses are multi-file a11y / lint-config scope, not one-line audit patches.

- [x] Implemented the minimal solution — verification pass recorded below.

- [x] Updated/added tests for non-trivial behavior — N/A (no code change).

**Implementation Notes:**

### Cohort children inventoried

- **BI-035.2** — `VISION.md` aligns with README / CLAUDE two-mode + BI-028 persistence language. No code.
- **BI-035.3** — Sidebar chrome naming complete; tests pin accessible names via mutation. Docstring records Label-in-Name choice.
- **BI-035.4** — pure builder contract honest; defense-in-depth on `src` attribute. Orthogonal to a11y siblings; no cross-ref conflict.
- **BI-035.5** — Lightbox is the focus-trap **precedent** for remaining modals; deliberately no shared hook yet (first consumer only at ship time; four eventual consumers now known).

### Coherence findings

- **Naming/style:** a11y children use component-local tests + mutation passes; storage child uses pure unit tests — consistent with each surface type. Task IDs cited in component docstrings (`BI-035.3`, `BI-035.5`) match PLAN.
- **Cross-refs:** no contradictory claims between siblings. Doc child (`.2`) and lib child (`.4`) do not overlap a11y children (`.3`/`.5`).
- **Regressions:** targeted vitest on cohort surfaces **98/98 pass** (`Sidebar` 13 + `Lightbox` 33 + `storage` 52). HEAD matches each child's claimed deliverable paths.
- **Inconsistencies in shipped work:** none. Residual gaps are *incomplete product surface* (other modals, lint policy), not broken cohort deliverables.

### Audit decisions (not misses)

- **`VISION.md` → AI-referenced list: leave off.** Product vision is now current; cold-start AI ground truth for sessions remains README / AGENTS / CLAUDE / PLAN. Duplicating mode/persistence facts into a fifth always-walked doc adds churn without new agent guidance. Revisit only if VISION becomes the operator-facing SSOT for a mode contract that CLAUDE no longer carries.
- **Shared `useFocusTrap`:** do **not** extract in this audit. File one follow-up that either extracts once across remaining modals or ports Lightbox's pattern — extraction is justified at second consumer, which the follow-up will be.

### Misses → `/ft-file-followup` candidates (file AFTER audit closure)

1. **`/ft-file-followup` — remaining modal focus management** — Apply Lightbox-class focus trap + restore to `FeedbackModal`, `IterateModal`, `DeleteTaskModal`, and `ImportBuilder` (all declare `aria-modal="true"` without trap/restore). Prefer one task extracting a small `useFocusTrap` (or equivalent) against the Lightbox precedent rather than four one-off copies. Medium a11y; clear Acceptance from BI-035.5 pattern.
2. **`/ft-file-followup` — add `eslint-plugin-jsx-a11y` (or Next a11y lint path)** — Prevent silent regression of unnamed controls after BI-035.3. Config-only with fix-up pass for any newly flagged sites. Light–medium.

No other misses. No inline fixes applied.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — N/A code change; regression smoke: `npx vitest run components/Sidebar.test.tsx components/Lightbox.test.tsx lib/storage.test.ts` → **98/98 pass**

- [x] Ran lint/type-check on changed code — N/A (markdown-only audit; no production files touched)

- [x] **Quality assertions** — N/A for new code; verification found no dead deliverables or contradictory cohort docs

- [x] (frontend) N/A — no UI change this task

**Testing Notes:**

Verification-only audit. Cohort surfaces green. No tsc/eslint delta to run.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — PLAN.md child stub `Completed 2026-08-09.`; kept 2-space nested beneath active parent `BI-EPIC-035` in `## Medium`; tasknote moved to `.flowtron/tasknote/archive/bi/BI-035.N.md`

- [x] **Evidence-based recap** drafted — see Final Summary (surfaces at the 📦 ready-to-commit gate)

**Final Summary:**

Audited BI-EPIC-035 (`doc-currency-polish`): all four implementation children cohere at HEAD with no regressions (98/98 cohort-surface tests green). Founding theme "modals complete / chrome incomplete" is half-wrong — chrome naming and Lightbox focus are fixed; residual focus gap remains on four other dialogs, plus missing jsx-a11y lint. Two follow-ups to file after commit; no inline code fixes.

| AI-referenced doc | Verdict |
|---|---|
| `README.md` | **no change.** Mode split, localStorage, sidebar Generate All / project controls — still accurate; a11y internals not README surface. |
| `AGENTS.md` | **no change.** Flowtron paste-block only. |
| `CLAUDE.md` | **no change.** Modes, BI-028, testing conventions still match cohort work. |
| `.flowtron/PLAN.md` | **Updated.** `BI-035.N` flipped to stub form; parent-flip Yes — `BI-EPIC-035` + full cohort moved to top of `## Completed`; `## Medium` restored to `(none)`. |

**Follow-ups (post-commit `/ft-file-followup`):** (1) remaining modal focus management — FeedbackModal, IterateModal, DeleteTaskModal, ImportBuilder; (2) eslint-plugin-jsx-a11y.

**Refactors:** none made. Deferred: shared `useFocusTrap` extraction (belongs with follow-up #1); VISION.md stays off AI-referenced list (judgment).

**Maintainability:** cohort deliverables are pinned by tests (Sidebar names, Lightbox focus, storage src escape) and VISION matches shipped product. Residual a11y class is named and sized for two follow-ups rather than left as theme folklore.

**Archived:** 2026-08-09
