---
title: vision-mode-rewrite
status: completed
tags: []
created: 2026-08-09
due:
related-tasks: [BI-EPIC-035, BI-028]
---

# BI-035.2 | vision-mode-rewrite

[← PLAN.md](../../PLAN.md) · ✅ Completed · 🔗 [[BI-EPIC-035]] [[BI-028]]

## 🎯 Goal

Rewrite `VISION.md` against the two shipped generation modes (Adopter terminal review loop + In-app Grok Build provider bridge); drop the mock claim and record the BI-028 hosted-mode removal.

## ✅ Acceptance

- [x] `VISION.md` describes the two shipped generation modes (Adopter / In-app) in plain product language, with pointers to the SSOT docs (`docs/REVIEW-LOOP.md`, `docs/GROK-AGENT.md`)
- [x] The "mocked for immediate use / API path prepared" claim is gone
- [x] BI-028 is reflected: localStorage-only default; optional Supabase hosted mode removed; future server-backed Neon + R2 noted only as a persistence direction, not as a live product mode
- [x] Core product loop (tasks, refs, batch review, iterate, approve, export+manifest) remains accurate and not bloated into a how-to
- [x] No code changes; doc-only task

## 🧩 Subtasks

- [x] Draft rewritten `VISION.md` (keep short product-vision tone; align with PLAN Vision + CLAUDE.md + README mode split)
- [x] Drop mock claim; add two-mode generation section
- [x] Record BI-028 hosted-mode removal + future Neon/R2 seam
- [x] Sanity-read against `docs/REVIEW-LOOP.md` / `docs/GROK-AGENT.md` / `Claude.md` for contradiction
- [x] Phase 3 doc checks + Phase 4 closure

## 🔗 Related

- [[BI-EPIC-035]] — parent epic (doc-currency-polish)
- [[BI-028]] — hosted-mode removal (content to record)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `VISION.md` still describes a mock-first cut and a vague "hosted webapp" future. The product has two shipped generation modes and BI-028 removed Supabase hosted mode. Rewrite is still the right scope.

- [x] Read relevant source files — `VISION.md`, `README.md` (Adopter / In-app sections), `Claude.md`, `docs/REVIEW-LOOP.md` §1–2, `docs/GROK-AGENT.md` §App context + mock-removal note, archive `BI-028.md`

- [x] **Best Practices Review** — N/A: pure documentation rewrite; no code, modules, or dependency boundaries.

- [x] **Archive skim** — `rg -l VISION|mock|hosted` over `archive/bi/`:
  - **BI-028** — removed optional Supabase hosted stack; localStorage default; keep async `PersistenceAdapter` seam for future Neon + R2 (not implemented). Docs must not tell operators to provision Supabase.
  - **BI-035.3/.4/.5** — a11y siblings; no VISION touch.
  - **GROK-AGENT / BI-EPIC-024 path** (via docs, not archive grep alone) — mock removed from `generateBatch`; provider bridge is the in-app path; adopter path is terminal skills + frontend viewer.

- [x] **Drift check** — `VISION.md` still at repo root. Stale claims confirmed:
  1. L14: "Batch generation of 3–5 images (mocked for immediate use; real Grok Imagine API integration path prepared)" — mock era is over.
  2. L7: "A hosted webapp variation is noted as a future opportunity" — conflates removed Supabase hosted mode with any future backend; should match BI-028/CLAUDE (local-only now; Neon/R2 seam later).
  3. Missing entirely: two mutually exclusive generation modes (Adopter vs In-app).

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  **No clarifications needed.** Assumptions:
  - Keep VISION short (product vision, not a how-to); deep contracts stay in REVIEW-LOOP / GROK-AGENT / ADOPT.
  - Two modes are mutually exclusive, as CLAUDE.md states.
  - "Hosted" language means the removed Supabase product mode, not "runs on localhost hosting Next."
  - Future Neon + R2 is a one-line persistence direction only — not a third generation mode.
  - Batch size 3–5 and the rest of the feature bullet list remain product-true.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

Current `VISION.md` (~20 lines) is first-cut language. README and CLAUDE already document the real product; VISION is the outlier. Rewrite should:

1. Preserve the opening problem/user framing (still true).
2. Replace the mock bullet with real Grok Imagine generation.
3. Add a short "Generation modes" section for Adopter vs In-app with doc links.
4. Replace hosted-webapp future with: localStorage default; Supabase hosted mode removed (BI-028); optional future server-backed Neon + R2 via the persistence seam.
5. Keep the feature list (workspace, refs, review grid, iterate, gallery, export+manifest) without turning VISION into README.

Discovery surfaced no significant deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — mirrored README / CLAUDE mode split (Adopter vs In-app table + SSOT links) at vision altitude; did not invent a third shape. Persistence paragraph mirrors BI-028 + Claude.md seam language.

- [x] **Minimal refactor gate** — rewrite of one file only; no adjacent docs touched. Deferred: adding `VISION.md` to AI-referenced docs list (not required by Acceptance; audit BI-035.N can decide).

- [x] Implemented the minimal solution — rewrote `VISION.md`: product surface bullets updated (mock → real Grok Imagine); new Generation modes section; Persistence section records BI-028 + future Neon/R2 seam.

- [x] Updated/added tests for non-trivial behavior — N/A (doc-only).

**Implementation Notes:**

Single deliverable path: `VISION.md` (+19 / −4 lines relative to prior). Structure: opening problem framing (kept) → Product surface → Generation modes table → Persistence.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — N/A (markdown only; no runtime surface).

- [x] Ran lint/type-check on changed code — N/A (markdown only).

- [x] **Quality assertions** — no mock claim remains; both modes present with SSOT links; BI-028 + Neon/R2 seam stated once; feature list still product-true and not a how-to. No dead sections.

- [x] (frontend) Asked the user for visual confirmation — N/A: no UI change.

**Testing Notes:**

Sanity grep: `mocked for immediate use` absent; `Adopter` / `In-app` / `BI-028` / `localStorage` / `Neon` present with correct doc links.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs":
  - `README.md` — no change (already documents both modes; VISION now aligned)
  - `AGENTS.md` — no change (workflow-only)
  - `CLAUDE.md` — no change (already accurate on modes + BI-028)
  - `.flowtron/PLAN.md` — BI-035.2 flipped to Completed stub, kept nested under active BI-EPIC-035

  Note: `VISION.md` itself is not on the AI-referenced list; left off (not Acceptance).

- [x] Closed — PLAN.md child stub `Completed 2026-08-09.`; nested under parent; tasknote archived to `.flowtron/tasknote/archive/bi/BI-035.2.md`

- [x] **Evidence-based recap** drafted

**Final Summary:**

Rewrote root `VISION.md` so the product vision matches shipped reality: real Grok Imagine generation in two mutually exclusive modes (Adopter terminal loop vs In-app provider bridge), with the mock claim removed and BI-028's localStorage-only / Supabase-hosted-removed state plus the future Neon+R2 persistence seam recorded. One markdown file; no code.

**Archived:** 2026-08-09
