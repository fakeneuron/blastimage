---
title: cloudflare-deploy
status: completed
tags: []
created: 2026-06-16
related-tasks: [BI-EPIC-022, BI-022.1, BI-022.3, BI-022.6, BI-022.7]
---

# BI-022.5 | cloudflare-deploy

[← PLAN.md](../PLAN.md) · ✅ Completed · 🔗 [[BI-EPIC-022]] [[BI-022.1]] [[BI-022.3]] [[BI-022.6]] [[BI-022.7]]

## 🎯 Goal

Ship the hosted-mode deploy story for blastimage — Cloudflare Pages (the client-rendered Next app) + production Supabase config + production OAuth redirect wiring — and resolve how Grok-Imagine generation works (or doesn't) in the deployed browser context, where the Grok Build sandbox the provider seam depends on is absent.

## ✅ Acceptance

- [ ] Static-export build config: `next.config.ts` emits a Cloudflare-Pages-deployable static site (`out/`) when targeted, without regressing the default/local `next build` or `next dev`
- [ ] `next build` (hosted env: `NEXT_PUBLIC_BLASTIMAGE_MODE=hosted` + dummy Supabase URL/anon-key) produces a valid static export locally — verified in-session
- [ ] Existing test suite stays green; `tsc --noEmit` + `eslint` clean on changed files
- [ ] `docs/DEPLOY.md` runbook: reproducible Cloudflare Pages + production Supabase (link/`db push`/auth URLs) + production Google/GitHub OAuth-app registration (the live external hop deferred from [[BI-022.6]]) + env-var/redirect-URL wiring + post-deploy verification
- [ ] `.env.example` / `supabase/config.toml` notes updated for the production redirect-URL story
- [ ] Hosted-mode generation limitation documented (deferred to the new [[BI-022.9]] spike); no generation-path code in this task
- [ ] Doc-drift sweep at closure

## 🧩 Subtasks

1. **Static-export config** (`next.config.ts`) — enable a Cloudflare-Pages static export (`output: 'export'` + `images.unoptimized`), keyed so the default/local build + `next dev` are unaffected (env-conditional, e.g. `BUILD_TARGET=cloudflare`); confirm no `next/image`/dynamic-route export blockers (grep first).
2. **Local export verification** — `BUILD_TARGET=cloudflare NEXT_PUBLIC_BLASTIMAGE_MODE=hosted NEXT_PUBLIC_SUPABASE_URL=… NEXT_PUBLIC_SUPABASE_ANON_KEY=… next build`; confirm `out/` generated, index renders the auth gate; default `next build` still works.
3. **`docs/DEPLOY.md` runbook** — (a) Cloudflare Pages: build cmd / output dir `out` / Node version / env vars; (b) production Supabase: `supabase link` + `supabase db push` (the two migrations) + set cloud auth `site_url`/`additional_redirect_urls` to the CF domain; (c) production OAuth: register Google + GitHub apps with the Supabase callback + CF origin, set client/secret in the Supabase dashboard (the [[BI-022.6]]-deferred live hop); (d) env-var matrix; (e) post-deploy verification checklist (sign in, session persists, RLS round-trip).
4. **Config/env doc touch-ups** — `.env.example` + `supabase/config.toml` comments noting production redirect URLs are set in the cloud dashboard (local stays `:3003`); document hosted generation as not-yet-available (→ [[BI-022.9]]).
5. **Tests + lint/type-check** — full suite green (config-only change shouldn't move it); `tsc --noEmit` + `eslint` on changed files.
6. **Phase 4** — doc-drift sweep, flip PLAN `.5` to stub, archive tasknote.

## 🔗 Related

- [[BI-EPIC-022]] — parent epic (hosted-webapp)
- [[BI-022.1]] — discovery; scoped this child (Grok-Build generation in hosted mode + Cloudflare deploy); flagged the generation wiring as the least-defined / spike-prone
- [[BI-022.3]] — predecessor; built the Supabase client/schema/RLS + config-gate this deploy provisions for production
- [[BI-022.6]] — predecessor; built OAuth login UI + provider config; deferred the live external Google/GitHub redirect hop here
- [[BI-022.7]] — the local→cloud session importer; relevant if hosted mode is review/persist-only and generation stays in local Grok Build mode

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Re-scope
  **Rationale:** Both blockers ([[BI-022.3]] schema/auth, [[BI-022.6]] OAuth) are complete, so the task is relevant. But Discovery confirmed the filed line bundles two halves of very different maturity: a **well-defined deploy** (static export → Cloudflare Pages + prod Supabase/OAuth wiring) and a **spike-prone generation** half that hits an architectural wall — the Grok Build sandbox the `__grokImagineProvider` seam depends on does not exist in a deployed browser, and there is no public "Grok Build CLI auth" path. Per user clarification, the generation half **splits into a new deferred child [[BI-022.9]]**; this task narrows to deploy-config + reproducible docs, verified locally. PLAN reconciliation in Discovery Notes, applied at the 🛠️ gate.

- [x] Read relevant source files

  - `lib/generate.ts` — the generation seam. `generateBatch` delegates to `globalThis.__grokImagineProvider` and **throws** if none is installed. The provider is installed by a Grok Build agent and uses `image_gen` + `node:fs` (filesystem→data URL). **None of this exists in a deployed user browser.**
  - `docs/GROK-AGENT.md` — confirms the model: "runs inside Grok Build with a SuperGrok subscription; Grok Imagine is available as a built-in … the generation call runs client-side inside the Grok Build sandbox." The provider reads `image_gen` filesystem output via `node:fs/promises`.
  - `lib/config.ts` — `isHostedMode()` (`NEXT_PUBLIC_BLASTIMAGE_MODE=hosted`) + `supabaseConfig()` (throws if `NEXT_PUBLIC_SUPABASE_URL`/`_ANON_KEY` missing). The mode gate the deploy sets.
  - `app/page.tsx` / `app/layout.tsx` — purely client-rendered (`<AuthGate><Workspace/></AuthGate>`); no RSC data work, no API routes, no route handlers, no `'use server'`. **Statically exportable** → Cloudflare Pages with no edge-worker/`next-on-pages` complexity.
  - `next.config.ts` — empty config; would need `output: 'export'` (+ image config) for a static-export deploy.
  - `supabase/config.toml` — local stack (ports 54341–54344; trimmed services); `[auth.external.google|github]` enabled via env-substituted secrets; `site_url`/redirects → `:3003` (need production URLs at deploy).
  - `.env.example` — mode + Supabase + OAuth provider vars; notes "Production credentials are configured at deploy (BI-022.5)."

- [x] **Archive skim** — `bi/BI-022.1.md` (deep pre-pass: Cloudflare Pages + Supabase resolved; generation kept on Grok Build, BYO-key/operator-key path an explicit **non-goal**; `.5` generation wiring flagged least-defined, "may want a spike or a split"). `bi/BI-022.3.md` (built Supabase client/schema/RLS + config-gate + trimmed `config.toml`; "deploy = `.5`"). `bi/BI-022.6.md` (built OAuth UI client-only — no `@supabase/ssr`/middleware/callback route, relies on default `detectSessionInUrl`; **explicitly deferred the live external Google/GitHub redirect hop to this task**; verified config starts cleanly with providers enabled + empty secrets). `bi/BI-022.7.md` (the importer = local→cloud on-ramp; relevant if hosted is persist/review-only). `bi/BI-013.md` (real Grok returns images inside the sandbox). No prior tasknote attempted deploy or a non-sandbox generation path — net-new.

- [x] **Drift check** — confirmed at HEAD: `generateBatch` still throws with no provider; the provider model is Grok-Build-sandbox-only (`image_gen` + `node:fs`); app is client-only/static-exportable; `next.config.ts` empty; `config.toml` carries local URLs; OAuth providers enabled with env-sub secrets. The PLAN line's phrase "Grok Build CLI auth so the provider seam resolves" assumes a mechanism that does not exist for a deployed browser — surfaced as the central clarification below, not silently executed.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  Resolved via AskUserQuestion (2 forks):

  | Question | Resolution | Effect |
  |---|---|---|
  | Hosted-mode generation (Grok Build sandbox absent in browser) | **Defer — deploy now, spike later** | Generation half splits into new child [[BI-022.9]]; hosted generation stays unavailable until that spike resolves it; this task ships deploy only |
  | This tasknote's scope | **Deploy config + reproducible docs** | Static-export config + `DEPLOY.md` runbook + prod Supabase/OAuth wiring; verified locally (`next build` export OK); operator runs the live Cloudflare/Supabase-cloud/OAuth-app steps (mirrors how `.3`/`.6` deferred live external hops) |

  **Assumptions carried into execution:** (a) Static export is keyed (env-conditional `BUILD_TARGET=cloudflare`) so the default/local `next build` + `next dev` + submodule adopters are unaffected. (b) **No generate-UI change** in this task — the deployed hosted Generate currently throws the existing non-fatal "Generation failed" toast; making it a graceful "unavailable / generate-in-local-and-import" affordance is generation-strategy work owned by [[BI-022.9]]; this task only documents the limitation. (c) Live deploy steps (Cloudflare Pages project, prod Supabase project, Google/GitHub OAuth-app registration) are operator-run from the runbook, not executed in-session.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

The deploy half is genuinely easy: the app is purely client-rendered (no RSC data work, no API routes, no route handlers, no `'use server'`), so it qualifies for a Next **static export** to Cloudflare Pages — no `@cloudflare/next-on-pages`/edge-worker complexity. The hard half is generation: blastimage's entire generation model is the **Grok Build sandbox** (`image_gen` built-in + agent-installed `__grokImagineProvider` + `node:fs` filesystem→data-URL). A deployed user browser has none of that, and the PLAN line's "Grok Build CLI auth so the provider seam resolves" assumes a mechanism that does not exist. `.1` itself flagged this as the least-defined / spike-prone and contemplated a split. Resolved per clarification: **split generation into [[BI-022.9]]**, ship deploy now.

**Downstream-impact reconciliation (Re-scope → new filing).** Active PLAN beyond the cohort: High/Medium/Low = `(none)`; only the `BI-EPIC-022` cohort is active.

| Entry | Classification | Reconcile action |
|---|---|---|
| `BI-022.5` (this) | contradictory (scope shrank) | **edit** — narrow the line to deploy-config + docs; drop the generation half; drop the stale `Blocked by` (both blockers now complete) |
| `BI-022.8` audit | unaffected | leave — remains the cohort-closing audit (brackets `.2`–`.7` + `.5` deploy); runs next |
| _new_ `BI-022.9` grok-hosted-gen | new child | **file** — spike: how (if at all) Grok-Imagine generation works in the deployed browser context (Grok Build sandbox absent); investigate xAI public image API / alternatives. Deferred future child, filed above the audit per SPEC/epic.md step 5 (post-audit follow-up numbering) |
| `BI-EPIC-022` parent | description refers to generation-in-hosted as in-scope of `.5` | **edit** — note generation-in-hosted deferred to `.9` |

Audit (`.8`) intentionally **not** renumbered: unlike the `.3`→`.6` oauth split (an implementation child that had to land before closure), `.9` is genuinely-uncertain deferred work that should not block the epic closing on its built scope. The `.8` audit closes the persistence+deploy cohort; if `.9` later lands it carries its own mini-audit per SPEC/epic.md step 5.

**Exit judgment:** Discovery surfaced a Re-scope verdict (generation split to new child `.9`, PLAN reconciliation incl. a new filing + parent-description edit) → **fire 🛠️**.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — confirmed the app is static-export-eligible: no `next/image` (no optimizer blocker), `next/font/google` (Geist) is export-safe, no dynamic routes / API routes / route handlers / `'use server'`. Followed the existing config-gating idiom (`isHostedMode()` reads an env var) by env-keying the export on `BUILD_TARGET=cloudflare` so the default build is untouched — same "additive, local byte-identical" principle the whole epic follows. Docs match the repo's existing `docs/*.md` style (title + intro + numbered `##` sections + `---` rules, as in `GROK-AGENT.md`/`ADOPT.md`).

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — N/A (build-config + docs only; the behavior is verified by the export build itself, below). No runtime code changed.

**Implementation Notes:**

- **`next.config.ts`** — env-gated static export: `BUILD_TARGET=cloudflare` → `{ output: 'export', images: { unoptimized: true } }`; otherwise `{}` (default server build). Keeps local `next build` / `next dev` / submodule adopters byte-identical.
- **`docs/DEPLOY.md`** (new) — full hosted-mode runbook: architecture rationale (static export, no edge worker, no server secrets), Supabase project create + `link`/`db push` (both migrations incl. the `images` bucket), production Google/GitHub OAuth-app registration (the BI-022.6-deferred live hop; secrets in the dashboard not config.toml), Cloudflare Pages build settings + env-var matrix (+ `wrangler pages deploy` alternative), auth URL configuration, a post-deploy verification checklist, and troubleshooting. Documents hosted generation as unavailable (→ BI-022.9), with the export/import migration as the workflow.
- **`.env.example`** — production OAuth-secret note now points to `docs/DEPLOY.md` (dashboard, not config.toml) instead of the bare "configured at deploy (BI-022.5)".
- **Live deploy not run in-session** per the scope decision — operator-run from the runbook (no Cloudflare/Supabase-cloud/OAuth-app creation).
- **No generate-UI change** — deferred to BI-022.9 per the Re-scope; the deployed hosted Generate keeps the existing non-fatal "Generation failed" toast, documented as a known limitation.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — full suite **101 pass** (unchanged; config/docs-only change moves nothing).

- [x] Ran lint/type-check on changed code — `tsc --noEmit` clean; `eslint next.config.ts` clean.

- [x] **Static-export build verification** (in place of a runtime test) — `BUILD_TARGET=cloudflare NEXT_PUBLIC_BLASTIMAGE_MODE=hosted NEXT_PUBLIC_SUPABASE_URL=… NEXT_PUBLIC_SUPABASE_ANON_KEY=… npm run build` → exports `out/` with `index.html` (6.6 kB) + assets, turbopack export OK (Next 15.5.19). Default `npm run build` (no `BUILD_TARGET`) → standard server build, **no `out/`** → confirms local/adopters unaffected.

- [x] (frontend) Visual confirmation — N/A: no UI change in this task (build config + docs only); the build artifact is the verifiable output, confirmed above.

**Testing Notes:** 101 offline tests green, tsc + eslint clean. Both build paths verified (gated export → `out/`; default → server build, no `out/`). No new client-bound IO or UI; the live deploy + sign-in round-trip is operator-run from `docs/DEPLOY.md`.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md` **updated** (added `docs/DEPLOY.md` to the Docs index) · `AGENTS.md` **no change** (flowtron-only; no deploy/feature surface) · `CLAUDE.md` **updated** (Stack §State: "deploy still in progress" → deploy/runbook in; in-browser hosted generation deferred to BI-022.9) · `.flowtron/PLAN.md` **updated** (Re-scope reconciliation applied at the 🛠️ gate; `.5` flipped to stub this closure).

- [x] Closed — PLAN.md `.5` line flipped to stub form `Completed 2026-06-16.` and tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] Recap drafted

**Final Summary:**

Shipped the hosted-mode deploy story for blastimage and split the unsolvable-as-written generation half into a new deferred child. **Re-scoped at Discovery:** the PLAN line bundled a well-defined deploy with a spike-prone "preserve Grok Build generation in hosted mode" — but blastimage's generation rides the Grok Build sandbox (`__grokImagineProvider` + `image_gen` + `node:fs`), which doesn't exist in a deployed browser, and there is no "Grok Build CLI auth" path; that half split into [[BI-022.9]] (grok-hosted-gen spike). This task narrowed to deploy-config + reproducible docs. Implementation: env-gated static export in `next.config.ts` (`BUILD_TARGET=cloudflare` → `output: 'export'`; default `next build`/`next dev`/adopters byte-identical — verified both paths build) and a full `docs/DEPLOY.md` runbook (Supabase create + `link`/`db push` of both migrations incl. the `images` bucket, production Google/GitHub OAuth-app registration — the BI-022.6-deferred live hop, secrets in the dashboard — Cloudflare Pages build settings + env matrix + `wrangler` alternative, auth URL config, post-deploy verification, troubleshooting). `.env.example` production-secret note points to the runbook. The live Cloudflare/Supabase-cloud/OAuth-app steps are operator-run from the runbook (no accounts touched in-session), mirroring how `.3`/`.6` deferred live external hops. No generate-UI change (deferred to `.9`; limitation documented). 101 tests green, tsc + eslint clean. Files: `next.config.ts`, `docs/DEPLOY.md` (new), `.env.example`, `README.md`, `CLAUDE.md`. With `.5` done, only the `.8` audit remains to close the built BI-EPIC-022 cohort (`.9` is deferred future work).

**Archived:** 2026-06-16
