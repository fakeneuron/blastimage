---
title: project-identity
status: completed
tags: []
created: 2026-09-20
due:
related-tasks: [BI-EPIC-053, BI-047, BI-053.3]
touches:
  - lib/useWorkspace.ts
  - lib/useWorkspace.test.ts
  - components/Sidebar.tsx
  - components/Sidebar.test.tsx
---

# BI-053.2 | project-identity

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-053]] · [[BI-047]] · [[BI-053.3]]

## 🎯 Goal

Widen BI-047 auto-naming so a still-default-named project takes the repo name on link (and a named one gets a "use repo name" offer), add a sidebar project header (name + repo label), and replace the project New/Rename `window.prompt`s with inline inputs.

## ✅ Acceptance

- [x] `linkImagegenFolder` auto-names via `projectNameFromRoot` whenever `session.name === DEFAULT_SESSION_NAME` (empty-tasks guard dropped); a non-default name is left alone — `npx vitest run lib/useWorkspace.test.ts`
- [x] Sidebar header shows the project name and, when linked, the repo label (`imagegenRootLabel`) with the absolute path as `title` — `npx vitest run components/Sidebar.test.tsx`
- [x] When linked and `name !== projectNameFromRoot(root)`, the header offers a "Use {derived}" control that calls `onRenameSession(derived)` — `npx vitest run components/Sidebar.test.tsx`
- [x] Project New and Rename no longer call `window.prompt`; they edit via inline inputs (Enter submits, Escape cancels). Task New/Rename prompts are unchanged — `npx vitest run components/Sidebar.test.tsx`
- [x] Typecheck and lint clean on the touched files — `npx tsc --noEmit` · `npm run lint`

## 🧩 Subtasks

- [x] Widen `linkImagegenFolder` auto-name: drop `tasks.length === 0`; keep the default-name check and the empty-derived skip
- [x] Rewrite the BI-047 "default-named with tasks stays" test to expect the rename; keep "operator-named stays"
- [x] Add a sidebar project header (name + `imagegenRootLabel` + absolute-path tooltip)
- [x] Add a "Use {derived}" offer in the header when linked and name ≠ derived
- [x] Replace project New/Rename `window.prompt` with local inline-input state (task prompts untouched)
- [x] Sidebar tests for header, offer, and inline New/Rename (no `window.prompt` on those two)
- [x] Update the Sidebar file-header comment that still describes native naming dialogs

## 🔗 Related

- [[BI-EPIC-053]] — parent epic (`session-round-navigation`)
- [[BI-047]] — depends-on: `projectNameFromRoot` / `imagegenRootLabel` / the original auto-name guard this child widens
- [[BI-053.3]] — sequential successor (round summaries + auto-ingest); Phase 3 visual confirm surfaced that BI-026 autoload + BI-043 replace wipes Approve chips on refresh — forwarded onto the `.3` PLAN line (`keep review decisions on already-loaded rounds`)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** First implementation child of BI-EPIC-053, filed today via .1. The BI-047 auto-name guard is still the narrow `DEFAULT_SESSION_NAME && tasks.length === 0` at `lib/useWorkspace.ts:1073`, the sidebar still has no project header, and project New/Rename still go through `window.prompt`. Scope matches the PLAN line.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; when the grep returns more than a handful of notes (~3 is a fair line), prefer handing the reading to a **probe**; log relevant findings in Discovery Notes before re-interpreting the task; an absent or empty `archive/<area>/` is a prompt to re-check `<area>` against the README table before logging "no prior tasknotes" — a derived-and-wrong folder is indistinguishable from a genuinely empty one

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit (omit only on a task with no file deliverable)

**Discovery Notes:**

**Read set:** `lib/useWorkspace.ts` (`DEFAULT_SESSION_NAME`, `createSession`, `renameSession`, `linkImagegenFolder`), `lib/workspace.ts` (`projectNameFromRoot`, `imagegenRootLabel`, `renameSession`), `components/Sidebar.tsx`, `components/Workspace.tsx` (Sidebar wiring), `lib/useWorkspace.test.ts` (BI-047 auto-name cases), `components/Sidebar.test.tsx` (a11y names), `components/ImagegenLinkModal.tsx` (in-modal offer idiom — not extended).

**Current auto-name (`lib/useWorkspace.ts:1073-1076`):**
```
if (next.name === DEFAULT_SESSION_NAME && next.tasks.length === 0) {
  const derived = projectNameFromRoot(root);
  if (derived) next = renameSessionName(next, derived);
}
```
`DEFAULT_SESSION_NAME` is `'My Website'` (file-private). `projectNameFromRoot` / `imagegenRootLabel` already exist and are tested in `lib/workspace.test.ts`. No `lib/workspace.ts` logic change needed.

**Current New/Rename:** `Sidebar.tsx:110-118` `window.prompt`. Task New/Rename at L120-128 stay (out of scope; `.5` may move them). No existing inline-edit pattern in the tree.

**Best Practices Review:** extend established seams, no new modules.
- Auto-name stays in `linkImagegenFolder` (orchestration; BI-049: add here, no parallel hook).
- Header and "Use {derived}" are presentational in `Sidebar`, computing `projectNameFromRoot(imagegenRoot)` locally and calling existing `onRenameSession` — no new `UseWorkspace` member, no `LinkOutcome` variant (the ImagegenLinkModal `owned` offer is a collision, not a rename).
- Inline New/Rename is local `useState` in Sidebar (same place that owns the prompts today). Do not extract a shared inline-input component for two fields.
- Dependency direction preserved: `workspace.ts` pure helpers ← `useWorkspace` orchestration ← `Sidebar` presentational.
- Deferred: sidebar regroup into header / rounds / ⋯ menu (BI-053.5); task `window.prompt`s; docs that name sidebar controls (also `.5`).

**Archive skim (`archive/bi/` confirmed via README Area prefixes `BI-` → `archive/bi/`; folder populated):**
- **BI-047** — auto-name guard is deliberately narrow ("a project the operator named or filled is not renamed"). `.2` widens the *filled* half (drop empty-tasks) and adds an explicit offer rather than silently overriding a named project. `imagegenRootLabel` + absolute-path tooltip already live on the link button; the new header reuses that pair for identity, not as a second link control.
- **BI-046** — retired `window.prompt` on the link path because a prompt can't carry a third choice; in-modal offer is the collision idiom. `.2` retires the project New/Rename prompts; does not extend the picker.
- **BI-053.1** — Fan-out Sequential, `.2` first (no `blocked-by`). Resolved scoping: identity auto-derives from linked root, manual rename kept. `.5` will regroup the header this child introduces.
- **BI-035.3** — Sidebar a11y names; keep `aria-label="New project"` / `"Rename project"` on the trigger buttons so existing tests still query them. Label-in-Name still applies to any new header controls.
- **BI-049** — `useWorkspace` stays whole; this child adds no hook split.

**Drift check:** PLAN line matches HEAD. `linkImagegenFolder` still has the two-clause guard; `imagegenRootLabel` is imported in Sidebar but only used on the link button, not a header; `window.prompt` still on all four New/Rename paths. Line numbers in .1 (`useWorkspace.ts` L1053 / L1073, `Sidebar.tsx` L132-247) still hold. No SPEC contract. Fan-out YAML omitted: `.2` is the sequence start (nothing to `blocked-by`); Parallel is none.

**Clarifications:** No clarifications needed. Assumptions:
1. "Still default-named" means `name === 'My Website'` only — drop `tasks.length === 0`. The BI-047 test that pins the old filled-project behaviour is rewritten to expect the rename.
2. "Offers use repo name otherwise" is a persistent header button `Use {derived}` when linked and `name !== derived` (and derived is non-empty). Not a new `LinkOutcome`, not a one-shot in the picker. Clicking it is `onRenameSession(derived)`.
3. Project header sits above the existing `<select>` + New/Rename; `.5` regroups. The select stays.
4. Inline New/Rename: click the existing buttons to reveal a text input; Enter submits, Escape cancels, empty blur/Enter is a no-op. Task New/Rename stay on `window.prompt`.
5. Docs that name sidebar controls land in `.5`, not here.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

Pattern survey: auto-name stays in `linkImagegenFolder` (BI-047 site); header reuses `imagegenRootLabel` / `projectNameFromRoot`; the "Use {derived}" offer is presentational in Sidebar and calls existing `onRenameSession` (no new `LinkOutcome`, no `UseWorkspace` member). Inline New/Rename is local `useState` in Sidebar — the same component that owned the prompts. No new module.

Minimal refactor: dropped `tasks.length === 0` from the auto-name guard; nothing else. The Enter-then-blur double-submit is guarded by `draftKindRef` because unmounting the focused input fires blur while `setState` still sees the old kind. Deferred: sidebar regroup (BI-053.5), task `window.prompt`s, docs that name sidebar controls (also `.5`).

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

```text
npx vitest run lib/useWorkspace.test.ts components/Sidebar.test.tsx  → 0  (64 tests)
npx tsc --noEmit                                                       → 0
npx eslint components/Sidebar.tsx components/Sidebar.test.tsx \
           lib/useWorkspace.ts lib/useWorkspace.test.ts               → 0
    (first lint pass failed jsx-a11y/no-autofocus on both draft inputs;
     replaced autoFocus with a mount-time ref focus. Re-lint clean.)
```

Structural: no new public hook member; `LinkOutcome` unchanged; `projectNameFromRoot` / `imagegenRootLabel` reused; Sidebar still the only owner of project naming UI. First lint miss (autoFocus) was in-scope, not leftover.

Browser (Playwright against :3003): Rename "My Website" → "Acme Site" via inline input; New "Beta" via inline input; Escape cancels New; linking `test-fixtures/imagegen` on operator-named Beta kept the name, showed repo label `test-fixtures/imagegen` and `Use test-fixtures`; clicking the offer renamed to `test-fixtures` and hid the button; switch back to Acme Site dropped the repo label. Mobile 375×667: header + inline Rename still present and focused.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed (standalone → top of `## Completed`; epic child → kept nested beneath its active parent — see SPEC/plan-filing.md §"`## Completed` archive convention" if unclear), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, the `touches:` scope reconciliation (`git diff --name-only` vs declared; name undeclared paths), and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

A linked project now takes its identity from the repo: still-default names auto-rename on link, a named project gets a persistent "Use {repo}" offer, and the sidebar header shows name + repo label. Project New/Rename edit inline instead of `window.prompt`.

- Changed: `lib/useWorkspace.ts` (drop empty-tasks auto-name guard) · `lib/useWorkspace.test.ts` · `components/Sidebar.tsx` (header, offer, inline drafts, `draftKindRef` double-submit guard) · `components/Sidebar.test.tsx`.
- Verify: `npx vitest run lib/useWorkspace.test.ts components/Sidebar.test.tsx` → 0 (64); `npx tsc --noEmit` → 0; eslint on those four files → 0. Operator 👁️ confirmed the header / offer / inline inputs at :3003.
- Refactors: none beyond the guard drop. Deferred: sidebar regroup and task prompts (`.5`); docs that name sidebar controls (`.5`).
- Doc-drift: `README.md`, `AGENTS.md`, `CLAUDE.md`, `VISION.md`, `docs/ADOPT.md`, `docs/WORKFLOW.md`, `docs/REVIEW-LOOP.md`, `docs/GROK-AGENT.md` — no change (sidebar copy is `.5`). `.flowtron/PLAN.md` — `.2` stubbed; `.3` line gained "keep review decisions on already-loaded rounds" after refresh was observed to wipe Approve chips (BI-026 autoload + BI-043 replace; not this child's ingest).
- `touches:` declared 4 files, changed 4 code files. Undeclared: none. PLAN.md + this tasknote excluded.
- Maintainability: project identity is visible without opening the link-button tooltip; naming no longer depends on `window.prompt`.

**Archived:** 2026-09-20
