---
title: iterate-discoverability
status: completed
tags: []
created: 2026-09-21
due:
related-tasks:
  - BI-EPIC-053
  - BI-009
  - BI-054
touches:
  - components/ReviewGrid.tsx
  - components/ReviewGrid.test.tsx
  - e2e/review-keyboard.spec.ts
  - e2e/iterate-feedback-modal.spec.ts
  - README.md
  - docs/WORKFLOW.md
---

# BI-055 | iterate-discoverability

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-053]] [[BI-009]] [[BI-054]]

## 🎯 Goal

Surface the keeper-then-iterate step on review cards so the next-round path is visible before Keep, without dropping the keep-first rule that Iterate only runs after Keep.

## ✅ Acceptance

- [x] Undecided cards render a disabled `Iterate →` with a Keep-this-image-first hint — `npx vitest run components/ReviewGrid.test.tsx`
- [x] Kept cards still offer an enabled `Iterate →` that reports `onIterate` — `npx vitest run components/ReviewGrid.test.tsx`
- [x] Discarded and approved cards still withhold `Iterate →` (approved is final) — `npx vitest run components/ReviewGrid.test.tsx`
- [x] e2e Keep still surfaces an enabled `Iterate →`; Approve still leaves none — `npx playwright test e2e/review-keyboard.spec.ts e2e/iterate-feedback-modal.spec.ts` (webServer blocked by Next lockfile on :3003, same as .3/.4/.5; covered by live :3003 pass)
- [x] README / WORKFLOW mention that undecided cards show `Iterate →` disabled until Keep — `grep -n 'Keep this image first\\|disabled until Keep\\|Iterate →' README.md docs/WORKFLOW.md`
- [x] Typecheck and lint clean on the touched files — `npx tsc --noEmit` · `npm run lint`
- [x] Disabled-until-Keep Iterate is visible on an undecided review card in the running app — `👁️` (operator: go, 2026-09-21)

## 🧩 Subtasks

- [x] `ReviewGrid` ReviewCard: show disabled `Iterate →` on `undecided` (`title="Keep this image first"`, existing disabled-opacity classes); keep the enabled button on `kept`; omit on `discarded` / `approved`
- [x] Update `ReviewGrid.test.tsx` BI-009 cases: disabled on undecided; still withheld on discarded/approved; enabled click still reports
- [x] Disambiguate e2e `Iterate →` locators with `{ disabled: false }` so a 2-image fixture after Keep-first does not match the sibling's disabled control
- [x] One-clause README + WORKFLOW update: undecided cards show the control disabled until Keep
- [x] Run targeted vitest + the two e2e specs + tsc/lint

## 🔗 Related

- [[BI-EPIC-053]] — related-decision: standalone Medium follow-up of session-round-navigation (sibling of [[BI-054]]); filed from [[BI-053.5]] visual confirm
- [[BI-009]] — depends-on: shipped keeper-only `Iterate →` (approved = final); this task nuances the undecided hide, not the keep-first rule
- [[BI-054]] — related-decision: sibling Medium follow-up (gallery-rail) filed in the same visual confirm

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Still a live UX gap: `ReviewGrid` withholds `Iterate →` until Keep, so undecided cards hide the next-round path. Keep-first (iterate only after Keep; never on approved) remains the intended rule.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; when the grep returns more than a handful of notes (~3 is a fair line), prefer handing the reading to a **probe**; log relevant findings in Discovery Notes before re-interpreting the task; an absent or empty `archive/<area>/` is a prompt to re-check `<area>` against the README table before logging "no prior tasknotes" — a derived-and-wrong folder is indistinguishable from a genuinely empty one

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit (omit only on a task with no file deliverable)

**Discovery Notes:**

**Read set.** `components/ReviewGrid.tsx` ReviewCard (iterate gated at `image.decision === 'kept'`, lines 192–202); `ReviewGrid.test.tsx` `describe('ReviewGrid — iterate (BI-009)')` pins withhold on `undecided`/`discarded`/`approved`; `TaskDetail` / `BulkReviewPane` only thread `onIterate` into the same grid; `Workspace.tsx` owns `iterateFor` + `IterateModal`; e2e `review-keyboard.spec.ts` + `iterate-feedback-modal.spec.ts` query `getByRole('button', { name: 'Iterate →' })` after Keep; fixture `test-fixtures/imagegen/rounds/r1/batch.json` has two images; README step 5 / WORKFLOW §7 describe Iterate as a kept-image action.

**Keep-first (operator, 2026-09-21).** Disabled `Iterate →` on undecided, with a Keep-this-image-first hint. Enabled only after Keep. Still hidden on discarded and approved. Rejected: auto-keep-on-click, hint-copy-only, pane banner.

**Best practices.** Card chrome stays in presentational `ReviewGrid` — do not lift this into `useWorkspace` / `IterateModal`. Nearby disabled-control classes: `disabled:cursor-not-allowed disabled:opacity-40` (`ImportBuilder`, `IterateModal`, `Sidebar`, `TaskDetail`). Hover reason via `title=` matches the Feedback button's tooltip idiom. No new component. No lib change.

**Archive skim (`archive/bi/`, confirmed against README Area prefixes `BI-` → blastimage; existing folder `archive/bi/`).** [[BI-009]] shipped keeper-only Iterate (approved = final) — the rule this task keeps; the undecided *hide* is what we nuance. [[BI-006]] deferred iterate here. [[BI-053.5]] filed this from visual confirm; [[BI-053.N]] treated it as already-filed, not an audit miss. TEST-002.2 (`ReviewGrid.test.tsx`) is the pin that currently asserts withhold-on-undecided. TEST-008 e2e will collide on a 2-image fixture once the sibling card shows a disabled control — locators must filter `{ disabled: false }`. [[BI-027]] `BulkReviewPane` reuses `ReviewGrid`, so one card change covers bulk review.

**Drift check.** PLAN.md line still matches HEAD: Iterate is hidden until Keep. No SPEC contract in play. No path/symbol drift.

**Clarifications.** Operator chose "Disabled Iterate → on undecided". Explicit assumptions: (1) same visible label `Iterate →` (no aria-label rewrite — WCAG 2.5.3 Label in Name); disabled native + `title="Keep this image first"` is the hint; (2) discarded and approved still omit the control; (3) native `disabled` means click does not call `onIterate`; (4) README + WORKFLOW get one clause that undecided shows the control disabled until Keep; REVIEW-LOOP is the file-loop SSOT and does not describe the card chrome, so leave it; (5) e2e Iterate queries that would match both cards after Keep-first-of-two use `{ disabled: false }`; (6) no `lib/` or `IterateModal` change.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

**Pattern survey.** Same presentational card chrome in `ReviewGrid`; disabled-control classes copied from `IterateModal` / `ImportBuilder` / `Sidebar` / `TaskDetail` (`enabled:hover:opacity-90 disabled:cursor-not-allowed disabled:opacity-40`). `title=` hint matches the Feedback button's tooltip idiom. No new component; `BulkReviewPane` covered because it renders `ReviewGrid`.

**Minimal refactor gate.** No refactor — the `decision === 'kept'` branch widened to also render a disabled control on `undecided`. Unrelated card density / Gallery rail left to [[BI-054]].

**Change.** `ReviewCard` shows `Iterate →` when `kept` or `undecided`; `disabled` + `title="Keep this image first"` on undecided; omitted on discarded/approved. Tests: undecided is now visible-disabled; discarded/approved still withheld. e2e Iterate locators use `{ disabled: false }` (and review-keyboard asserts two disabled Iterates before Keep). README step 5 / WORKFLOW §7 one clause each.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

```
npx vitest run components/ReviewGrid.test.tsx
  → 0  (26 tests)

npx playwright test e2e/review-keyboard.spec.ts e2e/iterate-feedback-modal.spec.ts
  → 1
    Error: Process from config.webServer was not able to start. Exit code: 1
    [WebServer] ⨯ Another next dev server is already running. (:3003 PID 36385)
    Same Next lockfile as BI-053.3/.4/.5. Live :3003 pass covered the same path
    (link fixture → two disabled Iterate → Keep enables first → Discard hides
    second → enabled Iterate opens IterateModal; Cancel writes nothing). Did
    not Approve (would dirty the committed fixture).

grep -n 'Keep this image first\|disabled until Keep\|Iterate →' README.md docs/WORKFLOW.md
  → 0  (README.md:62, docs/WORKFLOW.md:85)

npx tsc --noEmit  → 0
npm run lint      → 0
```

Structural: no extra public surface; no dead code; docs updated in lockstep with the card chrome. `fireEvent.click` on a native-disabled button is not asserted — testing-library dispatches through `disabled`, so the pin is `disabled` + `title`.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed (standalone → top of `## Completed`; epic child → kept nested beneath its active parent — see SPEC/plan-filing.md §"`## Completed` archive convention" if unclear), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, the `touches:` scope reconciliation (`git diff --name-only` vs declared; name undeclared paths), and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Undecided review cards now show `Iterate →` disabled (`Keep this image first`) so the next-round path is visible before Keep. The button enables after Keep and stays hidden on discarded and approved. Keep-first is unchanged.

**Doc-drift:** README.md updated (in-app loop step 5) · docs/WORKFLOW.md updated (§7) · AGENTS.md no change · CLAUDE.md no change · VISION.md no change · docs/ADOPT.md no change · docs/REVIEW-LOOP.md no change (file-loop SSOT, no card chrome) · docs/GROK-AGENT.md no change · PLAN.md flipped below.

**Verification:** vitest ReviewGrid 26/26 · tsc 0 · lint 0 · grep README/WORKFLOW 0 · playwright e2e webServer blocked by Next lockfile on :3003 (same as .3/.4/.5); live :3003 pass covered Keep/disable/discard/modal. Operator 👁️ go 2026-09-21.

**Refactors:** none. Gallery empty-rail still [[BI-054]].

**Scope reconciliation:** declared 6, changed 6. Undeclared: none. (tasknote + PLAN.md excluded)

**Maintainability:** TEST-002.2 pin now matches the keep-first surface (visible-disabled on undecided) instead of withhold-on-undecided, so a regression that hides Iterate again fails the unit test.

**Archived:** 2026-09-21
