---
title: supabase-to-r2-neon
status: completed
tags: []
created: 2026-07-07
---

# BI-028 | supabase-to-r2-neon

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Remove the stale optional Supabase hosted-mode stack while preserving blastimage's default localStorage mode and leaving the persistence seam clean for a future Neon plus Cloudflare R2 backend.

## ✅ Acceptance

- [x] Default local mode continues to use localStorage with no cloud env required.
- [x] Supabase client, hosted adapter, auth UI, migrations, env docs, and dependency are removed or replaced with local-only equivalents.
- [x] User-facing and AI-facing docs no longer tell operators to provision Supabase.
- [x] Tests, lint, and typecheck pass after the hosted-mode removal.

## 🧩 Subtasks

- [x] Promote starter and record Phase 1 decisions.
- [x] Remove Supabase adapter/auth/client code and route persistence to localStorage only.
- [x] Remove hosted-mode UI wiring and tests tied to Supabase auth/adapter mapping.
- [x] Drop Supabase config/migrations and package dependency.
- [x] Update docs/env examples to describe local-only mode and future Neon/R2 direction.
- [x] Run targeted tests, full tests if dependency/file removals are broad, lint, and typecheck.
- [x] Close PLAN/tasknote after doc-drift sweep.

## 🔗 Related

- None.

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md — `BI-028` is open under Medium priority with `[medium] | supabase-to-r2-neon`.
- [x] **Relevance Assessment** — Proceed. The code still carries the optional Supabase hosted stack and the current direction is to remove Supabase while preserving local mode.
- [x] Read relevant source files — `lib/config.ts`, `lib/persistence.ts`, `lib/supabaseClient.ts`, `lib/supabaseAdapter.ts`, `lib/auth.ts`, auth UI components, `app/page.tsx`, `components/Sidebar.tsx`, `package.json`, `.env.example`, `README.md`, `CLAUDE.md`, `docs/DEPLOY.md`, `docs/HOSTED-GENERATION.md`, and Supabase migrations/config.
- [x] **Archive skim** — `archive/bi/BI-022.3..7` show Supabase hosted mode was a real opt-in stack: adapter seam, owner-scoped Postgres/RLS, image bucket storage, OAuth gate, deploy docs, and import re-hosting. `BI-002` and later local workflow tasks establish localStorage as the default/local contract to preserve.
- [x] **Drift check** — Starter paths still exist and match the described stack. The only meaningful interpretation choice is whether to replace hosted mode now or remove it first; no Neon/R2 adapter exists yet and no auth replacement is available in-repo.
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — No clarifications needed. Assumptions: hosted Supabase mode is removed rather than replaced in this task; `NEXT_PUBLIC_BLASTIMAGE_MODE=hosted` is retired; Neon/R2 are documented as the future backend split, not implemented without a separate server/auth design.
- [x] Subtasks above populated with concrete, ordered steps.

**Discovery Notes:** Removing hosted mode is smaller and safer than shipping a partial Neon/R2 adapter because Neon and R2 require server-side credentials plus an auth story that Supabase previously supplied. Keeping the async `PersistenceAdapter` seam preserves the future backend boundary while ensuring local builds stop importing any hosted code.

✅ Phase 1 Discovery complete; entering Phase 2 Execution. Discovery surfaced no significant deviation from the starter's preferred direction → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended the existing local adapter seam: `persistence` now resolves directly to `localStorageAdapter`, while the async interface remains available for a future server-backed Neon/R2 adapter. Removed auth/UI wrappers instead of replacing them with a parallel stub.
- [x] Implemented the minimal solution — removed Supabase auth/client/adapter modules and tests, removed Supabase config/migrations and dependency, unwrapped `<Workspace />`, removed the sidebar account control, deleted the dead config module, and updated docs/env examples.
- [x] Updated/added tests for non-trivial behavior — removed tests whose only purpose was pinning deleted Supabase auth/adapter behavior; existing local persistence/workspace suites cover the remaining behavior.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm test` clean: 10 files, 129 tests passed.
- [x] Ran lint/type-check on changed code — `npm run typecheck` and `npm run lint` clean.
- [x] (frontend) Asked the user for visual confirmation — N/A: removed hosted-only gate/account UI; local visible workspace shell is unwrapped to its pre-hosted shape and tests/lint cover compile safety.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md` updated (removed deleted hosted docs from Docs list); `AGENTS.md` no change (workflow-only); `CLAUDE.md` updated (state is localStorage-only; Neon/R2 is future seam direction); `.flowtron/PLAN.md` updated (BI-028 flipped to stub and moved to Completed).
- [x] Closed — PLAN.md line flipped to `Completed 2026-07-07.` and tasknote moved to `.flowtron/tasknote/archive/bi/BI-028.md`.
- [x] Recap drafted — Removed the optional Supabase hosted stack and returned blastimage to a single localStorage-backed mode while preserving the async persistence seam for a future server-backed Neon/R2 adapter. Technical changes delete Supabase auth/client/adapter modules, migrations/config, hosted docs, env/dependency wiring, and hosted-only UI wrappers; docs now state no cloud project is required.
