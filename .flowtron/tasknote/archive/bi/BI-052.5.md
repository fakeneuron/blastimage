---
title: comment-claim-fossils
status: completed
tags: []
created: 2026-09-09
due:
related-tasks:
  - BI-EPIC-052
  - BI-052.2
  - BI-052.3
  - BI-052.4
  - BI-052.N
touches:
  - lib/types.ts
  - lib/generate.ts
  - CLAUDE.md
  - package.json
---

# BI-052.5 | comment-claim-fossils

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-052]] [[BI-052.2]] [[BI-052.3]] [[BI-052.4]] [[BI-052.N]]

## 🎯 Goal

Replace leftover comments and CLAUDE.md stats that still describe the pre-server / mock-era generation path, and pin the Node engine already required by Next 16.

## ✅ Acceptance

- [x] `lib/types.ts` `GeneratedImage.url` comment no longer cites mock picsum as a current URL shape
- [x] `lib/generate.ts` file header describes the live provider-bridge seam; mock/picsum history is past-tense (or only in tests)
- [x] `CLAUDE.md` Module-layout bullet says 1167 L for `useWorkspace.ts` (measured at start) and names `persistence` as the singleton, `ImagegenApi` as the injected seam
- [x] `package.json` has `"engines": { "node": ">=20.9" }` matching `docs/ADOPT.md` Prerequisites
- [x] No behavior change: generation, persistence, and UI code paths untouched except comments / engines metadata

## 🧩 Subtasks

- [x] Rewrite `GeneratedImage.url` JSDoc in `lib/types.ts`
- [x] Rewrite `lib/generate.ts` file header (and the "Updated for real path" request comment) to present-tense bridge language
- [x] Patch `CLAUDE.md` 1164→1167 and PersistenceAdapter-as-injected-seam wording
- [x] Add `engines.node >=20.9` to `package.json`
- [x] Confirm no other production comments still claim picsum is the live URL shape

## 🔗 Related

- [[BI-EPIC-052]] — parent epic: post-server-doc-currency
- [[BI-052.2]] — sibling: GROK-AGENT.md rewrite (closed; deferred generate.ts / CLAUDE.md fossils here)
- [[BI-052.3]] — sibling: ADOPT.md §4 rewrite (closed; same deferral)
- [[BI-052.4]] — sibling: Phase-4 walk-list (closed; same deferral)
- [[BI-052.N]] — terminal audit
- [[BI-049]] — related-decision: authored the 1164 L keep-it-whole claim now stale by 3 lines
- [[BI-013]] — related-decision: swapped generate.ts off the picsum mock
- [[BI-022.2]] — related-decision: PersistenceAdapter seam; hook imports the `persistence` singleton, does not inject the interface

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** All four cited fossils are still at HEAD. `GeneratedImage.url` still says "Data URL (mock picsum, BI-007)"; `generate.ts` header still narrates BI-007 mock as if it were the module's identity; CLAUDE.md still says 1164 L and lists PersistenceAdapter as an injected seam; `package.json` has no `engines`. The claims are wrong, the files are the right files, and siblings already closed around this leftover.

- [x] Read relevant source files — `lib/types.ts` (`GeneratedImage.url`), `lib/generate.ts` (file header + `GenerationRequest`), `CLAUDE.md` Module layout, `package.json`, `docs/ADOPT.md` Prerequisites, `lib/persistence.ts` + `lib/useWorkspace.ts` import of `persistence`, `lib/generate.test.ts` (picsum mock lives only there)

- [x] **Best Practices Review** — comment/doc-currency only; no module-boundary change. Preserve the generate.ts / types.ts JSDoc voice. `engines` is metadata already stated in ADOPT.md; do not add `engine-strict`. Leave `generate.test.ts` picsum mock as the test-only stand-in.

- [x] **Archive skim** — no `BI-052.1` (audit-repo supplied Discovery); no Fan-out YAML to echo. Load-bearing: [[BI-052.2]] / [[BI-052.3]] / [[BI-052.4]] each deferred generate.ts + CLAUDE.md fossils to this child. [[BI-049]] wrote the 1164 L / injected-seams sentence (measured 1164 on 2026-08-30; now 1167). [[BI-013]] performed the mock→bridge swap; [[BI-007]] authored the picsum mock (now test-only). [[BI-022.2]] put PersistenceAdapter behind the hook as a singleton export, not a constructor-injected dependency. Path grep on CLAUDE.md is too broad (every Phase-4 sweep); no extra load-bearing notes.

- [x] **Drift check** — PLAN-cited fossils match HEAD: `lib/types.ts:96` picsum JSDoc; `lib/generate.ts:1-18` mock-era header; CLAUDE.md "1164 L" and "injected seams (`PersistenceAdapter`, `ImagegenApi`)"; no `engines` in `package.json`. Measured `wc -l lib/useWorkspace.ts` = 1167, `lib/storage.ts` = 694 (ratio still 1.68x). SPEC: no contract touched. PLAN line unchanged. `engines` is marked optional on the PLAN line; included because ADOPT.md already requires Node ≥ 20.9 for Next 16.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

No clarifications needed. Assumptions: (1) include the optional `engines` field — it restates a requirement already in `docs/ADOPT.md` and Next 16's floor, and leaving it out would just bounce to [[BI-052.N]]; (2) keep the `referenceImages` field's one-line mock-era rename note — PLAN names the *file header*, not every historical footnote; (3) do not restyle or re-count the 26/33/42 figures; only the cited 1164→1167 and seam wording; (4) picsum URLs in tests (`generate.test.ts`, `workspace.test.ts`) stay — they are fixtures, not claims.

---

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

Pattern: same JSDoc voice as the rest of `types.ts` / `generate.ts`; CLAUDE.md Module-layout sentence kept as one bullet. `engines` placed after `private` to match common npm layout and ADOPT.md's Node ≥ 20.9 floor.

Minimal refactor: none. Deferred: `lib/useWorkspace.ts:714` mock-era comment (not on the PLAN line); GROK-AGENT.md historical picsum paragraph (already rewritten by [[BI-052.2]], past-tense on purpose); picsum URLs in tests.

Tests: comments + engines metadata — no new tests.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

- `npx vitest run lib/generate.test.ts lib/workspace.test.ts` → 71 passed / 71
- `npx tsc --noEmit` → exit 0
- `npm run lint` → clean
- Quality: no behavior change; remaining picsum mentions are tests, a past-tense history paragraph, or GROK-AGENT.md's historical mock-era note. Visual confirmation N/A — no rendered surface.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Replaced mock-era / pre-server comment fossils so types, the generation seam, and CLAUDE.md describe the live bridge and measured file size, and pinned Node ≥ 20.9 in package.json to match ADOPT.md.

**Changed** — `lib/types.ts` (url JSDoc), `lib/generate.ts` (file header + request comment), `CLAUDE.md` (1164→1167; seam wording), `package.json` (`engines.node`).

**Verified** — generate + workspace tests 71/71; tsc and lint clean.

**Refactors** — none. Deferred: useWorkspace.ts:714 mock-era footnote; test picsum fixtures.

**Documentation verdict.** `README.md` — no change. `AGENTS.md` — no change. `CLAUDE.md` — **updated** (this task). `.flowtron/PLAN.md` — this child stubbed, kept nested under BI-EPIC-052. `VISION.md` / `docs/ADOPT.md` / `docs/WORKFLOW.md` / `docs/REVIEW-LOOP.md` / `docs/GROK-AGENT.md` — no change.

**Maintainability.** Cold-start CLAUDE.md no longer contradicts `wc -l` or the actual injection vs singleton split; `npm` will warn on Node < 20.9 instead of only the install guide saying so.

**Archived:** 2026-09-09
