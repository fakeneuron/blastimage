---
title: iterate-save-ack
status: completed
tags: []
created: 2026-09-23
due:
related-tasks: [BI-024.2]
touches:
  - lib/useWorkspace.ts
  - lib/useWorkspace.test.ts
  - components/Workspace.tsx
  - components/Workspace.test.tsx
  - e2e/iterate-feedback-modal.spec.ts
---

# BI-057 | iterate-save-ack

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-024.2]]

## 🎯 Goal

After a successful Iterate "Save selection request", tell the user that `selection.json` was written and that the next step is `/blast-iterate` then ↻ Refresh rounds.

## ✅ Acceptance

- [x] A successful `requestNextRound` sets a notice that names `rounds/r<N>/selection.json`, `/blast-iterate`, and `Refresh rounds` — `npx vitest run lib/useWorkspace.test.ts -t "iterate save ack"`
- [x] A failed write sets `error` and does not claim the file was saved — same command
- [x] The workspace shows that notice as a dismissible status, separate from the red error banner — `npx vitest run components/Workspace.test.tsx -t "iterate save ack"`
- [x] Saving from the iterate modal in the browser leaves the dialog and shows the same status — `npx playwright test e2e/iterate-feedback-modal.spec.ts -g "writes selection.json"`

## 🧩 Subtasks

- [x] Add a success `notice` on `useWorkspace`, set only after `writeSelection` succeeds
- [x] Render it as a dismissible status banner in `Workspace`
- [x] Pin the hook, the banner, and the existing iterate e2e
- [x] Run the verify commands

## 🔗 Related

- [[BI-024.2]] — depends-on: iterate submit already writes `selection.json` via `requestNextRound` and then closes the modal with no success feedback

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** The modal still closes immediately after submit, and `requestNextRound` only clears `error` on success, so Viewer-mode iterate still looks like a no-op. The filed fix — say the file was written and name the next step — is still the gap.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; when the grep returns more than a handful of notes (~3 is a fair line), prefer handing the reading to a **probe**; log relevant findings in Discovery Notes before re-interpreting the task; an absent or empty `archive/<area>/` is a prompt to re-check `<area>` against the README table before logging "no prior tasknotes" — a derived-and-wrong folder is indistinguishable from a genuinely empty one

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit (omit only on a task with no file deliverable)

**Discovery Notes:**

Area is `bi`. README §"Archive layout" has no `BI-*` table row; the following sentence declares project-specific prefixes under their own subfolder, and `archive/bi/` is populated (BI-001 onward, including BI-054). Not derived from the ID in isolation.

**Read set.** `IterateModal` submit is `onSubmit(prompt.trim())` and does not itself close. `Workspace.tsx` calls `void ws.requestNextRound(...)` then `setIterateFor(null)`, so the dialog unmounts before the write finishes. `requestNextRound` (`lib/useWorkspace.ts`) writes via `imagegen.writeSelection` and on success only `setError(null)`. The red banner (`Workspace.tsx`) is `bg-red-600` and is the failure surface. Sidebar's refresh control is visible text `↻ Refresh rounds` with accessible name `Refresh rounds`.

**Best practices.** Orchestration stays in `useWorkspace` (BI-049: one hub). The notice is a sibling of `error`, not a second hook and not copy inside the presentational modal — the modal is already gone when the write resolves, and claiming "saved" before `writeSelection` returns would be a lie. Do not reuse the red banner for a success. No new component. `ImportBuilder`'s local `notice` is inline field warning copy; not this surface.

**Archive skim.** Path grep for `requestNextRound` / `IterateModal` across `archive/bi/` is large. Load-bearing hits, not a full re-read: **BI-024.2** moved iterate off in-browser generate onto `requestNextRound` and updated the modal copy to "save a selection request"; success still only clears the error. **BI-009** is the original modal (then it generated). **BI-055** changed when Iterate is enabled, not what happens after save. No `supersedes:` or ⚠️ pointer on those hits changes this ack. No prior note filed the missing success message — that is this task.

**Drift check.** PLAN line matches: button label is still `Save selection request`; close-with-no-feedback is still true; next step is still `/blast-iterate` then refresh. No SPEC contract covers this UI string. Plan forming here does not rewrite the PLAN line.

**Clarifications.** No clarifications needed. Assumptions: (1) the modal still closes on save; the ack is a workspace status banner after the write, not text left in the dialog; (2) the banner is set only when `writeSelection` returns ok, and a failure keeps the red error and clears any prior success notice; (3) copy is `Saved rounds/r<N>/selection.json. Next: run /blast-iterate, then ↻ Refresh rounds.`; (4) README / REVIEW-LOOP already describe the loop, so they do not change; (5) in-app generation is out of scope.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

`notice` sits beside `error` on `useWorkspace`. `requestNextRound` sets it only after `writeSelection` returns ok, and clears it on the linked-folder miss, the non-round image miss, and a failed write. `Workspace` renders it as `role="status"` with `bg-foreground` (the red `bg-red-600` strip stays errors only). Dismiss calls `dismissNotice`. No new component. No refactor beyond that field.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] **External review** — a context that did not write the diff graded it against `## ✅ Acceptance`, and every finding is recorded below with its disposition (**blocker** → back to Phase 2; **note** → fixed or filed). `N/A` with a one-line reason when the diff is too small to grade

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

- `npx vitest run lib/useWorkspace.test.ts components/Workspace.test.tsx -t "iterate save ack"` → 0 (4 passed, 58 skipped in those files).
- `npx tsc --noEmit` → 0.
- `npx eslint` on the five touched paths → 0.
- `npx playwright test e2e/iterate-feedback-modal.spec.ts -g "writes selection.json"` → 1. First failure line: `Error: Process from config.webServer was not able to start. Exit code: 1` (`Another next dev server is already running` on :3003, PID 36385). Re-ran the same spec against that live server (baseURL `http://localhost:3003`, no second webServer) → 0, 1 passed. The assertions (dialog hides; status contains `rounds/r1/selection.json`, `/blast-iterate`, `Refresh rounds`) decided the criterion.
- Structural: one notice string, no second hook, public surface grew only by `notice` / `dismissNotice` which Acceptance required. File header on `Workspace.tsx` updated. No dead code.
- External review (read-only subagent, not the author): findings `none`. No blocker. It graded the five files' contents and did not re-run the suites.
- Operator, 2026-09-24: proceed as-is. They confirmed r3 landed after `/blast-iterate` and did not ask for another pass over the status line. The live Playwright spec already asserted that line.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed (standalone → top of `## Completed`; epic child → kept nested beneath its active parent — see SPEC/plan-filing.md §"`## Completed` archive convention" if unclear), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, the `touches:` scope reconciliation (`git diff --name-only` vs declared; name undeclared paths), and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

- [x] **Learnings** — did this task teach something the always-loaded layer (AGENTS.md / README §AI-referenced docs) should carry? `N/A` or the line

**Final Summary:**

After Save selection request, a dismissible status line names the written `rounds/r<N>/selection.json` and the next step (`/blast-iterate`, then ↻ Refresh rounds). The modal still closes. A failed write stays on the red error banner and clears any prior success notice.

Five code paths, about +146 / −3. `lib/useWorkspace.ts` owns `notice`; `components/Workspace.tsx` renders it. Tests: hook, workspace banner, and the existing iterate e2e. No refactor. Docs: README, AGENTS.md, CLAUDE.md, VISION.md, ADOPT.md, WORKFLOW.md, REVIEW-LOOP.md, GROK-AGENT.md — no change. PLAN.md stub only. `touches:` matches the diff (those five files); the tasknote and PLAN.md are closure paperwork, not undeclared product paths. Maintainability: success is no longer the absence of the red banner.

Learnings: N/A. The loop was already documented; this is in-app feedback only.

**Archived:** 2026-09-24
