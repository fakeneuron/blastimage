---
title: blob-url-lifetime audit
status: completed
tags: []
created: 2026-08-09
due:
related-tasks: [BI-EPIC-042, BI-042.2, BI-042.3, BI-042.4]
---

# BI-042.N | blob-url-lifetime audit

[← PLAN.md](../../PLAN.md) · ✅ Completed · 🔗 [[BI-EPIC-042]]

## 🎯 Goal

Verify the completed BI-EPIC-042 (`blob-url-lifetime`) cohort sits coherently in the codebase: cumulative doc-drift sweep across `.flowtron/tasknote/README.md` §"AI-referenced docs", naming/style consistency across the cohort's deliverables, and follow-up filings for any miss.

## ✅ Acceptance

- [x] **Doc-drift sweep (fixed line, per SPEC/epic.md §"Audit acceptance — fixed doc-drift line")** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the specific update. Always present; surfaces cumulative slice-local staleness that per-task Phase 4 closures can miss.
- [x] Cohort coherence inventory: each implementation child's deliverables read against the others (naming consistency, style parity, no contradictory cross-refs)
- [x] No regressions surfaced in earlier-shipped cohort children's surfaces
- [x] Audit findings recorded in Implementation Notes; misses cited as candidates for `/ft-file-followup <NEW-ID>` filing (filed AFTER audit closure to preserve `/ft-file-followup`'s filing-discipline gate)
- [x] Single `feat: BI-042.N — audit BI-EPIC-042` (or `chore: ...` if no code edits land) commit lands
- [x] PLAN.md line for `BI-042.N` flipped to stub form `Completed YYYY-MM-DD.`
- [x] Tasknote moved to `.flowtron/tasknote/archive/bi/BI-042.N.md`
- [x] Parent-flip prompt surfaced after audit closure (skill Step 8) — user confirms or declines flipping `BI-EPIC-042` to `Completed` and moving the cohort to `## Completed`

## 🧩 Subtasks

- [x] Inventory cohort children's archived tasknotes — read each implementation child's Final Summary + Implementation Notes; capture deliverables in Discovery Notes
- [x] Walk `.flowtron/tasknote/README.md` §"AI-referenced docs" entries — fixed doc-drift sweep
- [x] Cohort coherence pass — naming consistency, style parity, no contradictory cross-refs across the cohort's deliverables
- [x] Surface audit findings in Implementation Notes; cite each miss as a `/ft-file-followup <NEW-ID>` candidate
- [x] Phase 4: flip `BI-042.N` PLAN line to stub form + archive tasknote
- [x] Parent-flip: skill Step 8 prompts user; on confirm, atomic flip parent line + move cohort to `## Completed`

## 🔗 Related

- [[BI-EPIC-042]] — parent epic (blob-url-lifetime)
- [[BI-042.2]] — revocation-epoch (mechanism)
- [[BI-042.3]] — consumer-recovery-test (component contract)
- [[BI-042.4]] — memoize-imagegen-value (context value identity)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** User invoked `/ft-close-epic BI-042.N`. All implementation children closed 2026-08-09 (`BI-042.2`, `.3`, `.4` — all `[x]`). No open siblings; early-audit gate not triggered. No `.1` Discovery child (PLAN notes "Discovery supplied by audit-repo 2026-08-09") — cohort is mechanism + consumer pin + memo, not a missing implementation slot.

- [x] Read relevant source files

- [x] **Best Practices Review** — N/A with reason: verification-only audit; no new code surface. Cohort responsibilities already partitioned: provider revocation semantics (`.2`), consumer contract pin (`.3`), context-value identity (`.4`).

- [x] **Archive skim** — skimmed cohort archives + prior path history (`BI-029.4` blob cache, `BI-038` `linked` re-resolve). `BI-029.4` carries the superseded-claim pointer from `.2`. No non-cohort archive contradicts the shipped split-path design.

- [x] **Drift check** — cited paths and symbols match HEAD:
  - `lib/ImagegenContext.tsx` — `blobEpoch`, `retainDisplayUrl`, soft-bound `evictOldestBlob`, count-returning `invalidateRoundBlobs`, `useMemo` on value
  - `components/ResolvedImage.tsx` — split `[src]` reset + resolve deps include `blobEpoch` + retain effect
  - `lib/useWorkspace.ts` / `lib/useWorkspace.test.ts` — `NOOP_IMAGEGEN` / recording stubs carry both new members
  - Tests: `lib/ImagegenContext.test.tsx` (provider + identity), `components/ResolvedImage.test.tsx` (consumer recovery)

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  **No clarifications needed.** Assumptions:
  1. Audit scope is the three closed implementation children (`.2`–`.4`); no `.1` exists by design.
  2. Operator visual repro deferred in `.2` remains operator-only and is not an audit miss to re-file.
  3. No code fixes unless a clear in-scope staleness is found.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Cohort inventory (from archived Final Summaries + Implementation Notes):**

| Child | Shortname | Deliverable |
|-------|-----------|-------------|
| **BI-042.2** | revocation-epoch | Mechanism: `blobEpoch` bumped only on staleness (`invalidateRoundBlobs` → `readRound`); `retainDisplayUrl` + held-set so LRU eviction skips live displays (soft bound); split `[src]` reset so epoch bumps don't flash raw URLs. Files: `ImagegenContext.tsx`, `ResolvedImage.tsx`, `useWorkspace.ts` (+ test stubs), six provider tests. |
| **BI-042.3** | consumer-recovery-test | Component pin: three `ResolvedImage` tests (same-round reload → live remint; LRU survival via retain; no raw flash on unrelated-round bump). Test-only; `stubBlobUrls` re-mint + `live` set. |
| **BI-042.4** | memoize-imagegen-value | `useMemo` on context value deps `[linked, blobEpoch, …callbacks]`; identity-stability test. Stops pure provider re-renders from re-walking `listRounds()` in `useWorkspace`. |

**Layering (intentional, not a gap):** `.2` mechanism → `.3` consumer contract → `.4` memo polish. Cross-refs in tasknotes and code comments align (each sibling names the deferred handoff; none re-open a closed decision).

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — N/A (verification pass; no new code surface)

- [x] **Minimal refactor gate** — N/A; no inline fixes required

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — N/A (no code change)

**Implementation Notes:**

**Cohort children inventoried** — see Discovery table above.

**Coherence findings — no inconsistencies surfaced:**

1. **Opposite-path design is consistent end-to-end.** Staleness revokes + bumps epoch; eviction skips held URLs and stays silent. Doc comments on `evictOldestBlob` / `invalidateRoundBlobs` / `ImagegenApi.blobEpoch` state the asymmetry; `.3` mutation-checks both consumer sides and the split `[src]` effect.
2. **Naming/style parity.** `blobEpoch` / `retainDisplayUrl` used uniformly in API, consumer, NOOP, and tests. Retain-returns-release disposer pattern is the single hold shape.
3. **No contradictory cross-refs.** `.2` deferred `.3`/`.4`; both landed as scoped; `BI-029.4` superseded pointer remains accurate.
4. **Regression check.** Targeted suite green: `npx vitest run lib/ImagegenContext.test.tsx components/ResolvedImage.test.tsx` → 22/22 passed (10 + 12). HEAD still has all six provider eviction/epoch tests, three consumer-recovery tests, and one identity-stability test.
5. **Missing `.1`.** Not a miss — PLAN records audit-repo Discovery; implementation cohort is complete without it.

**Inline fixes applied:** none.

**Misses / `/ft-file-followup` candidates:** none.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — N/A (no code change); regression suite run for audit verification

- [x] Ran lint/type-check on changed code — N/A (no code change)

- [x] **Quality assertions** — N/A with reason: audit is prose + PLAN/archive ops only

- [x] (frontend) Asked the user for visual confirmation — N/A (no UI change in this audit; `.2`'s operator visual repro remains deferred as filed)

**Testing Notes:**

- `npx vitest run lib/ImagegenContext.test.tsx components/ResolvedImage.test.tsx` → **22 passed** (10 + 12).
- No production or test edits in this audit.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update
  - `README.md` — **no change.** User-facing product surface; no blob-URL / thumbnail lifetime claims that the cohort falsifies or needs to document.
  - `AGENTS.md` — **no change.** Flowtron paste-block / assistant guide only.
  - `CLAUDE.md` — **no change.** Stack/testing conventions; cohort adds no dependency, gate, or agent convention.
  - `.flowtron/PLAN.md` — **updated.** `BI-042.N` flipped to Completed stub (kept nested under active parent until parent-flip Yes); parent flip + cohort move to `## Completed` applied on operator confirm in Step 9.

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-08-09.`, kept 2-space nested beneath the active `BI-EPIC-042` parent; tasknote moved to `.flowtron/tasknote/archive/bi/BI-042.N.md`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

BI-EPIC-042 audit found a coherent three-child cohort: observable staleness via `blobEpoch`, held-set soft-bound eviction, component-level recovery pins, and memoized context identity — no inconsistencies and no follow-ups to file.

**Verification:** cohort test surfaces 22/22 green; AI-referenced docs unchanged except PLAN/archive ops.

**Parent-flip:** Yes — parent stubbed and full cohort moved to top of `## Completed` (2026-08-09).

**Archived:** 2026-08-09
