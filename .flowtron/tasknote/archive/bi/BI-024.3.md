---
title: gen-iterate-skills
status: completed
tags: []
created: 2026-06-18
due:
related-tasks: [BI-EPIC-024, BI-024.1, BI-024.2]
---

# BI-024.3 | gen-iterate-skills

[← PLAN.md](../../PLAN.md) · ✅ Completed · 🔗 [[BI-EPIC-024]] · [[BI-024.1]] · [[BI-024.2]]

## 🎯 Goal

Ship `/blast-generate` and `/blast-iterate` Grok Build terminal skills that read `imagegen/` prompts, refs, and selection files, run `image_gen`/`image_edit`, write round directories + `batch.json`, and document the round-0 ref-bootstrap pass — adopter-installable from the blastimage submodule.

## ✅ Acceptance

- [x] `.grok/skills/blast-generate/SKILL.md` drives prompt+ref round generation and round-0 bootstrap
- [x] `.grok/skills/blast-iterate/SKILL.md` drives next-round generation from `selection.json`
- [x] Shared pure helpers (`lib/terminalRound.ts`) match `docs/REVIEW-LOOP.md` §3 contracts
- [x] `docs/ADOPT.md` documents adopter skill installation
- [x] Unit tests cover terminal-round helpers; lint/type-check clean

## 🧩 Subtasks

- [x] `lib/terminalRound.ts` — slug/ref/round planning + batch.json builder
- [x] `lib/terminalRound.test.ts` — contract tests
- [x] `.grok/skills/blast-generate/SKILL.md` — generate + round-0 ref-bootstrap workflow
- [x] `.grok/skills/blast-iterate/SKILL.md` — iterate-from-selection workflow
- [x] `docs/ADOPT.md` §5.1 + §7 — symlink/copy from `blastimage/.grok/skills/`

## 🔗 Related

- [[BI-EPIC-024]] — parent epic (terminal-generate / frontend-review loop)
- [[BI-024.1]] — load-round ingest (completed; `batch.json` reader)
- [[BI-024.2]] — next-round request (completed; `selection.json` writer)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Third child of BI-EPIC-024; completes the terminal half of the loop. Frontend ingest (BI-024.1) and emit (BI-024.2) are done — this task ships the Grok Build skills that satisfy `docs/REVIEW-LOOP.md` §1–3. No blockers.

- [x] Read relevant source files

- [x] **Archive skim** — BI-024.1/2 established `roundBatch.ts`, `roundSelection.ts`, `imagegenFs.ts`, slugify via `storage.ts`. BI-020 filed canonical `imagegen/` layout in ADOPT.md §7. BI-023 confirmed browser generation wall.

- [x] **Drift check** — PLAN paths, `docs/REVIEW-LOOP.md` §3 schemas, `DEFAULT_BATCH_SIZE=4`, image naming `<slug>-NNN.<ext>` in test fixtures all match. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

Assumptions: (1) skills live at `blastimage/.grok/skills/` and adopters symlink into host `.grok/skills/`; (2) terminal session cwd is the **host repo root** (`imagegen/` sibling to `blastimage/` submodule); (3) batch size 4 matches `DEFAULT_BATCH_SIZE`; (4) ref path convention `refs/<slug>.<ext>` per REVIEW-LOOP; (5) round-0 bootstrap copies keepers from post-review `selection.json` into `refs/` before the real seeded run; (6) pure planning logic in `lib/terminalRound.ts` for testability — skills orchestrate `image_gen`/`image_edit` procedurally.

Discovery surfaced no significant deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — mirrored BI-024.1/2 contract split: pure planning in `terminalRound.ts`, procedural orchestration in `.grok/skills/*/SKILL.md` (agent runs `image_gen`/`image_edit`).

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

`lib/terminalRound.ts` covers slug/ref indexing, round numbering, batch.json build, iterate planning (append→`image_edit`, overhaul→`image_gen`), and round-0 ref-bootstrap copy paths. Skills reference `docs/REVIEW-LOOP.md` + `imagine` skill; ADOPT.md §5.1 documents symlink install.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm test` 132/132 green (+10 new)

- [x] Ran lint/type-check on changed code — `tsc --noEmit` clean; `eslint` clean

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

No frontend UI changed — skills + lib helpers only. 👁️ N/A.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md` no change (loop docs land in BI-024.4 audit); `AGENTS.md` no change; `CLAUDE.md` no change; `docs/ADOPT.md` updated §5.1 + §7 `rounds/` layout; `.flowtron/PLAN.md` updated

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-06-18.` and tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Shipped adopter-installable `/blast-generate` and `/blast-iterate` Grok Build terminal skills (`.grok/skills/`) plus pure `lib/terminalRound.ts` helpers for round planning, batch.json build, iterate/append-vs-overhaul routing, and round-0 ref-bootstrap copy paths. ADOPT.md §5.1 documents symlink install; §7 adds `rounds/` to the canonical layout. 132 tests green.

**Archived:** 2026-06-18