---
title: loopback-bind-scripts
status: completed
tags: []
created: 2026-09-24
due:
related-tasks: [BI-EPIC-058]
touches:
  - package.json
  - playwright.config.ts
  - CLAUDE.md
  - docs/GROK-AGENT.md
---

# BI-058.2 | loopback-bind-scripts

[← PLAN.md](../PLAN.md) · ✅ Completed · 🔗 [[BI-EPIC-058]]

## 🎯 Goal

Bind the Next `dev` and `start` scripts and the Playwright webServer to 127.0.0.1, leaving `baseURL` on localhost.

## ✅ Acceptance

- [x] `dev` and `start` in `package.json` pass `-H 127.0.0.1` — `node -e` on `package.json` scripts
- [x] Playwright `webServer.command` passes `-H 127.0.0.1` and `baseURL` stays `http://localhost:3009` — `node -e` assertion on `playwright.config.ts` source
- [x] The same `-H 127.0.0.1` argv listens on loopback — `lsof -nP -iTCP:3013 -sTCP:LISTEN` showed `127.0.0.1:3013`. Port 3003 was already held by a pre-existing listener (`*:3003`, PID 36385) and was left running; `lsof` on 3003 still shows `*:3003` until that process is restarted from `npm run dev`.

## 🧩 Subtasks

- [x] Add `-H 127.0.0.1` to `package.json` `dev` and `start`
- [x] Add `-H 127.0.0.1` to the Playwright webServer command; leave `baseURL` on localhost
- [x] Update the two documented `next dev -p 3003` invocations so they match the bound command
- [x] Start Next with the same `-H` argv, confirm `lsof` shows 127.0.0.1, then stop that server

## 🔗 Related

- [[BI-EPIC-058]] — parent; loopback bind so Host checks cannot be forged from the LAN
- [[BI-058.3]] — follow-up; threat-model prose in `lib/imagegenGuard.ts` and the CLAUDE.md Stack note (out of scope here)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `next dev` / `next start` default hostname is `0.0.0.0` (`--help`). The scripts and Playwright webServer omit `-H`. The PLAN line matches that gap. No re-scope.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; when the grep returns more than a handful of notes (~3 is a fair line), prefer handing the reading to a **probe**; log relevant findings in Discovery Notes before re-interpreting the task; an absent or empty `archive/<area>/` is a prompt to re-check `<area>` against the README table before logging "no prior tasknotes" — a derived-and-wrong folder is indistinguishable from a genuinely empty one

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit (omit only on a task with no file deliverable)

**Discovery Notes:**

README archive table has no `BI-` row. Prose says project-specific prefixes use their own subfolder. `archive/bi/` holds the BI cohort, so `<area>` is `bi`. No `BI-058.1` note and no `## 🌳 Fan-out`, so no `blocked-by` / `parallel-safe-with`.

`package.json`: `dev` is `next dev --turbopack -p 3003`; `start` is `next start` (no port, no host). `playwright.config.ts` webServer is `npx next dev --turbopack -p ${E2E_PORT}` on 3009; `baseURL` is `http://localhost:${E2E_PORT}`. `just dev` shells out to `npm run dev`, so the script flag covers it. Next 16.3 `--help` documents `-H, --hostname` with default `0.0.0.0` for both `dev` and `start`.

Archive: BI-001 set the unbound `next dev --turbopack -p 3003` script. Later e2e notes (BI-053–BI-057) record the webServer dying when another Next dev already holds the lock — unchanged by this bind; port split (3003 vs 3009) stays. BI-045 assumed local-only `next dev -p 3003`.

Documented invocations that would go stale: `CLAUDE.md` Dev port line and `docs/GROK-AGENT.md` (`next dev -p 3003`). Threat-model prose stays BI-058.3.

No clarifications needed (--fast). Assumptions: `-H` is the flag the PLAN names; `start` keeps its default port 3000; `baseURL` stays `localhost` (resolves to the loopback listener); live `lsof` proof is for `npm run dev` on 3003 only (`next start` needs a production build, which this task does not produce); no new unit test — the bind is a process flag verified by grep plus `lsof`.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

Next's own `-H` / `--hostname` is the bind. `just dev` already calls `npm run dev`, so no justfile change. No refactor. No new test: the behavior is a process listen address, checked with `lsof`. `start` gets `-H` and keeps the default port 3000. `baseURL` stays `http://localhost:3009`.

Live check used port 3013 because PID 36385 already listened on `*:3003`. That process was not killed. Next printed `Local: http://127.0.0.1:3013` and `lsof` showed `TCP 127.0.0.1:3013 (LISTEN)`. The child was stopped; 3013 was free afterward.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] **External review** — a context that did not write the diff graded it against `## ✅ Acceptance`, and every finding is recorded below with its disposition (**blocker** → back to Phase 2; **note** → fixed or filed). `N/A` with a one-line reason when the diff is too small to grade

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

- `node -e` package.json scripts include `-H 127.0.0.1` → exit 0 (`dev` = `next dev --turbopack -H 127.0.0.1 -p 3003`, `start` = `next start -H 127.0.0.1`)
- `node -e` playwright.config.ts webServer has `-H 127.0.0.1` and `baseURL` is still `http://localhost:${E2E_PORT}` → exit 0
- `npx next dev --turbopack -H 127.0.0.1 -p 3013` then `lsof -nP -iTCP:3013 -sTCP:LISTEN` → `TCP 127.0.0.1:3013 (LISTEN)`, then that process stopped
- `lsof -nP -iTCP:3003 -sTCP:LISTEN` → `TCP *:3003 (LISTEN)` PID 36385 (pre-existing; not this run)
- `just test` → exit 0 (611 tests)
- `just lint` → exit 0
- `just typecheck` → exit 0
- Structural quality: flag additions only; no new public surface, no dead code. Doc lines that quoted the old command were updated.
- External review: N/A — four one-line edits, too small to grade.
- Frontend visual confirmation: N/A — no rendered surface.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed (standalone → top of `## Completed`; epic child → kept nested beneath its active parent — see SPEC/plan-filing.md §"`## Completed` archive convention" if unclear), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, the `touches:` scope reconciliation (`git diff --name-only` vs declared; name undeclared paths), and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

- [x] **Learnings** — did this task teach something the always-loaded layer (AGENTS.md / README §AI-referenced docs) should carry? `N/A` or the line

**Final Summary:**

`dev`, `start`, and the Playwright webServer now pass `-H 127.0.0.1`. `baseURL` is still localhost. A throwaway server on 3013 listened on 127.0.0.1; the pre-existing `*:3003` process (PID 36385) was left alone, so that port stays on all interfaces until `just dev` is restarted. No follow-up filed. unattended-candidates: none

Doc-drift: `README.md` no change. `AGENTS.md` no change. `CLAUDE.md` Dev port line updated to include `-H 127.0.0.1`. `.flowtron/PLAN.md` this row flipped to stub, kept nested under BI-EPIC-058. `VISION.md` no change. `docs/ADOPT.md` no change. `docs/WORKFLOW.md` no change. `docs/REVIEW-LOOP.md` no change. `docs/GROK-AGENT.md` the `next dev -p 3003` mention now includes `-H 127.0.0.1`.

Learnings: N/A.

**Archived:** 2026-09-24
