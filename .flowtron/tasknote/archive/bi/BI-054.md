---
title: gallery-rail
status: completed
tags: []
created: 2026-09-21
due:
related-tasks: [BI-EPIC-053, BI-053.5, BI-008, TEST-002.5]
touches:
  - components/GalleryPanel.tsx
  - components/GalleryPanel.test.tsx
  - e2e/review-keyboard.spec.ts
---

# BI-054 | gallery-rail

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-053]] [[BI-053.5]] [[BI-008]] [[TEST-002.5]]

## 🎯 Goal

Collapse the Gallery right-rail when empty so Folder/JSON/Sheet sit with approved images, not as a standing empty pane during review.

## ✅ Acceptance

- [x] Empty `approved` renders no gallery rail (no aside, no placeholder copy, no Folder/JSON/Sheet) — `npx vitest run components/GalleryPanel.test.tsx`
- [x] Populated `approved` still shows the rail, count badge, Folder/JSON/Sheet, and per-item rows — `npx vitest run components/GalleryPanel.test.tsx`
- [x] After Approve in the review e2e, the gallery export group is present (empty-placeholder pin retargeted) — `npx playwright test e2e/review-keyboard.spec.ts` (webServer blocked by Next lockfile on :3003; covered by live :3003 pass)
- [x] Typecheck and lint clean on the touched files — `npx tsc --noEmit` · `npm run lint`
- [x] Empty review is two-column (sidebar + review); approving an image opens the right rail with Folder/JSON/Sheet — `👁️` (operator: new project has no right rail; Fakeneuron with 10 approved still shows Gallery)

## 🧩 Subtasks

- [x] `GalleryPanel` returns `null` when `approved.length === 0`; drop the empty-state placeholder branch
- [x] Rewrite `GalleryPanel.test.tsx` empty-state cases to assert absence (no `Gallery` heading, no placeholder, no export group)
- [x] Retarget `e2e/review-keyboard.spec.ts` post-Approve pin from the placeholder copy to the Folder button
- [x] Typecheck, lint, targeted vitest; visual confirm on the live app

## 🔗 Related

- [[BI-EPIC-053]] — parent epic that filed this as a standalone Medium follow-up
- [[BI-053.5]] — sidebar-restructure; operator accepted the left-rail regroup and filed this from visual confirm
- [[BI-008]] — original gallery panel; assumed always-visible empty state
- [[TEST-002.5]] — authored the empty-placeholder test this task rewrites

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** The empty `w-64` rail is still a standing third column during review; Folder/JSON/Sheet already hide when empty, so collapsing the pane is the remaining gap filed at [[BI-053.5]] visual confirm.

- [x] Read relevant source files — `components/GalleryPanel.tsx`, `components/GalleryPanel.test.tsx`, `components/Workspace.tsx` (unconditional mount), `e2e/review-keyboard.spec.ts` (placeholder pin), README / WORKFLOW gallery copy

- [x] **Best Practices Review** — `GalleryPanel` already owns the empty-vs-populated branch (export group gated on `approved.length > 0`). Returning `null` when empty extends that gate to the whole rail; `Workspace` stays a pass-through. No new collapse state, no hook change, no user toggle. TEST-002.5's placeholder-copy criterion is superseded by this product change. Deferred: a user-collapsible rail while populated.

- [x] **Archive skim** — `archive/bi/` is the README table folder for `BI-*`. Hits: [[BI-008]] (always-visible empty pane by design; this task reverses that assumption); [[BI-021.2]] / [[BI-021.4]] (Folder/JSON/Sheet already gated on `approved.length > 0`); [[TEST-002.5]] (pins the placeholder copy); [[BI-053.5]] (filed this from visual confirm); [[BI-027]] / [[BI-029.2]] (lightbox + byte-resolve, out of scope). No ⚠️ pointers.

- [x] **Drift check** — PLAN line still matches HEAD: `GalleryPanel` is `aside.w-64.shrink-0`, always mounted from `Workspace.tsx:214`, empty copy at `GalleryPanel.tsx:101`. SPEC: no contract. Docs describe gallery as collecting approved images; none claim a standing empty pane.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed. Assumptions: (1) auto-hide when `approved.length === 0`, auto-show when any approved image exists — no user toggle, no collapsed stub, no animation; (2) empty-state placeholder copy is removed; (3) collapse lives in `GalleryPanel` (`return null`) so `Workspace.tsx` does not change; (4) docs already treat the gallery as an approved-images surface — no rewrite.

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit (omit only on a task with no file deliverable)

**Discovery Notes:**

The standing empty pane is `GalleryPanel`'s `aside` (`w-64 shrink-0`). Export buttons and the count badge already hide when empty (BI-008 / BI-021.2 / BI-021.4); the remaining cost is 256px of review width plus the "Approved images appear here." placeholder. `Workspace` always mounts the panel; `approvedImages` is a live derivation. e2e `review-keyboard.spec.ts:43` asserts the placeholder is gone after Approve — that pin becomes a tautology once the copy is deleted, so retarget it to the Folder button. Workspace tests stub `approvedImages: []` and never query the gallery.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

Pattern: `GalleryPanel` already gated Folder/JSON/Sheet and the count badge on `approved.length > 0`. Returning `null` when empty extends that gate to the whole `aside`; `Workspace` stays a pass-through. Nested empty-branch conditionals in the header/body dropped because they are unreachable after the early return.

No refactor beyond that. No collapse state, no hook change, no animation.

Tests: empty-state describe retitled BI-054 and asserts the rail is absent; export-group and item-rendering cases unchanged. e2e post-Approve pin retargeted from the deleted placeholder to the Folder button.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

```
npx vitest run components/GalleryPanel.test.tsx  → 0  (6 passed)
npx vitest run components/Workspace.test.tsx     → 0  (12 passed)
npx tsc --noEmit                                 → 0
npx eslint components/GalleryPanel.tsx components/GalleryPanel.test.tsx e2e/review-keyboard.spec.ts  → 0
npx playwright test e2e/review-keyboard.spec.ts  → 1
    Error: Process from config.webServer was not able to start.
    Another next dev server is already running. (:3003 PID 36385)
```

Playwright webServer blocked by the live `:3003` Next lockfile (same as BI-053.3/.4/.5). Live `:3003` pass covers the e2e pin: empty workspace has no Gallery/Folder aside; Approve opens a 256px rail with Gallery count, Folder/Sheet/JSON, and the task row; clearing Approve removes the rail. Mobile 390×844 empty: sidebar only.

Operator 👁️: new project has no right rail; Fakeneuron.com with 10 approved still shows Gallery + Folder/Sheet/JSON.

No avoidable duplication, dead code, extra public surface, or stale code-facing docs. Placeholder copy is gone on purpose.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed (standalone → top of `## Completed`; epic child → kept nested beneath its active parent — see SPEC/plan-filing.md §"`## Completed` archive convention" if unclear), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, the `touches:` scope reconciliation (`git diff --name-only` vs declared; name undeclared paths), and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Empty Gallery right-rail no longer occupies review width. `GalleryPanel` returns `null` when `approved.length === 0`; Folder/JSON/Sheet and the count badge still appear once anything is approved.

**Doc-drift:** README.md, AGENTS.md, CLAUDE.md, VISION.md, docs/ADOPT.md, docs/WORKFLOW.md, docs/REVIEW-LOOP.md, docs/GROK-AGENT.md — no change (gallery already described as an approved-images surface). PLAN.md — this stub.

**Changed:** `components/GalleryPanel.tsx` (early `return null`; empty placeholder removed) · `components/GalleryPanel.test.tsx` (empty-state asserts absence) · `e2e/review-keyboard.spec.ts` (post-Approve pin retargeted to Folder).

**Verification:** vitest GalleryPanel 6/6 and Workspace 12/12; `tsc --noEmit` 0; eslint on touched files 0; Playwright e2e blocked by :3003 lockfile, covered live; operator confirmed empty project has no rail and populated Fakeneuron still shows Gallery.

**Refactors:** none. Deferred: user-collapsible rail while populated; iterate-save acknowledgment filed as [[BI-057]].

**`touches:` reconciliation:** declared `GalleryPanel.tsx`, `GalleryPanel.test.tsx`, `e2e/review-keyboard.spec.ts`. Closure also writes PLAN.md + this archive path. No undeclared source paths.

**Maintainability:** empty-vs-populated lives in one early return instead of a standing empty `aside` plus nested header/body gates.

**Archived:** 2026-09-23
