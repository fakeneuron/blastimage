---
title: imagegen-byte-resolution audit
status: in-progress
tags: []
created: 2026-08-08
due:
related-tasks: [BI-EPIC-029, BI-029.2, BI-029.3, BI-029.4]
---

# BI-029.N | imagegen-byte-resolution audit

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-029]]

## 🎯 Goal

Verify the completed `BI-EPIC-029` (`imagegen-byte-resolution`) cohort sits coherently in the codebase: cumulative doc-drift sweep across `.flowtron/tasknote/README.md` §"AI-referenced docs", naming/style consistency across the cohort's deliverables, and follow-up filings for any miss.

## ✅ Acceptance

- [ ] **Doc-drift sweep (fixed line, per SPEC/epic.md §"Audit acceptance — fixed doc-drift line")** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the specific update. Always present; surfaces cumulative slice-local staleness that per-task Phase 4 closures can miss.
- [ ] Cohort coherence inventory: each implementation child's deliverables read against the others (naming consistency, style parity, no contradictory cross-refs)
- [ ] No regressions surfaced in earlier-shipped cohort children's surfaces
- [ ] Audit findings recorded in Implementation Notes; misses cited as candidates for `/ft-file-followup <NEW-ID>` filing (filed AFTER audit closure to preserve `/ft-file-followup`'s filing-discipline gate)
- [ ] Single `feat: BI-029.N — audit BI-EPIC-029` (or `chore: ...` if no code edits land) commit lands
- [ ] PLAN.md line for `BI-029.N` flipped to stub form `Completed YYYY-MM-DD.`
- [ ] Tasknote moved to `.flowtron/tasknote/archive/bi/BI-029.N.md`
- [ ] Parent-flip prompt surfaced after audit closure (skill Step 8) — user confirms or declines flipping `BI-EPIC-029` to `Completed` and moving the cohort to `## Completed`

## 🧩 Subtasks

- [ ] Inventory cohort children's archived tasknotes — read each implementation child's Final Summary + Implementation Notes; capture deliverables in Discovery Notes
- [ ] Walk `.flowtron/tasknote/README.md` §"AI-referenced docs" entries — fixed doc-drift sweep
- [ ] Cohort coherence pass — naming consistency, style parity, no contradictory cross-refs across the cohort's deliverables
- [ ] Surface audit findings in Implementation Notes; cite each miss as a `/ft-file-followup <NEW-ID>` candidate
- [ ] Phase 4: flip `BI-029.N` PLAN line to stub form + archive tasknote
- [ ] Parent-flip: skill Step 8 prompts user; on confirm, atomic flip parent line + move cohort to `## Completed`

## 🔗 Related

- [[BI-EPIC-029]] — parent epic (imagegen-byte-resolution)
- [[BI-029.2]] — resolve-image-blob-seam: introduced `resolveImageBlob` as the sole URL→bytes path
- [[BI-029.3]] — export-imagegen-regression: end-to-end `imagegen:` coverage on the three export entry points
- [[BI-029.4]] — blob-cache-bounds: capped + invalidated `ImagegenContext`'s display-blob cache

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** User explicitly invoked `/ft-close-epic BI-029.N`. All three implementation children (`BI-029.2`, `.3`, `.4`) are closed in PLAN.md with no open siblings — the cohort is ready for audit.

- [x] Read relevant source files

  - `lib/imageBlob.ts` (BI-029.2) — `resolveImageBlob(url, root)`, the sole URL→bytes path. `grep -rn "fetch(" lib components app` (non-test) still returns exactly one hit, at `lib/imageBlob.ts:37` — the single-fetch-path invariant holds at audit time.
  - `lib/storage.test.ts` (BI-029.3) — `describe('exports with imagegen: URLs')` block with 6 tests still present, exercising the real resolver against a fake root.
  - `lib/ImagegenContext.tsx` / `lib/ImagegenContext.test.tsx` (BI-029.4) — `BLOB_CACHE_MAX_ENTRIES` bound + `invalidateRoundBlobs` on `readRound`, with 3 dedicated tests.
  - Archived tasknotes for all three children (`.flowtron/tasknote/archive/bi/BI-029.{2,3,4}.md`) — read in full for Discovery/Implementation/Final Summary content.

- [x] **Best Practices Review** — N/A. Audit subtask; no new code surface. Cohort coherence assessed instead (see Discovery Notes).

- [x] **Archive skim** — all three cohort children's archived tasknotes read in full (see above). Each explicitly cross-references the others ([[BI-029.2]] deferred `.3` and `.4`'s scope forward; `.3` and `.4` both cite `.2` as predecessor) — the cohort was planned and executed as a coherent whole, not three independent tasks.

- [x] **Drift check** — re-verified against current code, not just archived notes:
  - `resolveImageBlob` (`lib/imageBlob.ts:29`) unchanged from `.2`'s description; still the sole `fetch()` call site on an image URL (confirmed by grep above).
  - `lib/storage.test.ts`'s `imagegen:` regression block (`.3`'s deliverable) still present and passing.
  - `ImagegenContext.tsx`'s cache bound/invalidation (`.4`'s deliverable) still present and passing.
  - No drift from any archived tasknote's claims.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  **No clarifications needed.** No open siblings, no ambiguity in cohort scope — audit covers exactly `BI-029.2`, `.3`, `.4` as filed.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

Cohort deliverables, one bullet each:

- **BI-029.2** (resolve-image-blob-seam) — new `lib/imageBlob.ts` (`resolveImageBlob`/`ImageBlobResolver`), threaded through `lib/storage.ts`'s `gatherExportFiles`/`gatherReviewImages`, `lib/ImagegenContext.tsx`'s new `resolveBlob` member, `lib/useWorkspace.ts`'s three export call sites + `NOOP_IMAGEGEN`, and `components/GalleryPanel.tsx`'s `downloadImage`. Root fix: gave the byte-consuming paths the same resolver the render path already had.
- **BI-029.3** (export-imagegen-regression) — test-only, `lib/storage.test.ts` +152/−1: six new tests driving the real `resolveImageBlob` against a fake linked root through all three export entry points, plus a counter-case pinning the pre-seam failure behaviour. Mutation-checked (reverted `.2`'s fix temporarily, confirmed the new tests catch the regression).
- **BI-029.4** (blob-cache-bounds) — `lib/ImagegenContext.tsx` +34/−4: `BLOB_CACHE_MAX_ENTRIES` (200) with LRU eviction, `invalidateRoundBlobs` called from `readRound`. New `lib/ImagegenContext.test.tsx` (the repo's first `lib/*.test.tsx`), 3 tests.

✅ Phase 1 Discovery complete; entering Phase 2 Execution.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — N/A. Audit subtask; no new code surface authored under this task.

- [x] **Minimal refactor gate** — N/A. No fixes surfaced that needed an inline correction.

- [x] Implemented the minimal solution — N/A (verification-only audit; no code changes required)

- [x] Updated/added tests for non-trivial behavior — N/A

**Implementation Notes:**

**Cohort coherence inventory:**

- **Naming consistency** — `resolveImageBlob`/`ImageBlobResolver` (`.2`), `resolveBlob` (the `ImagegenApi` binding), `BLOB_CACHE_MAX_ENTRIES`/`evictOldestBlob`/`invalidateRoundBlobs` (`.4`) all follow the codebase's existing verb-noun / SCREAMING_SNAKE_CASE-constant conventions and sit alongside `resolveDisplayUrl` without name collision or overlap in responsibility.
- **Style parity** — all three children followed the repo's local-fixture test idiom (`makeFakeImagegenRoot`, `makeFakeRoot`, explicit vitest imports, hand-wired `afterEach(cleanup)`) per `TEST-001.N`'s recorded convention; `.4`'s `lib/ImagegenContext.test.tsx` is a new file but matches the established shape.
- **No contradictory cross-refs** — `.3`'s Discovery explicitly acknowledges `.2`'s deferral of end-to-end coverage; `.4`'s Discovery explicitly acknowledges `.2`'s deferral of cache bounds. Both consumed what `.2` deferred to them; no child re-litigates a decision another child made.
- **Dependency direction preserved** — verified live: `resolveImageBlob` (`lib/imageBlob.ts:29`) remains the only `fetch()` call on an image URL in `lib/components/app` (grep confirms one hit, at the resolver itself); `lib/storage.ts` still takes the resolver by parameter, no import of `imagegenFs` or React context.
- **No regressions** — `npm test` 14 files / 154 passed (matches `.4`'s Final Summary exactly, confirming no drift since); `npx tsc --noEmit` clean; `npm run lint` clean.
- **`GalleryPanel.downloadImage`** — confirmed still wired to `resolveBlob` from `useImagegen()` (`components/GalleryPanel.tsx:37,55,133`); the one byte site `.2` fixed but `.3` explicitly left uncovered by a dedicated component test (recorded assumption, not a miss — no component test file exists for `GalleryPanel` in this epic's scope).

**No misses found.** No `/ft-file-followup` candidates from this audit.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm test`: 14 files / 154 passed (no code changed by this audit; full suite run as the cohort-wide regression check)

- [x] Ran lint/type-check on changed code — `npx tsc --noEmit` clean; `npm run lint` clean

- [x] **Quality assertions** — N/A. No code changed under this audit; per-child quality assertions were already recorded in `.2`/`.3`/`.4`'s own Phase 3 sections and re-verified live above (single-fetch-path invariant, no dead code, public surface unchanged).

- [x] (frontend) Asked the user for visual confirmation — N/A. No UI change; audit is verification-only.

**Testing Notes:**

| Gate | Result |
|---|---|
| `npm test` | 14 files / 154 passed |
| `npx tsc --noEmit` | clean |
| `npm run lint` | clean |
| `grep -rn "fetch(" lib components app` (non-test) | 1 hit — `lib/imageBlob.ts:37` (the resolver itself) |

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

  - `README.md` — **no change.** §"In-app loop" step 6 already describes Folder export as writing every approved image plus a `manifest.json` provenance file — that description is now accurate for `imagegen:` sessions, and was already verified accurate at `.2`'s closure. Re-confirmed cumulatively across the full cohort: no `cache`/`blob`-related claims exist anywhere in the file that `.4` would contradict.
  - `AGENTS.md` — **no change.** Carries the flowtron paste-block only; no `imagegen`/`export`/`blob`/`cache` references to drift.
  - `CLAUDE.md` — **no change.** §Stack's `lib/persistence.ts` line is an orthogonal future-adapter seam, unaffected by this cohort's in-browser byte-resolution and cache-lifecycle work.
  - `.flowtron/PLAN.md` — **updated.** `BI-029.N` flipped to the stub form below, kept 2-space nested beneath the active `BI-EPIC-029` in `## High` (parent-flip decision handled separately, post-closure).

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-08-08.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] **Evidence-based recap** drafted — see Final Summary

**Final Summary:**

Audited the closed `BI-EPIC-029` (imagegen-byte-resolution) cohort — `BI-029.2` (the `resolveImageBlob` seam), `BI-029.3` (end-to-end `imagegen:` export/review regression coverage), and `BI-029.4` (blob-cache bounds + invalidation). All three sit coherently: consistent naming, consistent test conventions, no contradictory cross-refs, and each child's deliverable is exactly what the prior child deferred to it. The single-fetch-path invariant `.2` established still holds by construction (`grep -rn "fetch(" lib components app` returns one hit, the resolver itself), and the full test suite is green at the same 14 files / 154 tests `.4` left it at.

**Changed:** no code changed by this audit — verification-only. `.flowtron/tasknote/BI-029.N.md` (this tasknote, scaffolded and closed) is the only artifact.

**Verification:** `npm test` 14 files / 154 passed · `npx tsc --noEmit` clean · `npm run lint` clean · `grep -rn "fetch(" lib components app` (non-test) → 1 hit (`lib/imageBlob.ts:37`).

**Refactors:** none — audit found nothing requiring an inline fix.

**Documentation:** all four AI-referenced docs verified current; no drift accumulated across the cohort.

**Maintainability:** the epic closes with the invariant it set out to establish (`imagegen:` bytes have exactly one resolution path) verifiably intact, backed by executable regression coverage (`.3`) and bounded, invalidation-safe caching (`.4`) rather than by convention alone.

**No `/ft-file-followup` candidates** — no misses surfaced.

**Archived:** 2026-08-08
