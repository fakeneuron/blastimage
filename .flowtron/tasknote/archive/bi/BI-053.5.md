---
title: sidebar-restructure
status: completed
tags: []
created: 2026-09-20
due:
related-tasks: [BI-EPIC-053, BI-053.4, BI-053.1]
blocked-by:
  - BI-053.4
touches:
  - components/Sidebar.tsx
  - components/Sidebar.test.tsx
  - components/Workspace.tsx
  - components/Workspace.test.tsx
  - e2e/helpers.ts
  - e2e/imagegen-link-load.spec.ts
  - e2e/smoke.spec.ts
  - e2e/review-keyboard.spec.ts
  - e2e/iterate-feedback-modal.spec.ts
  - README.md
  - docs/WORKFLOW.md
  - docs/REVIEW-LOOP.md
  - docs/ADOPT.md
  - docs/GROK-AGENT.md
---

# BI-053.5 | sidebar-restructure

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-053]] [[BI-053.4]] [[BI-053.1]]

## 🎯 Goal

Regroup the sidebar so a project's identity, rounds, and tasks sit in the open, while the remaining project/workspace actions move into a ⋯ menu.

## ✅ Acceptance

- [x] Visible sidebar: project header (name, repo label, Viewer/In-app badge), rounds list + ↻, tasks + New task; Generate All only when `generationAvailable`, otherwise a viewer-mode note — `npx vitest run components/Sidebar.test.tsx`
- [x] ⋯ menu holds New/Switch project, Link imagegen, Export/Import backup, Build, Import tasks; those controls are not in the open rail — `npx vitest run components/Sidebar.test.tsx`
- [x] e2e link flow opens the ⋯ menu before Link imagegen; round chips stay visible without opening it — `npx playwright test e2e/imagegen-link-load.spec.ts` (webServer blocked by Next lockfile on :3003, same as .3/.4; covered by live :3003 pass)
- [x] README / WORKFLOW / REVIEW-LOOP / ADOPT name the ⋯ menu for the moved actions; GROK-AGENT no longer claims Generate All stays visible-but-disabled in viewer mode — `grep -n '⋯\\|Project menu\\|Generate All' README.md docs/WORKFLOW.md docs/REVIEW-LOOP.md docs/ADOPT.md docs/GROK-AGENT.md`
- [x] Typecheck and lint clean on the touched files — `npx tsc --noEmit` · `npm run lint`
- [x] Header / rounds / ⋯ / Generate-vs-note are visible in the running app — `👁️` (operator: left rail is fine; Gallery + Iterate filed as BI-054 / BI-055)

## 🧩 Subtasks

- [x] Regroup `Sidebar.tsx`: header (name click-to-rename, repo, mode badge, Use-repo offer) + ⋯ disclosure; rounds + ↻ visible when linked; tasks + New task; Generate All vs viewer note
- [x] ⋯ panel: project `<select>`, New project (opens existing inline draft), Link imagegen, Export/Import backup, Build, Import tasks; keep hidden file inputs mounted outside the panel
- [x] Update `Sidebar.test.tsx` (open-menu helper; Generate All hidden in viewer mode; accessible names still resolve through the menu)
- [x] Point e2e `helpers.ts` + `imagegen-link-load.spec.ts` at the ⋯ → Link imagegen path; assert repo label in the header after link
- [x] Sweep README / WORKFLOW / REVIEW-LOOP / ADOPT / GROK-AGENT sidebar references
- [x] Workspace tests: only if a query breaks (no combobox/Generate All queries today) — smoke/review-keyboard/iterate-feedback-modal landmarks updated instead

## 🔗 Related

- [[BI-EPIC-053]] — parent epic
- [[BI-053.4]] — blocked-by: round-view-filter lands first (header, rounds list, picker this child regroups)
- [[BI-053.1]] — Discovery triage of the sidebar action stack

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Last implementation child of BI-EPIC-053. `.2`–`.4` already landed the header, round summaries, and current-round picker this child regroups; the eight-action stack is still the clutter `.1` triaged.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; when the grep returns more than a handful of notes (~3 is a fair line), prefer handing the reading to a **probe**; log relevant findings in Discovery Notes before re-interpreting the task; an absent or empty `archive/<area>/` is a prompt to re-check `<area>` against the README table before logging "no prior tasknotes" — a derived-and-wrong folder is indistinguishable from a genuinely empty one

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit (omit only on a task with no file deliverable)

**Discovery Notes:**

**Read set:** `components/Sidebar.tsx` (header + eight-action stack + rounds + Generate All), `components/Sidebar.test.tsx` (smoke / BI-035.3 names / BI-053.2 identity / BI-053.4 chips), `components/Workspace.tsx` (prop wiring only — no query of the moved controls), `e2e/helpers.ts` + `e2e/imagegen-link-load.spec.ts` (visible `Link imagegen` + `combobox Project` as the ready landmark), `README.md` / `docs/WORKFLOW.md` / `docs/REVIEW-LOOP.md` / `docs/ADOPT.md` / `docs/GROK-AGENT.md`. No shared dropdown/popover in the tree.

**Best Practices:** Sidebar stays the presentational owner of DOM file-reads and local draft/menu state (BI-019 / BI-053.2). Workspace keeps the handlers. Do not extract a shared Menu primitive — this is the first disclosure, and a one-off toggle is the existing modal-local-state shape. Hidden file inputs must stay *mounted* outside the conditional panel so a closing menu cannot drop the file-picker's `<input>`. Accessible names from BI-035.3 are load-bearing for tests and e2e — keep the strings, change only where they live. No hook/API change.

**Archive skim (`archive/bi/` confirmed via README Area prefixes + existing folder):** load-bearing: **BI-053.1** triage table (visible: header · rounds · tasks · Generate All in-app only; ⋯ : New/Switch · Link · Export/Import · Build · Import tasks; ↻ = refresh; hide Generate All in adopter mode with a viewer-mode note). **BI-053.2** header + inline New/Rename (comment already says `.5` regroups this). **BI-053.3/4** ↻ + rN chips as view filter. **BI-035.3** names (round chips "Load round rN", distinct Import labels, Project `htmlFor`). **BI-031.2** disabled-with-reason — `.1` explicitly superseded the *visible-disabled* Generate All in adopter mode; TaskDetail Generate stays as-is. **BI-047** repo label in the header is what makes burying the Link button safe. **BI-019/021.3/022.7** Import/Build/backup stay in Sidebar.

**Drift check:** PLAN line matches HEAD: `generationAvailable` still gates Generate All's *reason*, not its presence — this child is what hides it. Identity header and round chips are already in the top block (`.2`/`.4`). No SPEC contract. e2e still clicks a visible `Link imagegen` and waits on `combobox Project` — both move into ⋯, so helpers/spec must follow. GROK-AGENT.md still says Generate All "render[s] disabled with the reason stated"; that sentence is the doc drift this child owns.

**No clarifications needed.** Assumptions:

1. ↻ stays visible beside the rounds list (`.1`: "↻ = refresh"); not in the ⋯ list.
2. Rename is click-the-name on the header (existing inline draft); PLAN did not put Rename in ⋯.
3. Use-{repo} offer stays in the header.
4. Switch stays the existing `<select>` inside the ⋯ panel.
5. New project in ⋯ closes the panel and opens the header inline draft.
6. Mode badge is `Viewer` when `!generationAvailable`, `In-app` otherwise.
7. Viewer-mode note replaces Generate All and reuses TaskDetail's BI-031.2 wording.
8. ⋯ is a local disclosure (button + panel), not `role="menu"` — the panel holds a combobox and file-trigger buttons.
9. Rounds block renders only when linked (Link lives in ⋯; a disabled ↻ on an unbound project is the clutter this child removes).
10. `Workspace.tsx` wiring unchanged; `Workspace.test.tsx` only if a query breaks.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

Pattern: local disclosure (button + panel + click-outside/Escape), not a new Menu primitive and not `role="menu"` (panel holds a combobox and file-trigger buttons). Hidden file inputs stay mounted outside the panel. Header rename is click-the-name (`aria-label="Rename {name}"` for WCAG 2.5.3). No `useWorkspace` change.

e2e landmarks that waited on the now-hidden Project combobox (smoke, focus-trap "not focused" asserts) retargeted to `Project menu`.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

```
npx vitest run components/Sidebar.test.tsx components/Workspace.test.tsx  → 0  (39 tests)
npx tsc --noEmit                                                          → 0
npm run lint                                                              → 0
npx playwright test e2e/smoke.spec.ts e2e/imagegen-link-load.spec.ts       → 1
    Another next dev server is already running (:3003 PID 36385); same lockfile block as .3/.4
```

Live :3003 (Playwright MCP): unbound shell shows name + Viewer + ⋯ + Tasks/New task + viewer note, no Link/combobox/Generate All on the rail. ⋯ opens Project select, New, Link, Export/Import, Build, Import tasks. Linking `test-fixtures/imagegen` auto-names the header, shows repo label + Rounds/↻/r1, keeps Generate All hidden. ⋯ → New project closes the panel and focuses the inline name field.

Quality: no new public surface (Sidebar props unchanged); no shared Menu extraction; file inputs stay mounted; BI-035.3 names preserved, only relocated.

Docs grep: README / WORKFLOW / REVIEW-LOOP / ADOPT name ⋯ for moved actions; GROK-AGENT records Generate All hidden in viewer mode.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed (standalone → top of `## Completed`; epic child → kept nested beneath its active parent — see SPEC/plan-filing.md §"`## Completed` archive convention" if unclear), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, the `touches:` scope reconciliation (`git diff --name-only` vs declared; name undeclared paths), and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Regrouped the left sidebar so identity, rounds, and tasks stay on the open rail, and the remaining project/workspace actions live in a ⋯ disclosure. Generate All is hidden in viewer mode (badge + note); TaskDetail Generate is unchanged.

13 source/doc files, ~+406/−257. Vitest 39/39, tsc 0, lint 0. Playwright webServer blocked by :3003 lockfile (same as .3/.4); live :3003 pass covered link → header/rounds and ⋯ contents. No refactor beyond the regroup. `Workspace.test.tsx` declared, unchanged. Undeclared: none.

Doc-drift: README / WORKFLOW / REVIEW-LOOP / ADOPT / GROK-AGENT updated (⋯ + viewer Generate All); AGENTS.md, CLAUDE.md, VISION.md no change; PLAN.md 053.5 stub + BI-054/BI-055 filed `3d23c0b`.

Visual confirm filed two out-of-scope gaps: empty Gallery rail (BI-054) and Iterate-after-Keep discoverability (BI-055).

**Archived:** 2026-09-20
