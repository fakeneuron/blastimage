---
title: hosted-webapp audit
status: in-progress
tags: []
created: 2026-06-16
due:
related-tasks: [BI-EPIC-022, BI-022.1, BI-022.2, BI-022.3, BI-022.4, BI-022.5, BI-022.6, BI-022.7]
---

# BI-022.8 | hosted-webapp audit

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-022]]

## 🎯 Goal

Verify the completed `BI-EPIC-022` (`hosted-webapp`) cohort sits coherently in the codebase: cumulative doc-drift sweep across `.flowtron/tasknote/README.md` §"AI-referenced docs", naming/style consistency across the cohort's deliverables, and follow-up filings for any miss.

## ✅ Acceptance

- [ ] **Doc-drift sweep (fixed line, per SPEC/epic.md §"Audit acceptance — fixed doc-drift line")** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the specific update. Always present; surfaces cumulative slice-local staleness that per-task Phase 4 closures can miss.
- [ ] Cohort coherence inventory: each implementation child's deliverables read against the others (naming consistency, style parity, no contradictory cross-refs)
- [ ] No regressions surfaced in earlier-shipped cohort children's surfaces
- [ ] Audit findings recorded in Implementation Notes; misses cited as candidates for `/ft-file-followup <NEW-ID>` filing (filed AFTER audit closure to preserve `/ft-file-followup`'s filing-discipline gate)
- [ ] Single `feat: BI-022.8 — audit BI-EPIC-022` (or `chore: ...` if no code edits land) commit lands
- [ ] PLAN.md line for `BI-022.8` flipped to stub form `Completed YYYY-MM-DD.`
- [ ] Tasknote moved to `.flowtron/tasknote/archive/bi/BI-022.8.md`
- [ ] Parent-flip prompt surfaced after audit closure (skill Step 8) — user confirms or declines flipping `BI-EPIC-022` to `Completed` and moving the cohort to `## Completed`

## 🧩 Subtasks

- [ ] Inventory cohort children's archived tasknotes — read each implementation child's Final Summary + Implementation Notes; capture deliverables in Discovery Notes
- [ ] Walk `.flowtron/tasknote/README.md` §"AI-referenced docs" entries — fixed doc-drift sweep
- [ ] Cohort coherence pass — naming consistency, style parity, no contradictory cross-refs across the cohort's deliverables
- [ ] Surface audit findings in Implementation Notes; cite each miss as a `/ft-file-followup <NEW-ID>` candidate
- [ ] Phase 4: flip `BI-022.8` PLAN line to stub form + archive tasknote
- [ ] Parent-flip: skill Step 8 prompts user; on confirm, atomic flip parent line + move cohort to `## Completed`

## 🔗 Related

- [[BI-EPIC-022]] — parent epic (hosted-webapp); this is its closing audit
- [[BI-023]] — grok-hosted-gen spike pulled out of the epic (was BI-022.9) so this audit can close the persistence+deploy cohort

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** User invoked `/ft-close-epic BI-022.8` and the pre-flight passed (after resolving a numbering conflict — see below). All seven implementation children (`.1`–`.7`) are closed and archived; the cohort is captured at audit time.

- [x] Read relevant source files

- [x] **Archive skim** — read all 7 cohort children's archived tasknotes (`archive/bi/BI-022.1`–`.7`); deliverables captured below

- [x] **Drift check** — every file path cited across the cohort's Final Summaries verified present at HEAD; no drift

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Numbering-conflict resolution (prerequisite to this audit).** At invocation `BI-022.8` was *not* the highest child — `BI-022.9` (grok-hosted-gen spike) sat above it. `.9` had been split out of `.5` *after* the `.8` audit was filed, and is an explicitly-deferred future spike (the epic vision itself defers hosted-mode generation to it). Per the user's decision (AskUserQuestion), `.9` was **pulled out of `BI-EPIC-022` into a standalone task `BI-023`** so the audit could close the persistence+deploy cohort cleanly. Renumber edits applied: `PLAN.md` (epic-vision wikilink `[[BI-022.9]]`→`[[BI-023]]`; `.9` child line removed; new top-level `BI-023` filed under Future Opportunities), `CLAUDE.md` (Stack §State ref), `docs/DEPLOY.md` (two refs). Archived `BI-022.5.md` references to `BI-022.9` left intact (immutable point-in-time record; benign historical drift).

**Cohort inventory (deliverables per child):**

- **`.1` discovery** — `--deep` pre-pass; reframed epic to personal-use cloud-persistent blastimage; filed the dependency-ordered children. No code.
- **`.2` storage-adapter-seam** — `lib/persistence.ts` (`PersistenceAdapter` 8-method seam + `localStorageAdapter` + resolved `persistence` const); rerouted `useWorkspace` 15 call-sites. Additive, local mode byte-identical.
- **`.3` supabase-auth-schema** — async-widened the seam; owner-scoped Postgres schema + `auth.uid()` RLS (`supabase/migrations/20260616000000_init_hosted_schema.sql`); `lib/{config,supabaseClient,supabaseAdapter}.ts`; trimmed `supabase/config.toml` (ports 54341–54344). Live-verified RLS round-trip. (OAuth split → `.6`, audit renumbered `.6`→`.7`.)
- **`.4` cloud-images-buckets** — private owner-scoped `images` storage bucket + `storage.objects` RLS (`20260616010000_image_storage_buckets.sql`); adapter uploads/dedupes/signs (8h URLs)/cleans-on-delete. Domain types + UI untouched. (Importer split → `.7`, audit renumbered `.7`→`.8`.)
- **`.5` cloudflare-deploy** — env-gated static export (`next.config.ts`, `BUILD_TARGET=cloudflare`); full `docs/DEPLOY.md` runbook. (Generation half split → `.9`, now `BI-023`.)
- **`.6` oauth-login** — `lib/auth.ts` (Google/GitHub `signInWithProvider`, `useAuthSession`); `components/{AuthGate,LoginScreen,AccountControl}.tsx`; `[auth.external.*]` in config.toml. All gated behind `isHostedMode()`.
- **`.7` session-importer** — `cloneSessionWithNewIds` (`lib/workspace.ts`); `exportSession`/`importSessionBackup` on `useWorkspace`; Sidebar `↓ Export`/`↑ Import`. Works in both modes via the seam, no mode branch.

**No clarifications needed** beyond the already-resolved `.9` conflict. Assumption: the audit closes the persistence+deploy cohort (`.1`–`.7`); hosted-mode *generation* is out of scope, now tracked by standalone `BI-023`.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — N/A for the audit verification itself; the renumber edits followed existing PLAN/doc conventions (stub-form task lines, `[[wikilink]]` cross-refs)

- [x] Implemented the minimal solution — verification pass over the cohort + the `.9`→`BI-023` renumber (the prerequisite that let the audit run)

- [x] Updated/added tests for non-trivial behavior — N/A (no behavior change; renumber is PLAN/doc bookkeeping)

**Implementation Notes:**

**Cohort coherence — no inconsistencies surfaced.**

- **Single mode gate.** `isHostedMode()` (`lib/config.ts`) is the one runtime gate, applied uniformly: `persistence.ts:103` (adapter resolution), `auth.ts` (doc-gated), `AuthGate.tsx:23`, `AccountControl.tsx:16`. The deploy-build gate `BUILD_TARGET=cloudflare` (`next.config.ts`) is cleanly separate. No competing or duplicated gate logic.
- **Single persistence seam.** `PersistenceAdapter` (`.2`) is the lone boundary; `.3` async-widened it, `.4` extended it for buckets, `.7`'s import/export ride it with no mode branch. Consistent seam discipline end-to-end.
- **Migrations** are timestamp-ordered and additive (`20260616000000_init_hosted_schema.sql`, `20260616010000_image_storage_buckets.sql`); naming consistent.
- **Cross-refs consistent** after the renumber: the only stale references were the `BI-022.9` mentions, all fixed (`CLAUDE.md`, `docs/DEPLOY.md` ×2, `PLAN.md` epic vision). Archived `.5` tasknote refs intentionally left as historical record.

**Inline fix applied:** the `.9`→`BI-023` renumber (see Discovery Notes) — `PLAN.md`, `CLAUDE.md`, `docs/DEPLOY.md`.

**Misses / follow-ups to file:** none. (One judgment-call observation, not a miss: `README.md` keeps a local-first framing and surfaces hosted mode only via its Docs-index link to `docs/DEPLOY.md` — an intentional additive-opt-in choice consistent across the `.5`/`.6`/`.7` closures. No change warranted.)

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — full suite green (no code behavior changed; ran it as the no-regression check)

- [x] Ran lint/type-check on changed code

- [ ] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

No-regression check across the cohort at HEAD: `npx tsc --noEmit` clean, `npm run lint` (eslint) clean, `npm test` (vitest) **101/101 passing across 6 files** — matches `.7`'s closure claim. No frontend change in this audit, so no visual confirmation needed.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs":
  - `README.md` — **no change** (hosted mode surfaced via the Docs-index link to `docs/DEPLOY.md`; local-first framing + `.7`'s Export/Import already documented; additive-opt-in by design)
  - `AGENTS.md` — **no change** (flowtron-only workflow guide; no feature surface)
  - `CLAUDE.md` — **updated** (Stack §State: deferred-generation ref `BI-022.9`→`BI-023`, via the renumber)
  - `.flowtron/PLAN.md` — **updated** (epic-vision wikilink `[[BI-022.9]]`→`[[BI-023]]`; `.9` pulled out to standalone `BI-023`; `.8` audit line flipped to stub at this closure)

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-06-16.` and tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate)

**Final Summary:**

Closed `BI-EPIC-022` (hosted-webapp) with its audit. **No inconsistencies surfaced** across the 7-child persistence+deploy cohort: a single `isHostedMode()` runtime gate applied uniformly, one `PersistenceAdapter` seam threaded from `.2`→`.7`, additive timestamp-ordered migrations, and (after this audit's renumber) consistent cross-refs. Verification at HEAD: tsc + eslint clean, 101/101 vitest green. **Prerequisite resolved:** `BI-022.9` (grok-hosted-gen spike) was a deferred future child sitting above the audit; per the user's decision it was pulled out into standalone task `BI-023` so the cohort could close cleanly — renumber applied across `PLAN.md`, `CLAUDE.md`, `docs/DEPLOY.md` (archived `.5` tasknote left as historical record). No audit follow-ups to file. Hosted-mode *generation* remains open as `BI-023`.

**Archived:** 2026-06-16
