---
title: round-summaries-auto-ingest
status: completed
tags: []
created: 2026-09-20
due:
related-tasks: [BI-EPIC-053, BI-053.2, BI-053.4, BI-043, BI-026]
blocked-by:
  - BI-053.2
touches:
  - lib/roundBatch.ts
  - lib/roundBatch.test.ts
  - lib/imagegenServerFs.ts
  - lib/imagegenServerFs.test.ts
  - lib/imagegenClient.ts
  - lib/imagegenClient.test.ts
  - lib/ImagegenContext.tsx
  - lib/ImagegenContext.test.tsx
  - lib/workspace.ts
  - lib/workspace.test.ts
  - lib/useWorkspace.ts
  - lib/useWorkspace.test.ts
  - components/Workspace.tsx
  - components/Workspace.test.tsx
  - components/Sidebar.tsx
  - components/Sidebar.test.tsx
  - app/api/imagegen/rounds/route.ts
  - e2e/imagegen-link-load.spec.ts
  - README.md
  - docs/REVIEW-LOOP.md
  - docs/WORKFLOW.md
  - docs/ADOPT.md
  - components/DeleteTaskModal.tsx
---

# BI-053.3 | round-summaries-auto-ingest

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-053]] [[BI-053.2]] [[BI-053.4]] [[BI-043]] [[BI-026]]

## 🎯 Goal

List every on-disk round with counts from `batch.json`, ingest them all on link/mount without wiping review decisions, and treat ↻ as a refresh that picks up new rounds.

## ✅ Acceptance

- [x] `GET /api/imagegen/rounds` / `ImagegenApi.listRounds` return `{round, generatedAt, taskCount, imageCount}` summaries parsed from each `batch.json` — `npx vitest run lib/imagegenServerFs.test.ts lib/imagegenClient.test.ts lib/roundBatch.test.ts lib/ImagegenContext.test.tsx lib/imagegenRouteGuard.test.ts`
- [x] Link/mount ingests every on-disk round the session does not already hold (replaces BI-026 latest-only autoload) — `npx vitest run lib/useWorkspace.test.ts components/Workspace.test.tsx`
- [x] Re-listing / ↻ skips already-ingested rounds, so Keep/Approve chips survive remount and refresh; explicit `loadRound(n)` still BI-043-replaces — `npx vitest run lib/useWorkspace.test.ts lib/workspace.test.ts`
- [x] Typecheck and lint clean on the touched files — `npx tsc --noEmit` · `npm run lint`

## 🧩 Subtasks

- [x] Add `RoundSummary` + `summarizeRoundBatch` in `lib/roundBatch.ts`; switch `listRounds` (server, client, `ImagegenApi`) to return summaries
- [x] Export `sessionRoundNumbers`; `loadRound()` with no arg re-lists and ingests missing rounds only; `loadRound(n)` stays replace
- [x] Store `roundSummaries` in `useWorkspace`; keep `availableRounds` as the derived number list for Sidebar
- [x] Workspace autoload calls no-arg `loadRound()` per session (not per mount); ↻ becomes Refresh; show rN chips whenever ≥1 round exists
- [x] Tests: summaries, skip-already-ingested (chips kept), ingest-new, autoload, Sidebar names, e2e Load-round chip

## 🔗 Related

- [[BI-EPIC-053]] — parent epic
- [[BI-053.2]] — blocked-by: project-identity (closed); this child is Sequential after it
- [[BI-053.4]] — follow-up: round-view-filter needs the summaries this child produces
- [[BI-043]] — depends-on: `upsertIterationForRound` is the idempotent ingest seam
- [[BI-026]] — related-decision: latest-only autoload this child replaces

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Next Sequential child of BI-EPIC-053; .2 is closed. `listRounds` still returns `number[]`, BI-026 still autoloads only the latest round, and remount still wipes chips via BI-043 replace. Scope matches the PLAN line.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; when the grep returns more than a handful of notes (~3 is a fair line), prefer handing the reading to a **probe**; log relevant findings in Discovery Notes before re-interpreting the task; an absent or empty `archive/<area>/` is a prompt to re-check `<area>` against the README table before logging "no prior tasknotes" — a derived-and-wrong folder is indistinguishable from a genuinely empty one

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit (omit only on a task with no file deliverable)

**Discovery Notes:**

**Read set:** `app/api/imagegen/rounds/route.ts`, `lib/imagegenServerFs.ts` `listRounds`, `lib/imagegenClient.ts`, `lib/ImagegenContext.tsx` (`listRounds: Promise<number[]>`), `lib/roundBatch.ts` (`RoundBatch.generatedAt`, `tasks[].images`), `lib/workspace.ts` `ingestRoundBatch` / private `upsertIterationForRound` / `newGeneratedImage` (always `decision: 'undecided'`), `lib/useWorkspace.ts` `loadRound` / `availableRounds` discover effect, `components/Workspace.tsx` BI-026 autoload, `components/Sidebar.tsx` ↻ + rN chips (chips only when `length > 1`).

**Keep-decisions (operator, 2026-09-20):** Skip already-ingested rounds. Auto-ingest and ↻ only pull round numbers the session does not already hold. Explicit `loadRound(n)` (rN chip) still BI-043-replaces and may wipe that round. Matches PLAN "↻ is refresh (re-list, ingest new)". Rejected merge-by-URL.

**Assumptions:**
- `loadRound()` no-arg = re-list + ingest missing (autoload + ↻). `loadRound(n)` unchanged.
- `availableRounds: number[]` stays for Sidebar; hook also exposes `roundSummaries` for .4.
- ↻ visible label becomes "Refresh rounds" (it no longer loads latest). Show rN chips whenever ≥1 round exists — otherwise a single-round folder has no explicit-reload path.
- Summaries use a loose `batch.json` parse (`summarizeRoundBatch`), not `parseRoundBatch` — listing must not fail on empty `tasks` (current `listRounds` only checks the file exists).
- Autoload one-shot keys off session id, not mount, so a project switch re-ingests that project's missing rounds.
- Sidebar regroup / counts-in-the-list / `currentRound` are .4/.5. Docs that still say "click ↻ Load round to ingest" are noted at Phase 4; .5 owns the sidebar copy sweep.

**Best Practices Review:** Extend existing seams — `listRounds` return type, `ingestRoundBatch` (unchanged), `useWorkspace` as the single orchestration layer (BI-049). `sessionRoundNumbers` is the session-level form of private `taskRoundNumbers`. No new hook. Deferred: merge-by-URL on explicit reload; toast when refresh ingests vs skips; `approved/` orphan cleanup (BI-043 deferred).

**Archive skim (load-bearing):** BI-043 — replace-on-reload, explicit assumption that decisions on the replaced round are discarded (disk bytes changed); this child does not change that path. BI-026 — latest-only one-shot autoload in `Workspace.tsx`. BI-053.2 — forwarded the remount wipe onto this PLAN line. BI-053.1 — round metadata source is the `rounds` route; ↻ = refresh; `.4` consumes summaries. BI-047 — `availableRounds` clears the instant the link drops (render-time). BI-049 — add orchestration in `useWorkspace`, do not split.

**Drift check:** PLAN line matches HEAD. `listRounds` still `number[]` at server/client/API. Autoload still `loadRound()` with no arg. `upsertIterationForRound` still private. `imagegenRouteGuard.test.ts` discovers routes from disk (guard only — value shape lives in server/client tests). No SPEC contract. Fan-out Sequential after closed `.2`.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

Pattern: `listRounds` stays the listing seam (return type widened to `RoundSummary[]`); `ingestRoundBatch` / BI-043 replace untouched; skip-already-ingested lives in `loadRound()` no-arg using new `sessionRoundNumbers`. `availableRounds` derived from stored `roundSummaries` so Sidebar chips stay `number[]` and `.4` can read counts. Autoload one-shot keys off session id so a project switch re-fires. ↻ label is "Refresh rounds"; rN chips render whenever ≥1 round exists (otherwise a single-round folder had no explicit reload).

Minimal: no merge-by-URL. Deferred: toast when refresh ingests vs skips; showing counts on chips (.4/.5); `approved/` orphan cleanup (BI-043).

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

```
npx vitest run lib/roundBatch.test.ts lib/imagegenServerFs.test.ts lib/imagegenClient.test.ts lib/ImagegenContext.test.tsx lib/imagegenRouteGuard.test.ts lib/workspace.test.ts lib/useWorkspace.test.ts components/Workspace.test.tsx components/Sidebar.test.tsx
  → 0  (233 passed)
npx tsc --noEmit  → 0
npx eslint <touched>  → 0
npm test  → 0  (592 passed)
npx playwright test e2e/imagegen-link-load.spec.ts  → 1
    Another next dev server is already running on :3003 (Next lockfile). Covered instead by live :3003 browser pass.
```

Live browser at http://localhost:3003 against `test-fixtures/imagegen`: link auto-ingested r1; ↻ is Refresh rounds; r1 chip present; Approve survived ↻ and a full reload. (Cleaned the fixture `approved/` + `selection.json` dirt that Approve wrote.)

No avoidable duplication; `summarizeRoundBatch` is the loose listing parse (strict `parseRoundBatch` stays for ingest). `roundSummaries` on the hook is the .4 handoff, not extra surface. DeleteTaskModal / rename-confirm copy updated so they still name the explicit rN path.



## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed (standalone → top of `## Completed`; epic child → kept nested beneath its active parent — see SPEC/plan-filing.md §"`## Completed` archive convention" if unclear), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, the `touches:` scope reconciliation (`git diff --name-only` vs declared; name undeclared paths), and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Round listings now carry `batch.json` counts, and link/mount/↻ ingest only rounds the session does not already hold, so Keep/Approve chips survive remount and refresh. Explicit rN still BI-043-replaces.

**Doc-drift:** `README.md`, `docs/WORKFLOW.md`, `docs/REVIEW-LOOP.md`, `docs/ADOPT.md` — ↻ Load round → auto-ingest + ↻ Refresh rounds / Load round rN. `AGENTS.md`, `CLAUDE.md`, `VISION.md`, `docs/GROK-AGENT.md` — no change. `.flowtron/PLAN.md` — `.3` stubbed.

**Scope:** Declared 21 paths, changed 23. All declared (docs + DeleteTaskModal added during Phase 4 after ↻ copy went stale). Plus tasknote + PLAN (excluded).

**Maintainability:** `.4` can read `roundSummaries` without re-fetching; BI-043 replace stays the rewrite path.

**Archived:** 2026-09-20
