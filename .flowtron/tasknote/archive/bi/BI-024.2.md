---
title: next-round-request
status: completed
tags: []
created: 2026-06-18
due:
related-tasks: [BI-EPIC-024, BI-024.1]
---

# BI-024.2 | next-round-request

[← PLAN.md](../../../PLAN.md) · ✅ Completed · 🔗 [[BI-EPIC-024]] · [[BI-024.1]]

## 🎯 Goal

Replace the iterate modal's in-browser `generateBatch` call with a write of `rounds/r<N>/selection.json` (keepers + `promptMode` + `nextPrompt`), and add approve→`imagegen/approved/` promote per `docs/REVIEW-LOOP.md` §3.

## ✅ Acceptance

- [x] Iterate modal writes `selection.json` instead of calling `generateBatch`
- [x] Approve decisions promote keeper images to `imagegen/approved/`
- [x] Schema matches `docs/REVIEW-LOOP.md` §3 (`schemaVersion`, `round`, `selectedAt`, per-task `decision`/`keeper`/`promptMode`/`nextPrompt`)
- [x] Tests cover selection write and approve promote paths

## 🧩 Subtasks

- [x] `lib/roundSelection.ts` — schema v1 types + `detectPromptMode` + parse/serialize/merge
- [x] `lib/imagegenUrl.ts` — `roundNumberFromImageUrl` + `roundImageFilenameFromUrl`
- [x] `lib/imagegenFs.ts` — readwrite link + `writeRoundSelection` + `promoteKeeperToApproved`
- [x] `lib/ImagegenContext.tsx` + `lib/useWorkspace.ts` — `requestNextRound`, approve promote hook, `loadedRound`
- [x] `components/IterateModal.tsx` + `Workspace.tsx` — wire submit to `requestNextRound`; update copy
- [x] Tests — `roundSelection.test.ts`, `imagegenUrl.test.ts`, `imagegenFs.test.ts` (selection writes); 122/122 green

## 🔗 Related

- [[BI-EPIC-024]] — parent epic (terminal-generate / frontend-review loop)
- [[BI-024.1]] — load-round-ingest (completed; folder seam + batch ingest)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Second child of BI-EPIC-024; completes the emit seam opposite BI-024.1's ingest. Iterate modal + approve paths are the only behavioural changes — `generate()` on TaskDetail stays for legacy local mode.

- [x] Read relevant source files

- [x] **Archive skim** — BI-024.1 established read seam (`imagegenFs`, `ImagegenContext`, `ingestRoundBatch`); BI-021.2 FSA write pattern in `storage.ts` (`exportManifestToFolder`). Iterate wired in `Workspace.tsx` via `ws.generate`.

- [x] **Drift check** — paths/functions cited in PLAN still match: `IterateModal.tsx`, `generateBatch` in `useWorkspace.generate`, `docs/REVIEW-LOOP.md` §3 schema. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

Assumptions: (1) imagegen link upgrades to `readwrite` so selection + approve writes work on the persisted handle; (2) round number inferred from keeper `imagegen:rounds/r<N>/…` URL with `loadedRound` fallback; (3) `promptMode` derived heuristically from base vs edited prompt per REVIEW-LOOP §6; (4) TaskDetail `generate()` unchanged — only iterate modal path switches.

Discovery surfaced no significant deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — mirrored BI-024.1 layer split: pure contract (`roundSelection.ts`), FSA writes (`imagegenFs.ts`), hook orchestration (`useWorkspace.ts`), presentational modal copy only.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

`requestNextRound` writes merged `selection.json`; `setImageDecision('approved')` + `submitFeedback(approve)` copy keeper to `approved/` and record approve entry. Link picker now `readwrite`.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm test` 122/122 green (+9 new tests)

- [x] Ran lint/type-check on changed code — `tsc --noEmit` clean; `eslint` clean

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

Operator confirmed via commit-go.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md` no change; `AGENTS.md` no change; `CLAUDE.md` no change (loop docs land in BI-024.4 audit); `.flowtron/PLAN.md` updated

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-06-18.` and tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Shipped the terminal-round **emit seam**: iterate modal now writes `rounds/r<N>/selection.json` (keeper + `promptMode` + `nextPrompt`) via `requestNextRound`; approve promotes keeper images to `imagegen/approved/` and records the decision. New module `roundSelection.ts`; extended `imagegenFs`/`ImagegenContext`/`useWorkspace`. 122 tests green.

**Archived:** 2026-06-18