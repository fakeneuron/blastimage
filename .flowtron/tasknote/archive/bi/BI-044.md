---
title: adopt-symlink-path
status: completed
tags: []
created: 2026-08-30
due:
related-tasks: []
touches:
  - docs/ADOPT.md
---

# BI-044 | adopt-symlink-path

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Fix the two dead `ln -sf ../blastimage/...` symlink targets in `docs/ADOPT.md` §5.1 to `../../blastimage/...` so the terminal-skill install instructions actually work.

## ✅ Acceptance

- [x] `docs/ADOPT.md` §5.1's two `ln -sf` commands target `../../blastimage/.grok/skills/blast-generate` and `../../blastimage/.grok/skills/blast-iterate`
- [x] Verified by symlink resolution math (or a scratch reproduction) that the corrected relative path resolves from `<host>/.grok/skills/` to `<host>/blastimage/.grok/skills/<name>`

## 🧩 Subtasks

- [x] Edit `docs/ADOPT.md` §5.1: fix both `ln -sf` lines from `../blastimage/...` to `../../blastimage/...`
- [x] Re-verify the corrected path resolves correctly
- [x] Doc-drift sweep + closure

## 🔗 Related

- Filed per cloutomaton CORE-021.N's operator call; re-confirmed at HEAD by CORE-035.3 (cross-project, no local wikilink)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Bug reproduced with a scratch symlink test; ADOPT.md §5.1 still carries the broken `../blastimage/...` targets at HEAD. Small, well-scoped doc fix — no re-scope needed.

- [x] Read relevant source files — read `docs/ADOPT.md` §5.1 (lines 69–92) in full; no broad search needed, no probe warranted.

- [x] **Best Practices Review** — N/A, prose-only doc fix (no code, no module boundaries touched).

- [x] **Archive skim** — `grep -l "ADOPT.md\|\.grok/skills"` across `archive/bi/*.md` surfaced BI-021.5 (unrelated §6/§7 reconciliation, not §5.1), and BI-024.3/BI-024.4. **BI-024.3** authored §5.1's symlink block originally. **BI-024.4** (the `BI-EPIC-024` audit, 2026-06-18) explicitly asserted "`docs/ADOPT.md` §5.1 correctly documents skill install and operating loop" — a factual claim this task falsifies. Per SPEC "Tasknote frontmatter" write-once carve-out (factual corrections), this closure will append a `> **⚠️ Superseded by [[BI-044]]**` pointer under BI-024.4's nav header, staged in this task's closure commit.

- [x] **Drift check** — Confirmed against current `docs/ADOPT.md` HEAD (read directly, not recalled): §5.1 lines 76–80 still read `mkdir -p .grok/skills` / `ln -sf ../blastimage/.grok/skills/blast-generate .grok/skills/blast-generate` / `ln -sf ../blastimage/.grok/skills/blast-iterate .grok/skills/blast-iterate`. Verified the failure mode with a scratch reproduction (`/private/tmp/.../scratchpad/adopt-symlink-test/`): a symlink at `host/.grok/skills/blast-generate` → `../blastimage/.grok/skills/blast-generate` resolves to `host/.grok/blastimage/.grok/skills/blast-generate` (one level short — dead). The corrected target `../../blastimage/.grok/skills/blast-generate` resolves to `host/blastimage/.grok/skills/blast-generate` (correct — matches the layout ADOPT.md §1 establishes, submodule at `<host>/blastimage/`). PLAN.md line's stated fix (`../../blastimage/...`) matches. No SPEC contract touched (docs-only, no workflow/contract change).

- [x] Asked clarifying questions OR logged "No clarifications needed" — No clarifications needed. Assumption: fix applies only to the two `ln -sf` lines in §5.1; the surrounding prose ("Copy instead of symlink if your tooling does not follow symlinks") stays accurate and untouched.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:** Root cause: relative symlink targets in §5.1 are resolved from the symlink's own containing directory (`.grok/skills/`), not from the repo root — the doc's `../blastimage/...` accounts for only one `../` when two are needed to climb from `.grok/skills/` back to the host project root. Confirmed by direct filesystem reproduction (see Drift check). No code changes; `docs/ADOPT.md` is the sole touched file.

Discovery surfaced no significant deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — N/A, single-line path fix in prose docs; no code pattern to survey.

- [x] **Minimal refactor gate** — N/A, no refactor; changed exactly the two broken `ln -sf` targets and nothing else.

- [x] Implemented the minimal solution — `docs/ADOPT.md` §5.1: both `ln -sf ../blastimage/...` lines changed to `ln -sf ../../blastimage/...`.

- [x] Updated/added tests for non-trivial behavior — N/A, doc-only change; no test surface.

**Implementation Notes:** 2-line diff in `docs/ADOPT.md` (lines 78–79). No other prose in §5.1 needed changes — the "Copy instead of symlink" fallback note stays accurate.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — N/A, no test surface (docs-only).

- [x] Ran lint/type-check on changed code — N/A, no code changed.

- [x] **Quality assertions** — N/A reasoning still recorded: no duplication, dead code, or complexity introduced; the two-line fix leaves everything else in §5.1 unchanged and no longer stale.

- [x] (frontend) Asked the user for visual confirmation — N/A, no UI surface; verified instead via filesystem reproduction (below).

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:** Reproduced the install steps in a scratch dir mirroring the doc's layout (`host/blastimage/.grok/skills/{blast-generate,blast-iterate}/` as the submodule, `host/.grok/skills/` as the install target) using the exact corrected `ln -sf` commands now in `docs/ADOPT.md`. Both symlinks resolved via `os.path.realpath` to their real, existing target directories — confirms the fix; scratch dir removed after verification.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md` no change; `AGENTS.md` no change; `CLAUDE.md` no change; `.flowtron/PLAN.md` updated (BI-044 flipped to stub, moved to `## Completed`). `docs/ADOPT.md` is outside the AI-referenced cold-start set but is the doc actually fixed by this task.

- [x] Closed — both Acceptance criteria ticked (above). YAML `status:` flipped to `completed`, PLAN.md line flipped to stub, tasknote moved to `.flowtron/tasknote/archive/bi/`. Falsified-claim pointer appended to BI-024.4 (see recap).

- [x] **Evidence-based recap** drafted — see Final Summary.

**Final Summary:** Fixed two dead `ln -sf` symlink targets in `docs/ADOPT.md` §5.1 (lines 78–79): `../blastimage/...` → `../../blastimage/...`. The old target resolved from the symlink's containing directory (`.grok/skills/`) one level short of the host project root, leaving `/blast-generate` and `/blast-iterate` uninstallable via the documented steps. Verified the fix with two scratch filesystem reproductions (pre- and post-fix) confirming symlink resolution. 2-line diff, docs-only, no tests applicable. Refactors: none needed. Documentation verdict: `docs/ADOPT.md` now correct; no AI-referenced cold-start doc required a change. Maintainability effect: removes a broken copy-pasteable command from the adopter onboarding path. Also appended `> **⚠️ Superseded by [[BI-044]]**` under BI-024.4's nav header (`.flowtron/tasknote/archive/bi/BI-024.4.md`), correcting its factual claim that ADOPT.md §5.1 "correctly documents skill install" — per SPEC's write-once factual-correction carve-out, staged in this same closure.

**Archived:** 2026-08-30
