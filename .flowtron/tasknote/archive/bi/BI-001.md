---
title: Core TypeScript data model
status: in-progress
tags: [types, data-model]
created: 2026-06-06
due:
related-tasks: [BI-002]
---

# BI-001 | Core TypeScript data model

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-002]]

## 🎯 Goal

Define the complete TypeScript data model for blastimage (Session, PromptTask, Iteration, GeneratedImage, RefImage, FeedbackState) in `lib/types.ts` so every downstream feature builds on one shared, documented shape.

## ✅ Acceptance

- [x] `lib/types.ts` defines all six core entities plus supporting types
- [x] Each type is documented with a brief doc comment describing its role and key fields
- [x] The model covers the full workflow: tasks → references → iterations → generated images → feedback → approval/export
- [x] Types compile cleanly under `tsc --noEmit`

## 🧩 Subtasks

- [ ] Bootstrap Next.js 15 app at repo root via `create-next-app` (TypeScript, App Router, Tailwind, ESLint, **no** `src/` dir, import alias `@/*`); set dev script to port 3003
- [ ] Verify tooling: `npx tsc --noEmit` and `npm run lint` run clean on the fresh scaffold
- [ ] Author `lib/types.ts` — full workflow data model (Session, PromptTask, RefImage, Iteration, GeneratedImage, FeedbackState + supporting types: ratings, generation status, approval/export manifest), each documented with a doc comment
- [ ] Typecheck the model with `npx tsc --noEmit`

## 🔗 Related

- [[BI-002]] — persistence layer serializes/deserializes this exact model to localStorage; export/import manifest derives from it

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Foundational first task; every downstream BI task consumes this shared model. Still the right work.

- [x] Read relevant source files — no existing code (`lib/` absent, no prior types). Read VISION.md, PLAN.md, project CLAUDE.md, and natabula `STACK-TENDENCIES.md` as the design sources.

- [x] **Archive skim** — `archive/bi/` absent → no prior tasknotes.

- [x] **Drift check** — PLAN cites `lib/types.ts`, but the Next.js project does not exist yet (no `package.json`/`tsconfig`/`app/`). Surfaced to operator; resolved → BI-001 bootstraps the declared stack as its enabling step.

- [x] Asked clarifying questions — Model depth: operator chose **full workflow model**. Scope/scaffold: operator redirected to natabula guidance → resolved via project CLAUDE.md (Next.js 15, port 3003) + natabula (Tailwind styling default, flat single-stack layout, defer to project stack).

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- **Stack (resolved):** Next.js 15, App Router, TypeScript, Tailwind, ESLint, repo-root layout (no `src/`), import alias `@/*`, dev port 3003. Project CLAUDE.md is authoritative on framework/port; natabula supplies Tailwind + flat-layout conventions.
- **Scope deviation:** the literal PLAN line is "define types in `lib/types.ts`," but a typecheck-verifiable types file requires the project to exist. BI-001 therefore also bootstraps the Next.js app. This is a significant deviation from the literal line → fire 🛠️ before execution.
- **Model depth:** full workflow model — model the whole loop now (the six named entities + supporting types for ratings, generation status, approval/export manifest) so later tasks extend rather than reshape.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — greenfield repo, no existing code to extend. The "pattern" is the declared stack convention: Next.js 15 App Router (project CLAUDE.md) + natabula Tailwind/flat-layout defaults. New shape justified by absence of any prior code.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — N/A: `lib/types.ts` is pure type declarations (sole runtime export is the `SCHEMA_VERSION` const); no behavior to test.

**Implementation Notes:**

- Bootstrapped Next.js 15.5.19 (App Router, TS, Tailwind v4, ESLint, flat layout, alias `@/*`) at repo root. `@latest` installed Next 16, but project CLAUDE.md pins "Next.js 15" → re-scaffolded with `create-next-app@15` to honor the declared contract.
- `package.json`: name → `blastimage`, dev script → `next dev --turbopack -p 3003` (port per CLAUDE.md).
- `.gitignore`: appended `.next/`, `out/`, `next-env.d.ts`.
- `eslint.config.mjs`: added `.flowtron/**` to ignores so lint doesn't reach into the read-only flowtron submodule's `viz/` source.
- `lib/types.ts`: full workflow model — `Session` (root persisted object) → `PromptTask` → `Iteration` → `GeneratedImage`; `RefImage` library; `FeedbackState`; supporting types `StarRating`, `BatchSize`, `GenerationStatus`, `ReviewDecision`; derived `ApprovedImage` + `ExportManifest`; `SCHEMA_VERSION` const anchoring persistence (BI-002). **Cost types deliberately deferred to BI-010** (separate Low task) to avoid speculative fields.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — N/A (no behavior; see Phase 2)

- [x] Ran lint/type-check on changed code — `npx tsc --noEmit` clean; `npm run lint` clean

- [x] (frontend) Visual confirmation — N/A: deliverable is a types file + scaffold, no rendered surface to confirm

**Testing Notes:** `tsc --noEmit` and `npm run lint` both pass on the scaffold + new model.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md`: no change (fill-in owned by BI-012). `AGENTS.md`: no change (workflow-only). `CLAUDE.md`: no change, now confirmed accurate (declares Next.js 15 + port 3003, matching the scaffold). `.flowtron/PLAN.md`: updated (this closure).

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-06-06.` and tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:** Bootstrapped the Next.js 15 app (App Router, TS, Tailwind v4, ESLint, flat layout, port 3003) and authored the complete blastimage data model in `lib/types.ts` — the six named entities (`Session`, `PromptTask`, `Iteration`, `GeneratedImage`, `RefImage`, `FeedbackState`) plus supporting types (ratings, generation status, review decision, batch size) and derived `ApprovedImage` / `ExportManifest`, all documented. Pinned Next to 15 (not the `@latest` 16) to honor the declared stack. Cost types deferred to BI-010. `tsc --noEmit` + lint clean.

**Archived:** 2026-06-06
