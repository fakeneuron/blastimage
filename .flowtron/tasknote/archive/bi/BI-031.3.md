---
title: readme-mode-labels
status: completed
tags: []
created: 2026-08-08
due:
related-tasks: [BI-EPIC-031]
---

# BI-031.3 | readme-mode-labels

[← PLAN.md](../PLAN.md) · ✅ Completed · 🔗 [[BI-EPIC-031]]

## 🎯 Goal

Label the README "In-app loop" section Grok-Build-only, and reconcile the mode framing with `docs/REVIEW-LOOP.md` §2 and CLAUDE.md.

## ✅ Acceptance

- [ ] README.md's "In-app loop" section states plainly that this mode only works inside a Grok Build session with the provider bridge wired — not in a plain adopter browser tab
- [ ] `docs/REVIEW-LOOP.md` §2's "The browser never generates" line is scoped to the adopter/terminal workflow this doc covers, so it no longer reads as contradicting README's in-app loop
- [ ] `CLAUDE.md`'s Project paragraph names both modes (adopter terminal-only vs. in-app Grok-Build-only) instead of describing only Adopter mode
- [ ] No prose is removed or restructured beyond the labeling/reconciliation — existing content, links, and voice preserved

## 🧩 Subtasks

- [ ] Edit `README.md` "In-app loop" section heading/intro to label it Grok-Build-only, mirroring the "Adopter loop" section's directness
- [ ] Edit `docs/REVIEW-LOOP.md` §2 to scope "The browser never generates" to the adopter workflow, cross-referencing README's in-app loop
- [ ] Edit `CLAUDE.md`'s Project paragraph to name the in-app mode alongside Adopter mode
- [ ] Grep for stale contradictory claims across the three docs; run `npm test` as a regression guard (doc-only change)

## 🔗 Related

- [[BI-EPIC-031]] — parent epic: generation-mode-clarity
- [[BI-031.2]] — predecessor: provider-absent-ux

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** All three docs are live at HEAD and still carry the gap. README's "In-app loop" heading already picked up a `(local Grok Build session)` qualifier back in BI-024.4 (2026-06-18) — well before the audit-repo finding (2026-08-06) that filed this epic — so the existing label was already judged insufficient: the section's opening sentence is a conditional ("When blastimage runs inside Grok Build…") rather than a plain statement that this is the *only* way it works, and nothing tells a reader that a plain browser tab can't use it. Meanwhile `docs/REVIEW-LOOP.md` §2 flatly states "**The browser never generates**" with no scope qualifier visible outside that doc's own opening paragraph, and `CLAUDE.md`'s Project line documents only Adopter mode, leaving the in-app mode undocumented there. [[BI-031.2]] (sibling, completed 2026-08-08) fixed the UI-side diagnostic; this task is the doc-side half the epic called for.

- [x] Read relevant source files — `README.md`, `docs/REVIEW-LOOP.md`, `docs/GROK-AGENT.md`, `CLAUDE.md`, `.flowtron/PLAN.md` (BI-EPIC-031 line)

- [x] **Best Practices Review** — doc-only change; `N/A` for code dependency direction. The three docs already have an established division: README is the top-level entry point (both loops, brief), `docs/REVIEW-LOOP.md` is the adopter-workflow SSOT (detailed contract), `docs/GROK-AGENT.md` is the in-app bridge SSOT, and `CLAUDE.md` is the assistant-facing project summary. [[BI-025]] set the precedent of cross-linking rather than duplicating detail across these docs — this task follows that pattern: strengthen labels and add scope qualifiers, don't restate each other's content.

- [x] **Archive skim** — grepped `archive/bi/` for the three files in scope. [[BI-031.2]]'s doc-drift sweep explicitly deferred this file: "the 'In-app loop' section already opens with 'When blastimage runs inside Grok Build with the provider bridge wired', so nothing it claims became false; strengthening that label is BI-031.3's deliverable and is deliberately not pre-empted here." [[BI-025]] rewrote `docs/WORKFLOW.md` steps 5-7 to cross-reference `docs/REVIEW-LOOP.md` for the adopter path — same reconciliation shape, different doc pair. No other archive hits touch these three files' mode-labeling content.

- [x] **Drift check** — every quote the task description cites still matches HEAD: README.md:29 heading is `### In-app loop (local Grok Build session)`; `docs/REVIEW-LOOP.md`:54 reads `**The browser never generates.**` inside §2 "Division of labour" (§ header at line 45); `CLAUDE.md`:5 Project paragraph names only `**Adopter mode** (BI-EPIC-024)`. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

No clarifications needed. Assumptions: (1) this is a labeling/reconciliation pass, not a rewrite — existing structure, links, and voice in all three docs stay intact per the task line's "Clarify, do not remove" epic framing; (2) `docs/GROK-AGENT.md` is out of scope — the task description names only README, REVIEW-LOOP.md §2, and CLAUDE.md, and GROK-AGENT.md already opens by stating it's the Grok Build integration guide (unambiguous by context); (3) the fix is additive — a short qualifier sentence/clause in each doc rather than restructuring headings, matching the [light]🔧 mechanical sizing on the PLAN.md line.

Discovery surfaced no significant deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — matched [[BI-025]]'s reconciliation shape: strengthen the label at the source doc, add a short cross-referencing scope qualifier at the doc that would otherwise contradict it, don't duplicate detail across docs.

- [x] **Minimal refactor gate** — no refactor; doc prose only. `N/A`.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — `N/A` (doc-only)

**Implementation Notes:**

Three files, additive prose only:

- `README.md` — "In-app loop" heading changed from `(local Grok Build session)` to `(Grok Build only)`; intro sentence rewritten to lead with the plain constraint (only works running inside Grok Build itself) before describing the in-browser behavior, and names that a plain/adopter browser tab has no bridge and stays disabled (cross-refs [[BI-031.2]]'s disabled-button UX).
- `docs/REVIEW-LOOP.md` §2 — added a parenthetical after "The browser never generates." scoping the claim to the adopter workflow this doc covers and cross-referencing README's in-app loop, so the sentence no longer reads as a blanket claim when encountered out of context.
- `CLAUDE.md` — Project paragraph rewritten to name both modes ("Two mutually exclusive generation modes") instead of only Adopter mode, with In-app mode pointing at `docs/GROK-AGENT.md`.

No structure, links, or unrelated content changed.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `N/A` (doc-only); full `npm test` run as regression guard: **185/185** green

- [x] Ran lint/type-check on changed code — `npx tsc --noEmit` exit 0; `npm run lint` clean (doc-only, but both confirm no incidental code touch)

- [x] **Quality assertions** — `N/A`, doc-only. No code changed. Grep confirms no remaining unscoped "browser never generates" claim and both loop headings now read consistently across README/REVIEW-LOOP.md/CLAUDE.md.

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask) — `N/A`, no UI changed

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** —
  - `README.md` — **updated.** This task's own deliverable: "In-app loop" heading + intro relabeled Grok-Build-only.
  - `AGENTS.md` — **no change.** Pure flowtron-workflow boilerplate; names no generation surface.
  - `CLAUDE.md` — **updated.** This task's own deliverable: Project paragraph now names both generation modes.
  - `.flowtron/PLAN.md` — **updated.** BI-031.3 flipped to stub form, kept nested under the active [[BI-EPIC-031]].
  - Off-list but touched: `docs/REVIEW-LOOP.md` §2 gains a scope qualifier on "The browser never generates," cross-referencing README's in-app loop.

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-08-08.` and kept nested beneath the active parent epic, then tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] **Evidence-based recap** drafted

**Final Summary:**

blastimage's generation-mode docs no longer read as contradicting each other. README's "In-app loop" section now leads with the plain constraint — it only works when blastimage itself runs inside a Grok Build session with the bridge wired, and a plain/adopter browser tab has no bridge and stays disabled (BI-031.2) — instead of a soft conditional. `docs/REVIEW-LOOP.md` §2's "The browser never generates" now carries a scope qualifier naming it as adopter-workflow-specific and cross-referencing the in-app mode, so it no longer reads as a blanket claim out of context. `CLAUDE.md`'s Project paragraph names both modes instead of only Adopter mode.

Three files touched, doc-prose only (~10 lines net): `README.md`, `docs/REVIEW-LOOP.md`, `CLAUDE.md`. No refactor — doc-only change, `N/A`. Verification: full `npm test` **185/185** green (regression guard, no code touched), `npx tsc --noEmit` exit 0, `npm run lint` clean. Documentation: this task *is* the documentation fix — the doc-drift sweep above covers the four AI-referenced docs plus the off-list `docs/REVIEW-LOOP.md` touch. Maintainability: a reader landing on any one of the three docs now gets an internally consistent answer to "which mode am I in," closing the doc-side half of [[BI-EPIC-031]] ([[BI-031.2]] closed the UI-side half).

**Archived:** 2026-08-08
