---
title: grok-handoff
status: in-progress
tags: []
created: 2026-06-06
due:
related-tasks: []
---

# BI-011 | grok-handoff

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Write the Grok Build agent instruction doc covering app context, the mock→real integration seam in `lib/generate.ts`, and the `generateBatch` interface contract the agent must preserve.

## ✅ Acceptance

- [ ] `docs/GROK-AGENT.md` exists with clear app context section
- [ ] Doc covers the mock→real seam in `lib/generate.ts` with the exact function signature and the swap point
- [ ] `generateBatch` interface contract is fully specified (inputs, outputs, error shape)
- [ ] Reference images adaptation path is explained (ID seeds → actual dataUrl bytes)
- [ ] A Grok Build agent can read this doc cold and implement the real integration without guessing

## 🧩 Subtasks

- [x] Read `lib/generate.ts` and callers to confirm interface
- [x] Create `docs/GROK-AGENT.md` with: app overview, auth context, mock seam, `generateBatch` contract, reference adaptation path
- [x] Type-check (`npx tsc --noEmit`) and lint (`npm run lint`) pass

## 🔗 Related

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `lib/generate.ts` exists exactly as described; BI-007 confirms it is the single swap seam. No drift.

- [x] Read relevant source files

- [x] **Archive skim** — BI-007 is the key prior tasknote: explicitly documented `lib/generate.ts` as "the BI-011 swap seam — it knows nothing about `GeneratedImage`/`Iteration`, returning plain `{ url, prompt }` candidates the hook assembles." No other archive tasknotes touch generate.ts.

- [x] **Drift check** — `lib/generate.ts` exists; `generateBatch` at line 77; `GenerationRequest` and `GeneratedCandidate` interfaces both exported. Only caller confirmed at `lib/useWorkspace.ts:223`. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- `generateBatch(req: GenerationRequest): Promise<GeneratedCandidate[]>` — sole exported function. `GenerationRequest = { prompt: string; batchSize: BatchSize; referenceSeeds?: string[] }`. `GeneratedCandidate = { url: string; prompt: string }`.
- Only caller: `lib/useWorkspace.ts:223` — passes `{ prompt, batchSize: 4, referenceSeeds: [primaryRefImageId?, ...task.activeRefImageIds] }` (IDs, not image bytes).
- `referenceSeeds` is mock-only opaque strings. Real API needs `RefImage.dataUrl` (base64). Doc must explain adaptation: change field + update caller to resolve IDs from `session.refLibrary`.
- Components render with `<img src={image.url}>` — accepts both remote URLs and base64 data URLs.
- No `docs/` directory exists yet; task creates it.
- No clarifications needed. Assumptions: doc at `docs/GROK-AGENT.md`; Grok Build environment provides Grok Imagine built-in; no API key management section needed.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — no prior doc pattern in this project; `docs/` directory did not exist. New shape justified: single focused agent-instruction markdown file, no template needed.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — documentation task; no tests applicable.

**Implementation Notes:**

Created `docs/GROK-AGENT.md` (1 new file, 128 lines). Covers: app context, data model table, auth/execution context, the `lib/generate.ts` seam, full interface contract with hard constraints, the single caller in `lib/useWorkspace.ts`, reference-image adaptation path (ID → `dataUrl` lookup), app guarantees, and a checklist for the Grok agent.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — documentation only; no tests to run.

- [x] Ran lint/type-check on changed code — `npx tsc --noEmit` clean; `npm run lint` clean.

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask) — not applicable; doc-only task.

**Testing Notes:**

`tsc --noEmit` and `eslint` both exit clean with no output.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md`: no change (BI-012 owns it) · `AGENTS.md`: no change · `CLAUDE.md`: no change · `.flowtron/PLAN.md`: updated by flip below.

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-06-06.` and tasknote moved to `.flowtron/tasknote/archive/bi/`.

- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:** Wrote `docs/GROK-AGENT.md` — the Grok Build agent instruction doc for wiring real Grok Imagine into blastimage. Covers app context, the `lib/generate.ts` single-seam swap, the full `generateBatch` / `GenerationRequest` / `GeneratedCandidate` interface contract with hard constraints, the reference-image adaptation path (mock ID seeds → actual `dataUrl` bytes from `session.refLibrary`), and a Grok agent checklist. `tsc --noEmit` + lint clean. 1 file created (`docs/GROK-AGENT.md`, ~128 lines); no code changed.

**Archived:** 2026-06-06
