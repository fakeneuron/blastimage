---
title: adopter-playbook
status: in-progress
tags: []
created: 2026-06-14
related-tasks: [BI-EPIC-021, BI-021.2, BI-021.3, BI-021.4, BI-020]
---

# BI-021.5 | adopter-playbook

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-021]]

## 🎯 Goal

Write `docs/WORKFLOW.md` — the directed adopter operating loop (stage → import → attach refs → generate → review → iterate → export → land → review) that ties together the BI-021.2/.3/.4 affordances into one end-to-end playbook, cross-linked from ADOPT.md and USAGE.md, and reconcile the now-stale ADOPT.md compose/export prose deferred here by BI-021.3.

## ✅ Acceptance

- [ ] `docs/WORKFLOW.md` exists and walks the full directed loop end-to-end in order, naming the in-app affordances at each stage (🛠 Build, ⇪ Import, Reference Library, Generate / ⚡ Generate All, keep/discard/approve + ratings/feedback, Iterate →, Folder/JSON/Sheet export, land into `imagegen/`, review.html consistency pass).
- [ ] WORKFLOW.md references (not duplicates) ADOPT §7 for the `imagegen/` layout and USAGE.md for prompt/reference craft — it owns the *sequence*, they own setup + craft.
- [ ] ADOPT.md §6/§7 reconciled: compose "by hand or with a small script" now points to 🛠 Build; the approved/ export prose reflects Folder (one-step `manifest.json` + images) and the new `review.html`, not just the per-image-rename flow.
- [ ] Cross-links added: README Docs list, ADOPT.md, and USAGE.md each point to WORKFLOW.md.
- [ ] Markdown is internally consistent (relative links resolve, headings/anchors correct); no contradictions with README's 6-step overview.

## 🧩 Subtasks

- [ ] Draft `docs/WORKFLOW.md`: intro (what this doc is vs README/ADOPT/USAGE) + the directed loop as ordered stages, each naming the affordance and linking to ADOPT/USAGE for depth; close with the land + house-style review pass (the spinalcord-friction motivation).
- [ ] Reconcile ADOPT.md §6 (compose prose → 🛠 Build) and §7 (`tasks.json` compose note + `approved/` export prose → Folder writes `manifest.json`+images directly; add `review.html` to the layout tree).
- [ ] Add cross-links to WORKFLOW.md from README.md Docs section, ADOPT.md, and USAGE.md.
- [ ] Mental markdown pass: link resolution, heading hierarchy, consistency with README's 6-step overview.

## 🔗 Related

- [[BI-EPIC-021]] — parent epic (adopter-workflow-ux); this is the docs-last child
- [[BI-021.2]] — export-folder-write: the Folder action the loop's export stage documents
- [[BI-021.3]] — import-builder: the 🛠 Build action the loop's stage step documents; deferred ADOPT reconciliation here
- [[BI-021.4]] — review-sheet: the Sheet/review.html the loop's house-style pass documents
- [[BI-020]] — adopter-repo-layout: ADOPT §7 `imagegen/` layout the loop references

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Fifth and final implementation child of `BI-EPIC-021`, filed docs-last precisely so it documents the shipped `.2`/`.3`/`.4` affordances. Directly addresses the spinalcord friction where the operating loop was implicit (a hand-rolled `imagegen/README.md`) and the provenance/export/review steps were improvised. The three features now exist; this writes the directed playbook that glues them together and reconciles the ADOPT staleness BI-021.3 explicitly deferred here.

- [x] Read relevant source files

  - `README.md:12–21` — the 6-step **Workflow** overview (already updated by `.2`/`.3`/`.4`: 🛠 Build + ⇪ Import in step 2, Folder/JSON/Sheet in step 6) + the **Docs** list (`:23–29`, no WORKFLOW.md entry yet).
  - `docs/ADOPT.md` — submodule setup mechanics (§1–5, §8) + §6 `tasks.json` contract + §7 `imagegen/` layout. **Stale prose:** §6 "emit … from your project's tooling" + §7 "Compose it from `prompts/` (by hand or with a small script)" predate 🛠 Build; §7 `approved/` block describes "Export JSON → rename to manifest.json" + "per-image ↓ downloads" (the pre-Folder, pre-Sheet flow). The `imagegen/` tree (`:101–111`) has no `review.html`.
  - `docs/USAGE.md` — prompt-writing + reference-image craft; the "craft layer" README's overview leaves implicit. No workflow-sequence content (correct division); no WORKFLOW.md link.
  - `components/Sidebar.tsx` — exact labels: `🛠 Build` (`:124`, `onOpenBuilder`), `⇪ Import` (`:131`), `⚡ Generate All` (`:155–166`).
  - `components/GalleryPanel.tsx` — export group labels **Folder · JSON · Sheet** (BI-021.2/.4), gated on approved images.

- [x] **Archive skim** — `grep -l` across `archive/bi/` for ADOPT/USAGE/README/WORKFLOW: BI-021.3 closure note explicitly defers ADOPT §6/§7 reconciliation **here** ("deeper ADOPT/WORKFLOW docs land in BI-021.5, per the epic's docs-last sequencing"). BI-021.2/.4 each updated README step 6 for their export action. BI-020 authored the `imagegen/` layout in ADOPT §7 (the canonical convention this loop references). BI-018 authored USAGE.md (craft division of labor). No prior WORKFLOW.md — net-new doc. Confirms division: README = overview, USAGE = craft, ADOPT = setup+layout, WORKFLOW = the directed loop.

- [x] **Drift check** — All cited paths/anchors verified at HEAD: README §Workflow + §Docs current, ADOPT §6/§7 present with the stale prose noted above, USAGE craft sections current, Sidebar/GalleryPanel labels match. No `docs/WORKFLOW.md` yet. The three affordances are shipped and named consistently in README. No drift beyond the already-known ADOPT staleness (in-scope to fix here).

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  **No clarifications needed** — the task line gives the loop outline (stage → import → attach refs → iterate → export → land → review) and the cross-link targets; BI-021.3 pre-assigned the ADOPT reconciliation here. Explicit assumptions asserted:
  1. **Division of labor (no duplication).** WORKFLOW.md owns the *directed sequence*; it **references** ADOPT §7 for the `imagegen/` layout and USAGE.md for prompt/reference craft rather than restating them. README's 6-step overview stays the quick scan; WORKFLOW is the narrative playbook.
  2. **ADOPT reconciliation kept tight.** Update the two stale spots (§6/§7 compose prose → 🛠 Build; §7 `approved/` export prose → Folder one-step write + `review.html`) and add `review.html` to the layout tree — not a wholesale ADOPT rewrite. Setup mechanics (§1–5, §8) untouched.
  3. **Loop framing centers the spinalcord motivation** — the land + house-style/consistency review pass (the friction that bypassed the manifest) is the loop's closing emphasis.
  4. **No code, no tests** — pure docs; verification is a markdown/link consistency pass (lib-only test culture is irrelevant here).

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

The four docs form a clean quadrant once WORKFLOW lands: **README** (feature overview) · **USAGE** (prompt/reference craft) · **ADOPT** (submodule setup + `imagegen/` layout) · **WORKFLOW** (the directed operating loop). WORKFLOW.md is the connective tissue that was missing — spinalcord improvised it as an `imagegen/README.md`. The risk to avoid is duplication: WORKFLOW links out for layout (ADOPT §7) and craft (USAGE) and keeps itself to the sequence + the land/review discipline. ADOPT edits are surgical reconciliation of prose that the `.2`/`.3`/`.4` features made stale, pre-assigned here by `.3`.

Discovery surfaced no significant deviation (the ADOPT reconciliation was pre-scoped here by `.3`; the loop outline + cross-link targets come straight from the task line) → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — matched the existing docs' voice/shape: `WORKFLOW.md` mirrors README/ADOPT/USAGE markdown conventions (H1 + intro positioning the doc against its siblings, fenced code, sectioned steps); ADOPT edits are surgical in-place prose swaps. The four docs now form a clean quadrant (overview / craft / setup+layout / loop) — no new doc pattern invented.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — N/A (pure docs; no executable surface).

**Implementation Notes:**

- `docs/WORKFLOW.md` (new) — the directed operating loop as 10 ordered stages (stage → build → import → attach refs → generate → review → iterate → export → land → review), each naming the in-app affordance (🛠 Build, ⇪ Import, Reference Library, Generate / ⚡ Generate All, keep/discard/approve + Use as reference, Iterate →, Folder/JSON/Sheet) and linking out to ADOPT §7 (layout) + USAGE (craft) instead of duplicating. Closes with a "why the closing review matters" section centering the spinalcord friction (manifest bypass, no consistency surface).
- `docs/ADOPT.md` — reconciled the `.2`/`.3`/`.4`-induced staleness deferred here by `.3`: §6 compose prose now names 🛠 Build; §7 `tasks.json` bullet → 🛠 Build; §7 `approved/` block rewritten for **Folder** (one-step `manifest.json` + images), **JSON**, **Sheet** (`review.html`) instead of the export-by-rename + per-image-↓ flow; `review.html` added to the `imagegen/` tree; intro + §7 tail cross-link WORKFLOW.md.
- `README.md` — added WORKFLOW.md to the Docs list (top of the three doc entries).
- `docs/USAGE.md` — intro now cross-links WORKFLOW.md for the end-to-end sequence.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — N/A (docs-only; no code touched).

- [x] Ran lint/type-check on changed code — N/A (no code; eslint/tsc not applicable). Verified instead: all cross-link targets exist (`docs/WORKFLOW.md`, `USAGE.md`, `ADOPT.md`, `README.md`) and the three USAGE anchors (`#prompt-writing`, `#iteration-prompts`, `#reference-image-preparation`) map exactly to their headers; all WORKFLOW.md outbound links resolve.

- [x] (frontend) Asked the user for visual confirmation — N/A (no UI surface; markdown only).

**Testing Notes:** Verification was the link/anchor consistency pass (grep against headers + file existence) — all resolve. No contradictions with README's 6-step overview (WORKFLOW expands it into the directed loop; the two stay consistent).

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md` **updated** (WORKFLOW.md added to Docs list) · `AGENTS.md` no change (no workflow/export prose) · `CLAUDE.md` no change · `.flowtron/PLAN.md` updated (`.5` flipped to stub; epic stays open with `.6` audit pending). `docs/WORKFLOW.md` (new) + `docs/ADOPT.md`/`docs/USAGE.md` (reconciled) are outside the AI-referenced cold-start set but are the substance of this task.

- [x] Closed — PLAN.md `.5` line flipped to stub form `Completed 2026-06-14.` and tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Wrote `docs/WORKFLOW.md` — the directed adopter operating loop (10 ordered stages from prompt-staging through the closing house-style review pass) that ties the BI-021.2/.3/.4 affordances into one playbook, completing the doc quadrant (README overview · USAGE craft · ADOPT setup+layout · WORKFLOW loop). It links out for layout and craft rather than duplicating. Also reconciled the ADOPT.md staleness BI-021.3 deferred here: §6/§7 compose prose now names 🛠 Build, the `approved/` export prose reflects Folder/JSON/Sheet (one-step manifest write + `review.html`) instead of the old export-by-rename flow, and `review.html` joins the `imagegen/` tree. Cross-links added from README, ADOPT, and USAGE. Docs-only; all cross-links + USAGE anchors verified to resolve. `BI-EPIC-021` now has only its `.6` audit remaining.

**Archived:** 2026-06-14
