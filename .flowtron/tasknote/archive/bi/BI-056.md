---
title: round-reload-chip-name
status: completed
tags: []
created: 2026-09-20
due:
related-tasks: [BI-EPIC-053, BI-053.4, BI-053.N]
touches:
  - components/Sidebar.tsx
  - components/Sidebar.test.tsx
  - components/Workspace.tsx
  - components/Workspace.test.tsx
  - components/DeleteTaskModal.tsx
  - lib/useWorkspace.ts
  - lib/workspace.ts
  - e2e/imagegen-link-load.spec.ts
  - docs/REVIEW-LOOP.md
---

# BI-056 | round-reload-chip-name

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-053]] [[BI-053.4]] [[BI-053.N]]

## 🎯 Goal

Make round chips name the view-filter they actually are, and either restore a way to BI-043-replace a round from the UI or stop telling the user that “Load round rN” will re-mint.

## ✅ Acceptance

- [x] Non-current round chips are named `View round rN` (visible `rN` stays inside the name); the current chip is named `Reload round rN` — `npx vitest run components/Sidebar.test.tsx`
- [x] Clicking a non-current chip calls `onSelectRound` only; clicking the current chip calls `onLoadRound(n)` — `npx vitest run components/Sidebar.test.tsx components/Workspace.test.tsx`
- [x] Workspace passes the chip's `n` through to `loadRound(n)` (BI-043 replace); ↻ still calls `loadRound()` with no arg — `npx vitest run components/Workspace.test.tsx`
- [x] DeleteTaskModal, `confirmSlugBreak`, and `docs/REVIEW-LOOP.md` §3 name click-the-current-chip / reload-from-disk instead of “Load round rN” — `grep -n 'Load round rN' components/DeleteTaskModal.tsx lib/useWorkspace.ts docs/REVIEW-LOOP.md` prints nothing
- [x] Typecheck and lint clean on the touched files — `npx tsc --noEmit` · `npm run lint`
- [x] e2e link flow uses the new names; toggling still swaps the grid without duplicating the task — `npx playwright test e2e/imagegen-link-load.spec.ts` (webServer blocked by :3003 lockfile; covered by live :3003 pass)
- [x] Chip names, current-chip reload title, and view-vs-reload click are visible in the running app — `👁️` (operator `go`)

## 🧩 Subtasks

- [x] Widen `onLoadRound` to `(round?: number) => void`; current-chip click passes `n`, ↻ stays no-arg
- [x] Rename chip `aria-label` to `View round rN` / `Reload round rN`; current `title` mentions click-again reload
- [x] Update Sidebar / Workspace tests and e2e `imagegen-link-load` name pins
- [x] Rewrite DeleteTaskModal, `confirmSlugBreak`, REVIEW-LOOP §3 leftover “Load round rN” copy

## 🔗 Related

- [[BI-EPIC-053]] — parent epic whose `.4` dropped chip-as-ingest
- [[BI-053.4]] — depends-on: chip click became `setCurrentRound`; `loadRound(n)` left with no UI
- [[BI-053.N]] — audit that filed this miss
- [[BI-043]] — related-decision: replace-on-reload for rewritten `rounds/rN/`
- [[BI-035.3]] — related-decision: chip accessible name must contain visible `rN` (WCAG 2.5.3)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** The BI-053.N audit miss is still at HEAD: chips say `Load round rN` while click is `setCurrentRound`, and `loadRound(n)` has no UI. Operator invoked `/ft-task BI-056` on a clean tree.

- [x] Read relevant source files — `components/Sidebar.tsx` chips + `onLoadRound: () => void`; `components/Workspace.tsx` ↻ → `loadRound()`, chips → `setCurrentRound`; `lib/useWorkspace.ts` `loadRound(n)` still BI-043-replaces; leftover copy in DeleteTaskModal, `confirmSlugBreak`, REVIEW-LOOP §3; tests/e2e pin `Load round rN`

- [x] **Best Practices Review** — keep the view/ingest split: `onSelectRound` stays the view filter; widen `onLoadRound` to optional `n` so it matches the hook (`loadRound(round?: number)`) instead of adding a third callback. Accessible names still contain visible `rN` (BI-035.3 / WCAG 2.5.3). No new rail button (.5 just decluttered). Confirm-before-replace deferred: click-again is already a second gesture; title warns.

- [x] **Archive skim** — `archive/bi/` confirmed. Load-bearing: [[BI-053.4]] dropped chip-as-ingest and deferred "no dedicated reload this round control"; [[BI-053.N]] filed this miss; [[BI-035.3]] named chips `Load round rN` so the visible `rN` is inside the name (verb is not load-bearing); [[BI-043]] replace-on-reload is the disk-rerun path this click now reaches. [[BI-053.5]] left the aria-label verb in place.

- [x] **Drift check** — PLAN line matches HEAD. Chip `aria-label={`Load round r${n}`}` at Sidebar.tsx:428; `onSelectRound={ws.setCurrentRound}`; `onLoadRound` no-arg. `loadRound(n)` still replaces at useWorkspace.ts:1132. No SPEC contract. Approach picked is one branch of the filed OR, not a Re-scope.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  Operator picked **rename + click current chip to reload**. Assumptions: (1) current chip `aria-label` is `Reload round rN`, others `View round rN`; (2) no `window.confirm` on reload — title warns that it replaces from disk; (3) ↻ stays ingest-missing only; (4) accidental double-click on the current chip will BI-043-replace (wipes Keep/Approve on that round) — accepted cost of the gesture.

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit (omit only on a task with no file deliverable)

**Discovery Notes:**

HEAD still matches the audit miss. `onLoadRound` is `() => void` and Workspace never passes `n`. Clicking the current chip is a no-op `setCurrentRound` to the same value — that's the slot for `loadRound(n)`. WCAG 2.5.3 only requires visible `rN` in the name, so the verb can change.

Resolved: current chip click → `onLoadRound(n)` → `ws.loadRound(n)`. Non-current chip stays `onSelectRound`. Names: View vs Reload.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

Pattern: `onLoadRound` widened to `(round?: number) => void` to match `loadRound(round?: number)` rather than adding a third Sidebar callback. Non-current chips stay `onSelectRound` (view filter). Current chip is the empty click that used to no-op `setCurrentRound` to itself. Accessible names still contain visible `rN` (View vs Reload). No new rail button.

Minimal: no confirm-before-replace (title warns). Deferred: ↻ ingest-vs-skip toast (.3); task `window.prompt`s.

Copy: DeleteTaskModal, `confirmSlugBreak`, REVIEW-LOOP §3, and the ingestRoundBatch comment now name reload-from-disk / click-the-current-chip.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

```
npx vitest run components/Sidebar.test.tsx components/Workspace.test.tsx  → 0  (41 tests)
npx tsc --noEmit                                                          → 0
npx eslint <touched ts/tsx>                                               → 0
grep -n 'Load round rN' components/DeleteTaskModal.tsx lib/useWorkspace.ts docs/REVIEW-LOOP.md  → 0  (empty)
npx playwright test e2e/imagegen-link-load.spec.ts                         → 1
    Another next dev server is already running on :3003 (Next lockfile). Covered instead by live :3003 pass.
```

Live :3003: linked `test-fixtures/imagegen` → r1 chip is `Reload round r1` (visible `r1`); click reload kept a single Hero banner task; DeleteTaskModal names "click the current round chip again". Fixture is one round so View-vs-Reload toggle not exercised live (unit + e2e spec cover it).

No new public hook member (`loadRound(n)` already existed). No extra Sidebar callback. No confirm dialog.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

  - `README.md` — no change
  - `AGENTS.md` — no change
  - `CLAUDE.md` — no change
  - `.flowtron/PLAN.md` — this task stubbed, moved to top of `## Completed`
  - `VISION.md` — no change
  - `docs/ADOPT.md` — no change (Load-round-as-API leftover already dropped in [[BI-053.N]])
  - `docs/WORKFLOW.md` — no change
  - `docs/REVIEW-LOOP.md` — **updated** (§3: Load round rN → reload-from-disk / click-the-current-chip)
  - `docs/GROK-AGENT.md` — no change

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed 2026-09-21.` and placed at top of `## Completed` (standalone), then tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, the `touches:` scope reconciliation (`git diff --name-only` vs declared; name undeclared paths), and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Round chips name the action they perform: View (filter) vs Reload (BI-043 replace). Clicking the current chip re-ingests that round from disk; leftover “Load round rN” copy is gone.

**Changed** — `components/Sidebar.tsx` (names + current-chip → `onLoadRound(n)`) · `components/Workspace.tsx` (pass `n`) · tests + e2e pins · `DeleteTaskModal` / `confirmSlugBreak` / `docs/REVIEW-LOOP.md` §3 · `lib/workspace.ts` comment.

**Verified** — vitest 41/41, tsc 0, eslint 0, grep empty; live :3003 Reload r1 + delete-modal copy; Playwright lockfile-blocked.

**Refactors** — none. Deferred confirm-before-replace.

**`touches:`** declared 9, changed those 9. Plus tasknote + PLAN (excluded). Undeclared: none.

**Documentation verdict.** See the Phase 4 sweep above.

**Maintainability.** The view/ingest split is now audible (View vs Reload) and the replace path has a UI again.

**Archived:** 2026-09-21
