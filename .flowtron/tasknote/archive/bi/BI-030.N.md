---
title: silent-drift-surfacing audit
status: completed
tags: []
created: 2026-08-08
due:
related-tasks: [BI-EPIC-030, BI-030.2, BI-030.3, BI-030.4]
---

# BI-030.N | silent-drift-surfacing audit

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-030]]

## 🎯 Goal

Verify the completed `BI-EPIC-030` (`silent-drift-surfacing`) cohort sits coherently in the codebase: cumulative doc-drift sweep across `.flowtron/tasknote/README.md` §"AI-referenced docs", naming/style consistency across the cohort's deliverables, and follow-up filings for any miss.

## ✅ Acceptance

- [x] **Doc-drift sweep (fixed line, per SPEC/epic.md §"Audit acceptance — fixed doc-drift line")** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the specific update. Always present; surfaces cumulative slice-local staleness that per-task Phase 4 closures can miss.
- [x] Cohort coherence inventory: each implementation child's deliverables read against the others (naming consistency, style parity, no contradictory cross-refs)
- [x] No regressions surfaced in earlier-shipped cohort children's surfaces
- [x] Audit findings recorded in Implementation Notes; misses cited as candidates for `/ft-file-followup <NEW-ID>` filing (filed AFTER audit closure to preserve `/ft-file-followup`'s filing-discipline gate)
- [x] Single `chore: BI-030.N — audit BI-EPIC-030` commit lands (`chore:` — no code edits landed)
- [x] PLAN.md line for `BI-030.N` flipped to stub form `Completed YYYY-MM-DD.`
- [x] Tasknote moved to `.flowtron/tasknote/archive/bi/BI-030.N.md`
- [x] Parent-flip prompt surfaced after audit closure (skill Step 8) — user confirms or declines flipping `BI-EPIC-030` to `Completed` and moving the cohort to `## Completed`

## 🧩 Subtasks

- [x] Inventory cohort children's archived tasknotes — read each implementation child's Final Summary + Implementation Notes; capture deliverables in Discovery Notes
- [x] Walk `.flowtron/tasknote/README.md` §"AI-referenced docs" entries — fixed doc-drift sweep
- [x] Cohort coherence pass — naming consistency, style parity, no contradictory cross-refs across the cohort's deliverables
- [x] Surface audit findings in Implementation Notes; cite each miss as a `/ft-file-followup <NEW-ID>` candidate
- [x] Phase 4: flip `BI-030.N` PLAN line to stub form + archive tasknote
- [x] Parent-flip: skill Step 8 prompts user; on confirm, atomic flip parent line + move cohort to `## Completed`

## 🔗 Related

- [[BI-EPIC-030]] — parent epic: surface silent drift in three load-bearing joins
- [[BI-030.2]] — cohort child: reversible approve
- [[BI-030.3]] — cohort child: rename slug guard
- [[BI-030.4]] — cohort child: schema-mismatch banner

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md — parent `BI-EPIC-030` active under `## Medium`; children `.2`/`.3`/`.4` all `[x]`, `.N` the only open row. No `.1` Discovery child: this epic's scoping was supplied by `/ft-audit-repo` on 2026-08-06, so implementation children start at `.2` (same shape as `BI-EPIC-029` and `BI-EPIC-031` in this repo).

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Operator invoked `/ft-close-epic BI-030.N`; Step 2 pre-flight passed with no open siblings, so the cohort is complete and the audit covers the full three-child set rather than a partial one. No early-audit decision was taken.

- [x] Read relevant source files — all three children's touched modules verified at HEAD (see Drift check), plus the four AI-referenced docs and `docs/REVIEW-LOOP.md`.

- [x] **Best Practices Review** — `N/A` as a code-authoring gate (an audit is a verification pass, not new surface). Exercised instead as the coherence lens in Phase 2: the cohort's three children all extend the same pre-existing layering (pure contract in `lib/workspace.ts` / `lib/roundSelection.ts` / `lib/storage.ts` → FSA writes in `lib/imagegenFs.ts` → hook orchestration and user-facing strings in `lib/useWorkspace.ts` → `lib/ImagegenContext.tsx` as the injection seam). Dependency direction is unchanged in all three; no child introduced a new abstraction.

- [x] **Archive skim** — largely self-referential (the cohort children *are* the relevant archive entries: `BI-030.2.md`, `BI-030.3.md`, `BI-030.4.md`, all read in full). Non-cohort context that the children themselves cite and that this audit re-checked: `BI-024.1`/`BI-024.2` (built the round-load and approve/selection seams the cohort makes reversible and guarded), `BI-002` (authored the `loadSession` guards whose single `null` `.4` replaced), `BI-022.7` (the import path that has named schema mismatches since before `.4`), `BI-029.2`/`BI-029.4` (touched `imagegenFs.ts` / `ImagegenContext.tsx` for the blob seam — orthogonal to the write paths here, and still green).

- [x] **Drift check** — every symbol the three children shipped is present at HEAD and referenced by live callers: `buildSkipSelectionTask` (`lib/roundSelection.ts:76`), `removeApprovedFile` (`lib/imagegenFs.ts:361`), `unpromoteApproved` (`lib/ImagegenContext.tsx:81`), `handleImagegenUnapprove` + `renameSlugBreak`/`confirmSlugBreak` (`lib/workspace.ts` + `lib/useWorkspace.ts:118`), `SessionLoad` (`lib/storage.ts:61-65`, carried through `lib/persistence.ts:44`/`:56`). **No drift.**

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — **No clarifications needed.** Cohort scope is unambiguous (three closed children, no deferrals, no partial-cohort decision).

- [x] Subtasks above populated with concrete, ordered steps — the Step 3 scaffold's canonical epic-audit list holds unchanged; Discovery surfaced no scope shift.

**Discovery Notes:**

**Cohort inventory (deliverables per child).**

- **`BI-030.2` — reversible-approve.** Five source files, ~90 LOC. Made the approve write into the host repo undoable: `removeApprovedFile` (idempotent FSA delete) + `buildSkipSelectionTask` + `unpromoteApproved` + `handleImagegenUnapprove`, each the mirror of an existing symbol in the same layer. Two guards off the existing `buildApprovedImages` derivation stop the undo clobbering a sibling approval's `selection.json` entry or deleting a file another round's approval still needs. 163/163 tests (+9). Docs: `README.md` step 4, `docs/REVIEW-LOOP.md` §3/§4.
- **`BI-030.3` — rename-slug-guard.** Four source files, ~60 LOC. Pure predicate `renameSlugBreak` in `lib/workspace.ts` (beside `renameTask`/`ingestRoundBatch`) + `confirmSlugBreak` in the hook; `renameTask` widened `void`→`boolean` so `TaskDetail`'s uncontrolled name input resets on decline. Compares *slugs* not names, and deliberately never consults `imagegen.linked`. 172/172 tests (+9). Docs: `README.md` step 4, `docs/REVIEW-LOOP.md` §3.
- **`BI-030.4` — schema-mismatch-banner.** Three source files, ~55 LOC net. `SessionLoad` union (`ok`/`absent`/`corrupt`/`unsupported-version`) **replaces** `loadSession`'s `Session | null`; seam widened in `lib/persistence.ts`; `unreadableSessionReason` raises the existing red banner at mount-after-bootstrap and on a refused switch. Nothing is deleted on a rejected load. 179/179 tests (+7). Docs: `README.md` persistence paragraph.

**Epic premise coverage.** The PLAN line names three load-bearing contracts. All three are addressed, one child each: `slugify(name)` join → `.3`; `schemaVersion` load gate → `.4`; one-way approve write → `.2`. The epic's constraint ("make the drift visible; do not re-architect the joins") held — no child changed a join's shape; `.4`'s `SessionLoad` widening is a return-type change on an existing seam, not a new join.

**Verification at HEAD (audit re-run, not inherited):** `npm test` **179/179** across 14 files, `npx tsc --noEmit` clean, `npm run lint` exit 0. Matches `.4`'s closing figure exactly — no cohort child regressed a sibling's surface.

✅ Phase 1 Discovery complete; entering Phase 2 Execution.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `N/A` for new code (no source surface added). The one inline fix is a tasknote paperwork correction; the precedent surveyed is siblings `BI-030.2.md`/`BI-030.3.md`, whose Acceptance and Subtask blocks are fully ticked at archive time.

- [x] **Minimal refactor gate** — no refactor. Both code-level findings below were deliberately scoped out by their own child's Discovery (`BI-030.2` assumption 1, `BI-030.3` assumption 2); fixing either here would be the audit widening the epic's remit unasked. Logged as follow-up candidates instead.

- [x] Implemented the minimal solution — one inline fix (`BI-030.4.md` paperwork); two findings deferred to follow-up filings.

- [x] Updated/added tests for non-trivial behavior — `N/A`; no behavior changed.

**Implementation Notes:**

**Coherence verdict: the cohort is consistent.** The three children extend one layering without exception — pure decision in the module that already owns the contract (`roundSelection.ts` / `workspace.ts` / `storage.ts`), FSA writes in `imagegenFs.ts`, orchestration and every user-facing string in `useWorkspace.ts`, `ImagegenContext.tsx` as the injection seam. Naming is uniformly mirror-of-existing (`buildSkipSelectionTask`↔`buildApproveSelectionTask`, `removeApprovedFile`↔`promoteKeeperToApproved`, `unpromoteApproved`↔`promoteApproved`). Every new symbol carries a JSDoc citing its `BI-030.x` origin, and each of the three children ran a **mutation check** on its own guard to prove the new tests were load-bearing rather than vacuous — a method `.2` introduced and `.3`/`.4` explicitly adopted. Cross-refs are consistent and none contradict: `.3` and `.4` both cite `.2` as the epic's method-setter, and `docs/REVIEW-LOOP.md` §4 points forward to §3 for the undo contract.

**Three surfacing mechanisms, one per child — checked, not drift.** `.2` is silent (the user initiated the undo; there is nothing to report), `.3` blocks with `window.confirm` (pre-action, so the user can still back out), `.4` raises the existing red banner (post-facto report of something already done). Each fits its moment and none invents a fourth error channel — `.4` reuses `components/Workspace.tsx`'s existing `ws.error` render rather than adding a surface. The one *explicit* divergence is documented on both sides: `.3` does not consult `imagegen.linked` where `.2` no-ops when unlinked, because `.3`'s detection needs no FS handle. That reasoning is written into `.3`'s Implementation Notes **and** its `renameSlugBreak` JSDoc, so the next reader finds it at the code.

**No regressions in earlier-shipped children.** Re-ran at HEAD rather than trusting the closing figures: `npm test` 179/179 across 14 files, `tsc --noEmit` clean, `eslint` exit 0 — identical to `.4`'s closing number, and `.2`'s and `.3`'s surfaces (`imagegenFs.test.ts`, `roundSelection.test.ts`, `workspace.test.ts`, `useWorkspace.test.ts`) are all still green. `BI-029`'s blob-seam work in the same two files is unaffected.

**Inline fix applied (1).** `.flowtron/tasknote/archive/bi/BI-030.4.md` — its eight Acceptance criteria and seven Subtasks were left `[ ]` at closure while all four phase blocks were ticked, against SPEC §"🚀 Phase 4: Closure" ("never leave a box silently unticked") and out of parity with `.2`/`.3`. Ticked against re-verified evidence at HEAD (symbol locations + the 179/179 / tsc / lint run above), with an HTML comment recording that the audit did it and on what basis; also added the template's missing `due:` frontmatter key. Paperwork only — no source file touched.

**Follow-up candidates (2)** — both are *unsurfaced silent drift in the epic's own named contracts*, each deliberately deferred by the child that found it, so they belong to the epic's theme rather than to scope creep:

1. **`promoteKeeperToApproved` overwrites silently on a filename collision.** `lib/imagegenFs.ts:343` opens the destination with `getFileHandle(keeperFilename, { create: true })` under a flat `approved/` namespace, so approving `r1/hero-001.jpg` and later `r2/hero-001.jpg` replaces the first file with no warning. `BI-030.2` guarded its *undo* against exactly this collision (and `docs/REVIEW-LOOP.md` §3 names `approved/` as "flat, filename-keyed") but explicitly left the *approve* path alone as pre-existing — assumption 1: "this task only avoids *worsening* it". It is a one-way silent write into the user's repo, which is the third contract `BI-EPIC-030` set out to surface. `/ft-file-followup` candidate.
2. **`deleteTask` breaks the same slug join `renameSlugBreak` now guards.** `lib/useWorkspace.ts:461` deletes straight through `deleteTaskFrom` with no equivalent check, so deleting a task carrying `imagegen:rounds/r<N>/…` images orphans the on-disk round exactly as an unguarded rename would. `BI-030.3` names it as out of scope (assumption 2). The epic's first named contract is the slug join "in both directions", so the guard covering one mutation and not its sibling is a visible half-edge. `/ft-file-followup` candidate.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — no source changed, but the cohort's suites were re-run as the regression check: full `npm test` **179/179** across 14 files (5.31s).

- [x] Ran lint/type-check on changed code — `npx tsc --noEmit` clean; `npm run lint` exit 0. Run against HEAD as a whole since the audit's own diff is markdown.

- [x] **Quality assertions** — `N/A` for the audit's own diff (markdown only: one new tasknote, one archived tasknote's checkboxes + `due:` key). Applied *as the audit lens* to the cohort in Phase 2: no avoidable duplication (each child reused an existing derivation rather than adding a parallel helper), no dead code (`SessionLoad`'s four states each have a live producer and consumer), public surface grew only by the mirror symbols each child's Acceptance required, and every new symbol's JSDoc is current.

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask) — `N/A`: the audit changed no rendered surface. Each cohort child carried its own 👁️ ask at its 📦 gate.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

The audit's verification is the cohort's own suite re-run at HEAD, which is the meaningful check for "no regressions surfaced in earlier-shipped cohort children's surfaces": 179/179 matches `BI-030.4`'s closing figure exactly, so nothing that landed after `.2` and `.3` disturbed them. No test was added — the audit changed no behavior, and adding one would assert nothing the cohort's 25 new tests (+9 / +9 / +7) do not already cover.

Also spot-checked that the cohort's guards remain load-bearing rather than merely present: each child recorded a mutation check at its own Phase 3 (neutralize the guard → exactly the load-bearing tests fail, silence controls stay green), and those tests are all in the passing 179.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — walked all four entries in `.flowtron/tasknote/README.md` §"AI-referenced docs":
  - `README.md` — **no change.** All three children's user-facing behavior is already stated and reads coherently as one surface: the rename confirm + `tasks.json` follow-up in adopter-loop step 4 (`.3`), reversible approve in in-app step 4 (`.2`), and the unreadable-project banner paragraph (`.4`). Cross-checked against `docs/REVIEW-LOOP.md` §3 — no contradiction.
  - `AGENTS.md` — **no change.** Carries flowtron workflow scaffolding only; no persistence, slug-join, or review-loop detail to drift.
  - `CLAUDE.md` — **no change.** Its Stack section describes `lib/persistence.ts` as the async seam, still accurate after `.4` widened the two seam methods (the seam's shape moved, not its role); the testing paragraph's seam-over-mock convention is exactly what `.3` and `.4` followed (`vi.stubGlobal` for `confirm`, real storage for the banner tests).
  - `.flowtron/PLAN.md` — **updated.** `BI-030.N` child line flipped to stub form; parent-flip held for the operator prompt.
  - Beyond the declared set, re-read `docs/REVIEW-LOOP.md` (the adopter SSOT the cohort actually touched): §3 carries both `.2`'s reversible-approve contract and `.3`'s slug-join warning, and §4 cross-refs §3. **No change needed** — already current and internally consistent.

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-08-08.`, kept 2-space nested beneath `BI-EPIC-030` per SPEC/epic.md §"Child placement invariant", then tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] **Evidence-based recap** drafted — surfaces at the 📦 ready-to-commit gate alongside the parent-flip prompt

**Final Summary:**

`BI-EPIC-030` closes coherent: the three named contracts each got exactly one child, none re-architected a join, and re-running the suite at HEAD reproduces `.4`'s 179/179 with `tsc` and `eslint` clean — so no child regressed a sibling's surface.

The cohort reads as one piece of work rather than three. Every new symbol is the mirror of an existing one in the same layer (`buildSkipSelectionTask`↔`buildApproveSelectionTask`, `removeApprovedFile`↔`promoteKeeperToApproved`, `unpromoteApproved`↔`promoteApproved`), the pure-decision / hook-owns-strings split holds in all three, and the one deliberate divergence — `.3` warning even when the imagegen folder is unlinked where `.2` no-ops — is justified in both tasknotes *and* at the code in `renameSlugBreak`'s JSDoc. The three different surfacing mechanisms (silent undo · blocking confirm · red banner) were checked and are not drift: each matches whether the user is acting, about to act, or being told after the fact, and `.4` reused the existing `ws.error` render rather than adding a fourth channel. Notably, `.2` established a mutation check on its own guard and `.3`/`.4` both adopted it, so the cohort's 25 new tests are demonstrably load-bearing rather than vacuous.

One inline fix, paperwork only: `BI-030.4`'s archived tasknote had all four phase blocks ticked but its eight Acceptance criteria and seven Subtasks left `[ ]`, against SPEC §"🚀 Phase 4: Closure" and out of parity with `.2`/`.3`. Ticked against re-verified evidence (symbol locations at HEAD plus the 179/179 run), annotated with an HTML comment naming the audit as the author and its basis; missing `due:` frontmatter key restored. No source file touched.

Two misses logged for `/ft-file-followup`, both unsurfaced silent drift inside the epic's *own* named contracts and both deliberately deferred by the child that found them: `promoteKeeperToApproved` still overwrites silently on a flat-`approved/` filename collision (`lib/imagegenFs.ts:343` — `.2` guarded only its undo against this), and `deleteTask` breaks the same slug join `renameSlugBreak` now guards (`lib/useWorkspace.ts:461` — `.3` scoped it out). Maintainability effect: all three joins now speak at the moment they break, and the two remaining half-edges are written down with file:line rather than living only in a deferred assumption inside an archived tasknote.

**Parent flip:** Operator confirmed at the 📦 gate. `BI-EPIC-030` flipped to stub form `Completed 2026-08-08.` and the parent plus all four children (`.2`, `.3`, `.4`, `.N`) moved atomically from `## Medium` to the top of `## Completed` in the same commit. `## Medium` retains `BI-EPIC-031`, so no `(none)` placeholder was needed.

**Archived:** 2026-08-08
