---
title: memoize-imagegen-value
status: completed
tags: []
created: 2026-08-09
due:
related-tasks: [BI-EPIC-042, BI-042.2, BI-042.3]
---

# BI-042.4 | memoize-imagegen-value

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-042]] · [[BI-042.2]] · [[BI-042.3]]

## 🎯 Goal

Stabilize the `ImagegenContext` value with `useMemo` so identity only changes when `linked` or `blobEpoch` (or a callback) changes, ending the latent per-render `listRounds()` walk in `useWorkspace`.

## ✅ Acceptance

- [x] `ImagegenProvider` builds its context `value` via `useMemo`, not a fresh object every render.
- [x] Memo deps cover `linked`, `blobEpoch`, and the stable callback set (all already `useCallback(..., [])`).
- [x] Existing `lib/ImagegenContext.test.tsx` suite still passes; add a small identity-stability assertion if cheap.
- [x] No production behavior change beyond fewer redundant `listRounds()` / consumer re-renders.

## 🧩 Subtasks

- [x] Wrap the context `value` object in `useMemo` with correct deps in `lib/ImagegenContext.tsx`
- [x] Add (or skip-with-reason) a referential-stability test under `lib/ImagegenContext.test.tsx`
- [x] Run targeted tests + type-check

## 🔗 Related

- [[BI-EPIC-042]] — parent epic (blob-url-lifetime)
- [[BI-042.2]] — shipped epoch/retain; deferred `useMemo` here
- [[BI-042.3]] — consumer recovery tests; also deferred `useMemo` here

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** The gap is still real and unchanged: `value` is a plain object literal each render (lines 284–297), while `useWorkspace`'s discover-rounds effect depends on `[imagegen, imagegen.linked]` and re-walks `listRounds()` whenever that object identity flips. Prior siblings explicitly deferred this fix.

- [x] Read relevant source files

- [x] **Best Practices Review** — Touched responsibility is only the context value's referential stability inside `ImagegenProvider`. Dependency direction unchanged (`imagegenFs` → context → consumers). Pattern: callbacks already `useCallback([], [])`; only `linked` and `blobEpoch` are state. No nearby `useMemo` on context values elsewhere in the repo. Required change is exactly the memo wrap; no in-scope refactor beyond that. Deferred cleanup: none.

- [x] **Archive skim** — BI-042.2 and BI-042.3 both record "useMemo on the context value stays in BI-042.4". BI-029.4 introduced the cache; BI-042.2 added `blobEpoch` + retain without memoizing value. No contradictory prior decision.

- [x] **Drift check** — PLAN claim matches code:
  - `lib/ImagegenContext.tsx` L284–297: `const value: ImagegenApi = { … }` (not memoized)
  - `lib/useWorkspace.ts` L365–378: effect deps `[imagegen, imagegen.linked]` calling `imagegen.listRounds()`
  - Callbacks are all `useCallback(..., [])` — stable once memoized deps include them

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  **No clarifications needed.** Assumptions:
  1. Only production file change is `lib/ImagegenContext.tsx` (`useMemo` + import).
  2. When `blobEpoch` bumps, value identity *should* change (consumers must re-render); a redundant `listRounds()` on that rare path is acceptable.
  3. Do not rewrite `useWorkspace` effect deps to `[imagegen.linked]` alone — out of scope; memo is the filed fix.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- Provider wraps `WorkspaceInner` (`components/Workspace.tsx`); provider re-renders on its own state or parent re-render, each time minting a new `value` without memo.
- `listRounds` itself is already stable (`useCallback` empty deps) — the waste is the effect re-firing on object identity, not the callback changing.
- `blobEpoch` in the memo deps is correct: ResolvedImage and other consumers read it from context and need identity/value change to wake.

Discovery surfaced no significant deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — React context-value `useMemo` is the standard fix; no prior local example, so this is the justified first instance. DRY/SRP: still one place builds `ImagegenApi`; composition unchanged.

- [x] **Minimal refactor gate** — only the value construction + `useMemo` import. No dep rewrites in `useWorkspace`. Deferred: none.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- `lib/ImagegenContext.tsx` — `useMemo` wrap with deps `[linked, blobEpoch, …all callbacks]`.
- `lib/ImagegenContext.test.tsx` — new describe `ImagegenApi value identity (BI-042.4)`: parent state bump re-renders the provider without changing `linked`/`blobEpoch`; context object reference must be identical.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — single memo wrap; no dead code; public surface unchanged; file header comment still accurate (no API/docs update needed).

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

- `npx vitest run lib/ImagegenContext.test.tsx` — 10/10 passed (includes new identity test).
- `npx tsc --noEmit` — clean.
- **👁️ Visual confirmation — N/A.** No UI chrome or render path change; only context object identity. Operator cannot observe this without React DevTools / FSA profiling.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-08-09.`, kept 2-space nested beneath the active `BI-EPIC-042` parent; tasknote moved to `.flowtron/tasknote/archive/bi/BI-042.4.md`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

`ImagegenProvider` now memoizes its context value so object identity tracks only real state (`linked` / `blobEpoch`) and stable callbacks. That stops pure provider re-renders from re-running `useWorkspace`'s discover-rounds effect and re-walking the FSA tree. One identity-stability unit test pins the contract.

**Archived:** 2026-08-09
