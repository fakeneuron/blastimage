---
title: generated-quota
status: in-progress
tags: []
created: 2026-06-10
due:
related-tasks: []
---

# BI-017 | generated-quota

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Add a session-level advisory warning when accumulated generated-image data URLs exceed a threshold, alerting users before they hit the localStorage cliff.

## ✅ Acceptance

- [ ] `countGeneratedImageBytes(session)` pure function in `lib/workspace.ts` sums the byte-length of all generated image data URLs
- [ ] `GENERATED_QUOTA_WARN_BYTES` constant exported from `lib/workspace.ts`
- [ ] Amber dismissible banner appears in `Workspace.tsx` when generated image bytes ≥ threshold
- [ ] Banner is styled consistently with the existing amber warning in `ReferenceLibrary.tsx`
- [ ] Test for `countGeneratedImageBytes` added to `lib/workspace.test.ts`
- [ ] TypeScript type-check passes; lint passes

## 🧩 Subtasks

- [ ] Add `GENERATED_QUOTA_WARN_BYTES` constant and `countGeneratedImageBytes` to `lib/workspace.ts`
- [ ] Import + wire the warning into `components/Workspace.tsx` (amber dismissible banner)
- [ ] Add test for `countGeneratedImageBytes` in `lib/workspace.test.ts`
- [ ] Run type-check + lint

## 🔗 Related

- (none)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** The Grok Imagine provider returns data URLs that are stored inline in `GeneratedImage.url` (confirmed in `lib/generate.ts` comment and the BI-013 architecture). Accumulating batches of 4 images per task across multiple iterations can easily push the session past a 5 MB localStorage limit. The guard is directly relevant and well-scoped.

- [x] Read relevant source files

- [x] **Archive skim** — no prior tasknotes covering quota guards or storage warnings beyond what's already in code; `BI-007.md` and `BI-001.md` were checked.

- [x] **Drift check** — `MAX_REF_BYTES = 2 * 1024 * 1024` at `ReferenceLibrary.tsx:19`, amber warning at line 126, `DEFAULT_BATCH_SIZE = 4` in `useWorkspace.ts:56`, `GeneratedImage.url` confirmed as data URL storage site. All paths match.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed. Assumptions:
  - Threshold: `4 * 1024 * 1024` (4 MB) for warning — conservative enough to warn before the 5 MB cliff, generous enough not to fire too early
  - Only data URLs (`.startsWith('data:')`) are counted — remote URLs are negligible
  - Warning is session-level amber dismissible banner in `Workspace.tsx`, not per-task
  - Pure helper + constant in `lib/workspace.ts`; no changes to `useWorkspace.ts` interface (compute in the component)
  - Test in `lib/workspace.test.ts`

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**
- Real Grok Imagine returns data URLs: "returns data URLs so the results are self-contained, persist in localStorage exactly like uploaded refs" (`lib/generate.ts` line 56)
- `string.length` is byte-accurate for base64/ASCII data URLs (ASCII chars = 1 byte per code unit)
- The existing error banner (red, dismissible) at `Workspace.tsx:71-78` is the styling template; add an amber variant for advisory warnings
- `buildApprovedImages` / `buildExportManifest` in `workspace.ts` are the pattern for pure session-level derivation functions

Discovery surfaced no significant deviation → skip 🛠️.

✅ Phase 1 Discovery complete; entering Phase 2 Execution.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — looked at neighboring code for an existing pattern to extend; justified the new shape if none fits

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**
- Pure helper `countGeneratedImageBytes` follows the `buildApprovedImages`/`buildExportManifest` pattern in `lib/workspace.ts` — session-level derivation, no side effects
- `GENERATED_QUOTA_WARN_BYTES = 4 * 1024 * 1024` placed near `MAX_ACTIVE_REFS` for discoverability
- Amber dismissible banner in `Workspace.tsx` follows the existing red error-banner pattern; computed `genBytes` once in component body to avoid duplicate traversal
- Only data URLs (`startsWith('data:')`) counted — remote URLs are negligible

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**
- `npm test`: 62/62 pass (4 new `countGeneratedImageBytes` tests added)
- `npm run lint`: clean
- `npx tsc --noEmit`: clean

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — README.md: no change; AGENTS.md: no change; CLAUDE.md: no change; PLAN.md: updated (line moved to Completed).

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-06-10.` and tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] Recap drafted

**Final Summary:**
Added `GENERATED_QUOTA_WARN_BYTES` (4 MB) + `countGeneratedImageBytes` pure helper to `lib/workspace.ts`, and wired an amber dismissible banner into `Workspace.tsx` that fires when accumulated generated-image data URLs exceed the threshold. Four new tests cover empty sessions, remote URL exclusion, and multi-task accumulation.

Files changed: `lib/workspace.ts` (+18 LOC), `components/Workspace.tsx` (+12 LOC), `lib/workspace.test.ts` (+31 LOC).

**Archived: 2026-06-10**
