---
title: review-sheet-src-escape
status: completed
tags: []
created: 2026-08-09
due:
related-tasks: [BI-EPIC-035, BI-021.4]
---

# BI-035.4 | review-sheet-src-escape

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-035]] [[BI-021.4]]

## 🎯 Goal

Escape the `src` attribute in `buildReviewSheetHtml` so the function's documented guarantee ("all free text is escaped via `escapeHtml`") holds.

## ✅ Acceptance

- [x] `src` in the `<img class="thumb">` template is passed through `escapeHtml` before interpolation
- [x] Existing `buildReviewSheetHtml` tests still pass (data URLs embed correctly; free-text XSS cases unchanged)
- [x] New test covers a `src` value that would break out of the attribute without escaping (e.g. `"` / `>` injection)

## 🧩 Subtasks

- [x] Wrap `src` with `escapeHtml` at `lib/storage.ts` thumb line
- [x] Add a unit test in `lib/storage.test.ts` asserting attribute breakout is neutralized
- [x] Run targeted vitest + tsc on the changed files

## 🔗 Related

- [[BI-EPIC-035]] — parent epic (doc-currency-polish / a11y + doc gaps)
- [[BI-021.4]] — original review-sheet implementation; introduced `escapeHtml` + `buildReviewSheetHtml` but left `src` unescaped (data URLs assumed trusted)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Line 590 still interpolates raw `src` into `src="..."` while the docstring claims all free text is escaped — exact match to the filed gap; pure string fix + one test.

- [x] Read relevant source files

- [x] **Best Practices Review** — Touched responsibility is the pure HTML builder in `lib/storage.ts` §"Review sheet (BI-021.4)". Dependency direction: pure string out, no DOM. Existing abstraction: `escapeHtml` already used for every other free-text field (`alt`, task name, prompt, dates, session name, history). No new helper needed — reuse `escapeHtml` for attribute context (it already escapes `"` → `&quot;`). No duplication or SRP issue. Deferred: nothing.

- [x] **Archive skim** — Prior hits:
  - **BI-021.4** — introduced the section; intentionally escaped free text (prompts/names) but treated `embedded` map values as trusted data URLs from `gatherReviewImages`/`blobToDataUrl`. Pure builder is still public API and accepts arbitrary map values — docstring overclaims.
  - **BI-021.6 / BI-029.\*** — reference the builder; no later change to the thumb `src` path.
  - Sibling epic children BI-035.3 / BI-035.5 — a11y only; no overlap with storage review-sheet.

- [x] **Drift check** — PLAN cites `lib/storage.ts:590`. Current code: line 590 is exactly `? \`<img class="thumb" src="${src}" alt="${escapeHtml(img.taskName)}" />\``. Function name, file, and root cause (unescaped `src`) match. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  **No clarifications needed.** Assumptions:
  1. Reuse existing `escapeHtml` (no separate attribute escaper).
  2. Production path still produces data URLs via `blobToDataUrl` — escaping is defense-in-depth + docstring honesty for the pure public builder.
  3. Scope is the one `src` interpolation only (rating in `title=` is a number; CSS is a module constant).

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

Docstring at 574–581: *"All free text is escaped via `escapeHtml`."* Every free-text field uses it except the `src` attribute value from `embedded.get(img.imageId)`. Fix is one `escapeHtml(src)` wrap + a regression test for `"` breakout. Discovery surfaced no significant deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — Reused existing `escapeHtml` on the one unescaped free-text interpolation; same pattern as every sibling field in the builder. No new shape.

- [x] **Minimal refactor gate** — no refactor; one-call-site wrap only.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- `lib/storage.ts` — `src="${escapeHtml(src)}"` on the thumb `<img>` line. Docstring already correct; code now matches.
- `lib/storage.test.ts` — new case: embedded value `data:,x" onerror="alert(1)` must not appear raw in `src="..."`; appears as `src="data:,x&quot; onerror=&quot;alert(1)"`.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npx vitest run lib/storage.test.ts`: **52/52 pass**

- [x] Ran lint/type-check on changed code — `npx tsc --noEmit` clean; `npx eslint lib/storage.ts lib/storage.test.ts` clean

- [x] **Quality assertions** — no new public surface; no dead code; docstring already claimed the guarantee; no duplication (`escapeHtml` reused).

- [x] (frontend) N/A — pure lib string builder; no UI surface to confirm visually

**Testing Notes:**

Initial test assertion `not.toContain('onerror=')` was wrong: after correct escaping the substring still appears *inside* the attribute value as text. Switched to asserting the raw unescaped form is absent and the entity-encoded form is present.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and kept 2-space nested beneath active parent `BI-EPIC-035`; tasknote moved to `.flowtron/tasknote/archive/bi/BI-035.4.md`

- [x] **Evidence-based recap** drafted

**Final Summary:**

Escaped the review-sheet thumb `src` attribute through the existing `escapeHtml` helper so `buildReviewSheetHtml`'s docstring guarantee holds for every free-text interpolation, including map values that are not production data URLs. One-line production change + one regression test; 52/52 storage tests green; tsc/eslint clean. Doc-facing project docs unchanged (behavior-only fix). Maintainability: pure builder's public contract matches its comment; no new helper.

| AI-referenced doc | Verdict |
|---|---|
| `README.md` | no change |
| `AGENTS.md` | no change |
| `CLAUDE.md` | no change |
| `.flowtron/PLAN.md` | **Updated.** `BI-035.4` flipped to stub form and kept 2-space nested beneath still-active `BI-EPIC-035` in `## Medium` per SPEC/epic.md child placement invariant. |

**Archived:** 2026-08-09
