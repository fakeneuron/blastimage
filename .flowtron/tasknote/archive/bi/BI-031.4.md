---
title: orphan-config-cleanup
status: completed
tags: []
created: 2026-08-08
due:
related-tasks: [BI-EPIC-031]
---

# BI-031.4 | orphan-config-cleanup

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-031]]

## 🎯 Goal

Drop the orphaned `BUILD_TARGET=cloudflare` branch from `next.config.ts` and rewrite `lib/generate.ts`'s BI-013-era error copy as operator-facing text.

## ✅ Acceptance

- [ ] `next.config.ts` no longer branches on `BUILD_TARGET`/references the deleted `docs/DEPLOY.md`; a single unconditional `nextConfig` remains and `next build`/`next dev` behavior is unchanged
- [ ] `lib/generate.ts`'s no-provider `Error` message reads as operator-facing text — no `BI-013`, "live test", "agent must call image_gen", or other internal task/jargon references — while staying actionable (names what's missing and where to look)
- [ ] Tests, lint, and typecheck pass after both edits

## 🧩 Subtasks

- [ ] Rewrite `next.config.ts` to drop the `BUILD_TARGET`/Cloudflare-export branch, leaving a single unconditional `nextConfig` export
- [ ] Rewrite the no-provider throw message in `lib/generate.ts`'s `generateBatch` as operator-facing text
- [ ] Add/adjust a targeted test asserting the new message reads as expected
- [ ] Run targeted tests, lint, typecheck
- [ ] Doc-drift sweep + close

## 🔗 Related

- [[BI-EPIC-031]] — parent epic (generation-mode-clarity)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Both halves are live at HEAD. `next.config.ts:9` still gates on `BUILD_TARGET === "cloudflare"` and cites `docs/DEPLOY.md`, which BI-028 deleted; `grep` confirms no other file (`package.json`, `.github/workflows/ci.yml`, no `wrangler.*`) sets or reads `BUILD_TARGET` — the branch is dead. `lib/generate.ts:115-117`'s no-provider throw still reads "BI-013 live test: agent must call image_gen…"; BI-031.2 (just completed) stopped discarding that message before it reached the UI banner and explicitly named this task as owning the copy rewrite.

- [x] Read relevant source files — `next.config.ts`, `lib/generate.ts`, `lib/useWorkspace.ts` (the `generate()` catch that now surfaces `e.message`), `.github/workflows/ci.yml`, `package.json`, `docs/GROK-AGENT.md` §"The provider bridge"

- [x] **Best Practices Review** — `next.config.ts`: removing the dead branch returns the file to a single unconditional export; no abstraction to preserve, nothing else reads `isCloudflareBuild`. `lib/generate.ts`: only the thrown message string changes — `resolveProvider`/`generateBatch`'s structure (frozen by BI-031.2, landed same day) is untouched. No refactor beyond the two string edits; nothing deferred.

- [x] **Archive skim** — `archive/bi/BI-028.md` (removed the Supabase stack and hosted docs; deleted `docs/DEPLOY.md` per its Acceptance but never touched `next.config.ts`, confirming the orphan). `archive/bi/BI-022.5.md` (Cloudflare deploy — original author of the `BUILD_TARGET` branch; hosted mode it served is gone per BI-028). `archive/bi/BI-031.2.md` (sibling, completed today) — its Discovery Notes explicitly scope this task: *"[[BI-031.4]] owns rewriting `lib/generate.ts`'s BI-013-era message copy ('BI-013 live test: agent must call image_gen…') into operator-facing text — this task does not touch that string, it only stops discarding it."* It also confirms no test pins the fixed sentence text (the assertion was flipped to the thrown `'boom'` value). `archive/bi/BI-013.md` — original author of the throw message, written for an agent-driven live-test run rather than an end user.

- [x] **Drift check** — `next.config.ts:9` (`BUILD_TARGET`), `:1-8` (comment citing `docs/DEPLOY.md`, confirmed absent) match the task description exactly. `lib/generate.ts:115-117` (throw message) matches. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — No clarifications needed. Assumptions: (1) the `BUILD_TARGET` branch is removed outright, not preserved-but-unused, since nothing sets the env var anywhere in the repo; (2) only the throw message string is rewritten in `lib/generate.ts` — the surrounding code comments (BI-007/011/013 history, the "agent must call image_gen" *comment* above the throw) are developer-facing architecture notes, not user-visible error copy, and stay untouched per the surgical-edit convention; (3) the new message may reference `docs/GROK-AGENT.md` by name since that doc is the canonical bridge contract adopters/agents already read.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:** Two independent, unrelated orphans bundled under one PLAN line because both are small mechanical cleanups surfaced by the same BI-EPIC-031 audit. `next.config.ts`'s branch has been fully dead since BI-028 (2026-07-07) deleted the doc it depended on. `lib/generate.ts`'s message was always operator-*reachable* (thrown on every no-provider call) but only became operator-*visible* today when BI-031.2 stopped swallowing it into a fixed sentence — this task is the other half of that same fix, deliberately sequenced after it.

✅ Phase 1 Discovery complete; entering Phase 2 Execution. Discovery surfaced no significant deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `next.config.ts` collapses to the plain-object shape every non-Cloudflare adopter already builds with (`{}` was the fallback branch); no pattern to extend, just dead-branch removal. `lib/generate.ts`'s message keeps the existing `throw new Error(...)` call shape used elsewhere in the same function (the batch-count mismatch throw a few lines above) — only the string content changes.

- [x] **Minimal refactor gate** — no structural refactor; both edits are string/branch removal scoped exactly to Acceptance. The surrounding code comment above the throw (BI-013 live-test driver instructions) and the file-header architecture comment are developer-facing notes, not user-visible error copy, and are left untouched per the surgical-edit convention logged in Discovery.

- [x] Implemented the minimal solution — `next.config.ts` reduced to `const nextConfig: NextConfig = {};`; `lib/generate.ts`'s no-provider throw rewritten to name the missing bridge and point at `docs/GROK-AGENT.md` instead of citing BI-013/agent-driven live-test internals.

- [x] Updated/added tests for non-trivial behavior — added `lib/generate.test.ts` "generateBatch without a provider (BI-031.4)" asserting the new message text (no prior test pinned the old string, confirmed in Discovery).

**Implementation Notes:**

`next.config.ts` — 21 lines → 5. Removed `isCloudflareBuild`, the `BUILD_TARGET`/`docs/DEPLOY.md` comment block, and the conditional `output: 'export'` / `images: { unoptimized: true }` branch (both orphaned since BI-028 deleted the deploy doc and no other file in the repo sets/reads `BUILD_TARGET`).

`lib/generate.ts` — one string literal changed (the no-provider `throw new Error(...)` in `generateBatch`). New text: "Image generation isn't available in this browser. In-app generation requires blastimage to be running inside a Grok Build session with the Grok Imagine provider installed — see docs/GROK-AGENT.md." Names the missing capability and points to the canonical bridge doc instead of an agent/task-internal instruction set. The message now reaches the UI error banner unfiltered (BI-031.2), so this is the first time its wording is truly user-facing.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npx vitest run lib/generate.test.ts lib/useWorkspace.test.ts` 32/32; full `npm test` **186/186** (185 → 186, +1)

- [x] Ran lint/type-check on changed code — `npx tsc --noEmit` clean (exit 0); `npm run lint` clean

- [x] **Quality assertions** — no duplication introduced (one string literal changed in `lib/generate.ts`; `next.config.ts` only shrank). No dead code left behind — the removed `isCloudflareBuild` branch had zero remaining readers (confirmed via repo-wide grep for `BUILD_TARGET` in Discovery). No unexplained complexity; public surface unchanged (no new exports, no signature changes). Documentation: the file-header comment block in `next.config.ts` that explained the Cloudflare branch was removed along with the branch it described (stale docs would otherwise remain); `lib/generate.ts`'s surrounding code comments (architecture history, live-test driver note) are untouched — still accurate, they describe the function's structure, not the removed string.

- [x] (frontend) Asked the user for visual confirmation — **N/A.** The changed `lib/generate.ts` string only reaches the UI through the no-provider error banner, which sits behind the Generate button that BI-031.2 already disables whenever no provider is installed (i.e. in any plain browser, including this session) — reaching it requires bypassing that disabled state, not a normal interaction. Content is verified by the new targeted test instead. `next.config.ts` has no rendered surface at all.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep**
  - `README.md` — **no change.** No mention of `BUILD_TARGET`, Cloudflare, `docs/DEPLOY.md`, or the generation error copy.
  - `AGENTS.md` — **no change.** Flowtron paste-block only; names no build/generation surface.
  - `CLAUDE.md` — **no change.** Doesn't mention `BUILD_TARGET`; the `globalThis.__grokImagineProvider` bridge reference is unchanged (only the thrown message string changed, not the bridge contract).
  - `.flowtron/PLAN.md` — **updated.** BI-031.4 flipped to stub form, kept nested under the active [[BI-EPIC-031]].

- [x] Closed — PLAN.md line flipped to `Completed 2026-08-08.` and kept nested beneath the active parent epic, then tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] **Evidence-based recap** drafted

**Final Summary:**

Removed a fully dead build branch and rewrote an internal-jargon error string as operator-facing text — both surfaced by the BI-EPIC-031 generation-mode-clarity audit. `next.config.ts` no longer branches on `BUILD_TARGET=cloudflare` (orphaned since BI-028 deleted the `docs/DEPLOY.md` it cited; confirmed via repo-wide grep that nothing else sets or reads the env var); it collapses from 21 lines to 5. `lib/generate.ts`'s no-provider throw no longer reads "BI-013 live test: agent must call image_gen…" — it now names the missing Grok Imagine bridge and points at `docs/GROK-AGENT.md`, since BI-031.2 (landed same day) stopped discarding this message before it reached the UI's error banner.

Verification: `npm test` **186/186** (was 185, +1 new test asserting the rewritten message), `npx tsc --noEmit` exit 0, `npm run lint` clean. No refactor beyond the two scoped edits; nothing deferred. Documentation: three AI-referenced docs swept (all "no change" — none named the removed branch or old message), PLAN.md updated. Maintainability: one fewer conditional branch with zero live callers, and the one remaining no-provider error message an end user can actually act on instead of reading an agent-internal instruction set.

**Archived:** 2026-08-08
