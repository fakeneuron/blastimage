---
title: grok-agent-server-surface
status: completed
tags: []
created: 2026-09-09
due:
related-tasks:
  - BI-EPIC-052
touches:
  - docs/GROK-AGENT.md
---

# BI-052.2 | grok-agent-server-surface

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-052]]

## 🎯 Goal

Rewrite `docs/GROK-AGENT.md` so the in-app Grok Imagine provider contract sits next to the localhost imagegen API the rest of the app uses, instead of claiming there is no backend, no server, and no API proxy.

## ✅ Acceptance

- [x] `docs/GROK-AGENT.md` App context no longer claims “no backend, no server, no API proxy” (or “runs entirely in the browser” as a totalising statement)
- [x] App context names the in-app `__grokImagineProvider` contract **and** the localhost `app/api/imagegen/` surface as siblings; the provider section remains the SSOT for the bridge
- [x] Cross-link to `docs/REVIEW-LOOP.md` for the imagegen API rather than restating the route table
- [x] Stale `useWorkspace.generate()` line citations (`~239` / `~232`) updated to current HEAD
- [x] No edits to `docs/ADOPT.md`, `lib/generate.ts`, or CLAUDE.md (siblings [[BI-052.3]] / [[BI-052.5]])

## 🧩 Subtasks

- [x] Rewrite App context so the provider contract sits next to the localhost imagegen API
- [x] Update the two `generate()` line citations in the same file
- [x] Grep the file for leftover “no backend” / “no server” / “no API proxy” / totalising “entirely in the browser”
- [x] Sanity-read against CLAUDE.md Stack + REVIEW-LOOP.md §4 for contradiction

## 🔗 Related

- [[BI-EPIC-052]] — parent epic: post-server-doc-currency
- [[BI-052.3]] — sibling: ADOPT.md §4 “entirely frontend — no backend” (leave that file)
- [[BI-052.5]] — sibling: generate.ts / CLAUDE.md comment fossils (leave those files)
- [[BI-045]] — authored `app/api/imagegen/`; Phase 4 left GROK-AGENT.md unchanged on purpose
- [[BI-016]] — last full GROK-AGENT.md refresh (post-BI-013); established the line-citation currency pattern
- [[BI-031.3]] — established GROK-AGENT.md as the in-app bridge SSOT; cross-link, don’t duplicate

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** The quoted claim is still live at `docs/GROK-AGENT.md:14–15`; the localhost imagegen API has shipped (BI-045/046/047) and CLAUDE.md / REVIEW-LOOP.md already describe it. This file is the last agent-facing runbook still denying a server exists.

- [x] Read relevant source files — `docs/GROK-AGENT.md`, `docs/REVIEW-LOOP.md` §1–4, `docs/ADOPT.md` §4–5 (sibling, not this edit), CLAUDE.md Project/Stack, `lib/generate.ts` (bridge contract still matches the doc), `lib/useWorkspace.ts` `generate()` (~711–736)

- [x] **Best Practices Review** — N/A for code module boundaries (docs-only). Doc-division already set by [[BI-031.3]] / [[BI-025]]: GROK-AGENT.md is the in-app bridge SSOT; REVIEW-LOOP.md owns the imagegen file loop. Cross-link, don’t restate the route table. ADOPT.md §4 is [[BI-052.3]].

- [x] **Archive skim** — `grep -l GROK-AGENT` over `archive/bi/`. Load-bearing: [[BI-011]] authored the file; [[BI-016]] last full refresh (also the line-citation currency pattern); [[BI-031.2]] added the UI-gate note; [[BI-045]]/[[BI-046]]/[[BI-047]] Phase 4 each recorded GROK-AGENT.md as “names the button but never the mechanism, **no change**” — this task is that deferred rewrite. No `.1` Discovery sibling (audit-repo supplied Discovery); no Fan-out YAML to echo.

- [x] **Drift check** — PLAN-cited path and quote match HEAD (`docs/GROK-AGENT.md:14–15`). Same-file line citations are stale: doc says `generate()` ~239 / ~232; HEAD is `lib/useWorkspace.ts:696` (`generate`), resolution ~711–729, `generateBatch` call ~731. Included as same-file currency (BI-016 precedent), not a PLAN rewrite. SPEC: no contract touched. PLAN line unchanged.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

The false claim is one sentence in App context. Auth/execution (“generation call runs client-side inside the Grok Build sandbox”) is still true for *generation* and stays. “All state is persisted to localStorage” is half-true: Session is; round bytes in the viewer loop are `imagegen:` URLs via `/api/imagegen/file`. The rewrite nuances that without turning this doc into REVIEW-LOOP.

No clarifications needed. Assumptions: (1) this file only — ADOPT / generate.ts comments / CLAUDE.md line-count are siblings; (2) App context rewrite + same-file line-citation fix; provider interface, checklist, and FS-path recipe stay; (3) name `app/api/imagegen/` and point at REVIEW-LOOP.md §4 rather than listing routes; (4) Next dev server is required for those routes (matches sibling .3’s intended ADOPT wording, stated here so an in-app agent is not surprised).

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

Pattern: same App-context voice as the rest of GROK-AGENT.md; sibling-surface named in three short paragraphs; REVIEW-LOOP.md §4 cross-link instead of a route table (BI-025 / BI-031.3). Provider interface, checklist, and FS-path recipe untouched.

Minimal refactor: same-file `generate()` line citations `~239`/`~232` → `~731`/`~711` (BI-016 currency pattern). No other files.

Tests: N/A — docs-only.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

Docs-only. Targeted tests N/A; tsc/lint N/A. Quality: grep of `docs/GROK-AGENT.md` for “no backend” / “no server” / “no API proxy” / “entirely in the browser” is empty; App context now names both surfaces; provider section unedited. Visual confirmation N/A — no UI.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Rewrote `docs/GROK-AGENT.md` App context so the in-app `__grokImagineProvider` contract sits next to the localhost `app/api/imagegen/` API instead of claiming there is no backend, no server, and no API proxy. Same-file `generate()` line citations updated (`~239`/`~232` → `~731`/`~711`). Provider interface, checklist, and FS-path recipe unchanged.

**Changed** — `docs/GROK-AGENT.md` App context (+12/−3) and two line citations. No code.

**Verified** — grep of leftover totalising claims: empty. Sanity-read vs CLAUDE.md Stack (already names `app/api/imagegen/`) and REVIEW-LOOP.md §4 (the cross-link target). Tests/lint N/A.

**Refactors** — line-citation currency only. Deferred: ADOPT.md §4 ([[BI-052.3]]), generate.ts/CLAUDE.md fossils ([[BI-052.5]]), adding this file to the Phase-4 walk list ([[BI-052.4]]).

**Documentation verdict.** `README.md` — no change. `AGENTS.md` — no change. `CLAUDE.md` — no change (already accurate; fossils are .5). `.flowtron/PLAN.md` — this child stubbed, kept nested under BI-EPIC-052. Off-list: `docs/GROK-AGENT.md` is the deliverable.

**Maintainability.** An in-app Grok Build agent landing on this doc no longer plans as if Next has no server surface; the bridge contract and the imagegen file API are named as siblings with a pointer to the SSOT for the latter.

**Archived:** 2026-09-09
