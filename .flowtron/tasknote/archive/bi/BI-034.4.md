---
title: dependabot-config
status: completed
tags: []
created: 2026-08-08
due:
related-tasks: [BI-EPIC-034]
---

# BI-034.4 | dependabot-config

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-034]]

## 🎯 Goal

Add `.github/dependabot.yml` for the npm ecosystem, weekly cadence, with patch/minor updates grouped.

## ⚡ Notes

**Relevance:** Proceed — the PLAN line's premise holds exactly. `.github/dependabot.yml` does not exist yet; the repo is a single-manifest npm project (`package.json` at root), so a one-ecosystem config is all that's needed.
**Best Practices Review:** N/A — a declarative GitHub-native config file, no code abstraction or dependency-direction concern applies.
**Drift check:** No drift. `.github/` contains only `workflows/ci.yml`; no `dependabot.yml` present before this change.
**Archive skim:** `grep -rl` over `archive/*/*.md` for `dependabot`/`.github` returns `BI-022.6.md` (unrelated — GitHub OAuth provider config in `supabase/config.toml`, incidental filename match) and `BI-031.4.md` (read `.github/workflows/ci.yml` only, no writes) and `BI-034.3.md` (added the `Audit` step to `ci.yml`). No prior tasknote has created or touched a Dependabot config — no precedent constrains shape.
**Pattern survey:** No existing `dependabot.yml` to extend; used GitHub's standard `version: 2` schema — single `npm` ecosystem entry, root `directory`, `weekly` schedule, one `groups` entry bundling `patch`+`minor` update-types (matches the PLAN line's "grouped patch/minor" spec verbatim).
**Implementation:** Added `.github/dependabot.yml`: `package-ecosystem: npm`, `directory: /`, `schedule.interval: weekly`, `groups.patch-and-minor.update-types: [patch, minor]`. Majors are left ungrouped (GitHub's default — Dependabot opens them as individual PRs) so a breaking bump like `next@16` still surfaces distinctly rather than being buried inside a grouped PR.
**Docs touched:** `README.md` — no change (no CI/dependency-tooling section exists). `AGENTS.md` — no change. `CLAUDE.md` — no change. `.flowtron/PLAN.md` — flipped this task's line at closure.

## ✅ Recap

Added `.github/dependabot.yml`: single `npm` ecosystem entry, root directory, weekly schedule, one group (`patch-and-minor`) bundling `patch`+`minor` update-types per the PLAN line's spec. Majors stay ungrouped (Dependabot's default individual-PR behavior), so a future major bump (e.g. `next@16`, which BI-034.2/`.3` already track as a known upstream event) surfaces on its own rather than riding inside a grouped patch/minor PR. One new file, no code touched; YAML validated by parsing with `js-yaml`.

No prior tasknote had touched Dependabot config, so there was no existing shape to extend — the file follows GitHub's standard `version: 2` schema directly. Nothing deferred; nothing an `/ft-audit` pass needs to flag beyond the standard epic doc-drift sweep.

**Archived:** 2026-08-08
