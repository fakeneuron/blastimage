---
title: adopter-repo-layout
status: in-progress
tags: [docs]
created: 2026-06-11
due:
related-tasks: [BI-014, BI-018, BI-019]
---

# BI-020 | adopter-repo-layout

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Add a canonical adopter-repo layout convention to `docs/ADOPT.md` — an `imagegen/` root holding the task-import JSON, prompt sources, reference images, and the export landing spot — so adopter repos structure prompts and sample images for easy consumption (and future tooling can rely on the paths).

## ⚡ Notes

**Relevance:** Proceed — ADOPT.md §6 documents the import JSON contract but says nothing about where it (or prompt sources / ref images / exported output) should live in the adopter repo; spinalcord (first adopter) had to invent an organic layout (`candidates/<task>/prompt.txt` + `candidates/blastimage-tasks.json`). Operator chose: extend ADOPT.md, **canonical** convention, full scope (prompts + import JSON, refs, export landing), `imagegen/` flat layout (decided via AskUserQuestion this session).
**Drift check:** §6 import contract confirmed current at `docs/ADOPT.md:69-91`. Export filenames verified against code: per-image download `<task-slug>-<imageId8>.<ext>` (`components/GalleryPanel.tsx:91`), manifest download `<session-slug>-export.json` (`lib/useWorkspace.ts:384`). 2 MB ref cap (`MAX_REF_BYTES`, `ReferenceLibrary.tsx`) matches USAGE.md. No drift.
**Archive skim:** BI-014 authored ADOPT.md (install-guide structure: prereqs → numbered steps → Notes → Troubleshooting; plain README voice). BI-018 authored USAGE.md (craft layer: 2 MB ref cap, descriptive ref names — repo guidance must cross-reference, not duplicate). BI-019 added §6 import contract (`{version: 1, tasks: [{name, basePrompt}]}`; append semantics; refs attached in-app afterwards) and renumbered later sections.
**Pattern survey:** Extended ADOPT.md's existing numbered-section install-guide shape — new §7 inserted after §6 (import contract) and before the renumbered §8 (keep up to date), matching the doc's plain, direct voice and its ASCII-tree/bold-term conventions. No new doc shape.
**Implementation:** New ADOPT.md §7 "Structure your repo for blastimage": canonical `imagegen/` root with `tasks.json` (import file), `prompts/<task-name>.txt` (editable source of truth; tasks.json is the generated artifact), `refs/` (staged uploads, 2 MB cap, kebab naming → USAGE.md cross-refs), `approved/` (landing spot: rename `<session-slug>-export.json` → `manifest.json`, move `<task-slug>-<id>.png` downloads in as-is; asset-pipeline copy is a separate project-owned step). Framed as convention-not-requirement (blastimage never reads the parent repo) but canonical — agents/future tooling will assume these paths. Cross-link added at the end of §6; old §7 renumbered to §8. Key decisions (operator, via AskUserQuestion): extend ADOPT.md (not a new doc); canonical convention (not soft recommendation); flat `imagegen/` layout (not spinalcord's per-task `candidates/` shape — spinalcord migrates later, out of scope here). Grepped for stale `ADOPT.md §` references: none outside this tasknote.
**Docs touched:** `README.md` — no change (Docs section already lists ADOPT.md). `AGENTS.md` — no change. `CLAUDE.md` — no change. `.flowtron/PLAN.md` — BI-020 flipped to Completed stub (this closure).

## ✅ Recap

ADOPT.md gains §7 "Structure your repo for blastimage" — the canonical `imagegen/` adopter-repo convention (`tasks.json` import file · `prompts/<task-name>.txt` sources · `refs/` staged reference images · `approved/` export landing with `manifest.json`), linked from §6, with USAGE.md cross-refs for craft details and code-verified download filenames. Old §7 renumbered to §8. Docs-only; tsc + eslint clean. First migration candidate: spinalcord's `candidates/` layout (deliberately out of scope here).

**Archived:** 2026-06-11
