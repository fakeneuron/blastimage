---
title: dependency-vuln-gate audit
status: completed
tags: []
created: 2026-08-09
due:
related-tasks: [BI-EPIC-034, BI-034.2, BI-034.3, BI-034.4, BI-034.5]
---

# BI-034.N | dependency-vuln-gate audit

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-034]]

## 🎯 Goal

Verify the completed `BI-EPIC-034` (`dependency-vuln-gate`) cohort sits coherently in the codebase: cumulative doc-drift sweep across `.flowtron/tasknote/README.md` §"AI-referenced docs", naming/style consistency across the cohort's deliverables, and follow-up filings for any miss.

## ✅ Acceptance

- [x] **Doc-drift sweep (fixed line, per SPEC/epic.md §"Audit acceptance — fixed doc-drift line")** — walked all four entries; see Phase 4. One update (`README.md`), three "no change".
- [x] Cohort coherence inventory: each implementation child's deliverables read against the others — all four verified at HEAD, style parity holds, `.2 → .3` handoff chain resolved; one cross-child coupling gap found (F1) and fixed.
- [x] No regressions surfaced in earlier-shipped cohort children's surfaces — audit gate exits `0`; typecheck / lint / 213 tests green; deposits still byte-identical to natabula; hook still armed.
- [x] Audit findings recorded in Implementation Notes — F1 and F3 fixed inline; F2 cited as a `/ft-file-followup` candidate; one won't-fix observation recorded.
- [x] Single `feat: BI-034.N — audit BI-EPIC-034` commit lands
- [x] PLAN.md line for `BI-034.N` flipped to stub form `Completed 2026-08-09.` (bare `| audit` placeholder replaced with `| dependency-vuln-gate audit` per skill Step 7)
- [x] Tasknote moved to `.flowtron/tasknote/archive/bi/BI-034.N.md`
- [x] Parent-flip prompt surfaced after audit closure (skill Step 8) — bundled into the 📦 ready-to-commit gate

## 🧩 Subtasks

- [ ] Inventory cohort children's archived tasknotes — read each implementation child's Final Summary + Implementation Notes; capture deliverables in Discovery Notes
- [ ] Walk `.flowtron/tasknote/README.md` §"AI-referenced docs" entries — fixed doc-drift sweep
- [ ] Cohort coherence pass — naming consistency, style parity, no contradictory cross-refs across the cohort's deliverables
- [ ] Surface audit findings in Implementation Notes; cite each miss as a `/ft-file-followup <NEW-ID>` candidate
- [ ] Phase 4: flip `BI-034.N` PLAN line to stub form + archive tasknote
- [ ] Parent-flip: skill Step 8 prompts user; on confirm, atomic flip parent line + move cohort to `## Completed`

## 🔗 Related

- [[BI-EPIC-034]] — parent epic (dependency-vuln-gate)
- [[BI-034.2]] — relax-next-pin
- [[BI-034.3]] — ci-audit-step
- [[BI-034.4]] — dependabot-config
- [[BI-034.5]] — secret-scan-decision

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** The operator invoked `/ft-close-epic BI-034.N` and pre-flight passed cleanly — working tree clean, `BI-EPIC-034` active under `## High`, all four implementation children (`.2`–`.5`) checked and archived, `.N` open, no early-audit compromise. The cohort is complete, so the audit covers the whole epic rather than a partial slice.

- [x] Read relevant source files

- [x] **Best Practices Review** — N/A for the survey itself (verification pass, no new abstraction). For the two inline fixes: both are comment/prose additions to existing cohort deliverables, no dependency direction or module boundary involved.

- [x] **Archive skim** — self-referential for this cohort (the four children are themselves archive entries, read in full). No non-cohort archive entry touches `package.json` resolution behavior, `ci.yml`, Dependabot, or secret scanning beyond the hits each child already logged (`CORE-001.4` scripts-only, `BI-031.4` read-only on `ci.yml`).

- [x] **Drift check** — every path cited by the cohort still matches HEAD: `package.json` (`next`/`eslint-config-next` at `^15.5.23`, `overrides` for `postcss`/`sharp`), `.github/workflows/ci.yml` (Audit step last), `.github/dependabot.yml`, `.gitleaks.toml` + `.pre-commit-config.yaml` at root, README arming block. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" — **No clarifications needed.** Assumption: audit scope is the full `.2`–`.5` cohort plus the epic's shared surfaces (`package.json`, `.github/`, root deposits, AI-referenced docs).

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Cohort inventory** (each child's deliverable, from its archived tasknote):

- **BI-034.2 — relax-next-pin.** `package.json:15` `next` and `:28` `eslint-config-next` moved from exact `15.5.19` to `^15.5.23` (bumped in lockstep, flagged as a deliberate widening beyond the PLAN wording). Corrected the epic's filing premise: the bump cleared Next's own 8 advisories but left 4 flagged via Next 15's bundled `postcss`/`sharp`, and explicitly handed that residue to `.3`.
- **BI-034.3 — ci-audit-step.** `package.json` gained `overrides` (`postcss: ^8.5.26`, `sharp: ^0.35.3`) — two entries clear all four residual advisories, since `postcss@8.5.26` lifts `nanoid` transitively and `next` was flagged only by association. `.github/workflows/ci.yml` gained an `Audit` step (`npm audit --omit=dev --audit-level=high`) placed **last, after Build**, with a three-line rationale comment. Records that the `postcss` override deliberately overrides Next's exact `8.4.31` pin.
- **BI-034.4 — dependabot-config.** New `.github/dependabot.yml`: `version: 2`, single `npm` ecosystem, root directory, weekly, one `patch-and-minor` group. Majors left ungrouped so a `next@16` bump surfaces on its own.
- **BI-034.5 — secret-scan-decision.** `.gitleaks.toml` + `.pre-commit-config.yaml` byte-copied from natabula HEAD `6af815d`, hook armed, README gained a five-line arming block (arming is per-clone, and the deposit's failure mode is silent). CI secret-scan step deliberately declined as out of the PLAN line's scope, with the `.next/`-hits trap recorded.

**Cohort style parity.** All four children used the micro-tasknote shape (`## ⚡ Notes` / `## ✅ Recap`) consistently — no format drift within the cohort. Each ran the same evidence pattern (relevance → drift → archive skim → pattern survey → verification → docs-touched), and each recorded its handoffs to the next child explicitly. `.2 → .3` is the strongest chain and it held.

**Handoff chain verified.** `.2` predicted the CI gate would fail on day one; `.3` resolved it with overrides rather than a weakened threshold; the gate now exits `0`. No child's premise was left stranded.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — both inline fixes extend shapes the cohort itself established: the `ci.yml` addendum continues the existing three-line rationale comment `.3` wrote directly above the `Audit` step, and the README paragraph sits alongside the secret-scan block `.5` added under "Install & Run", in the same terse declarative voice.

- [x] **Minimal refactor gate** — no refactor. Two comment/prose additions only; nothing restructured, nothing deleted.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — N/A. No behavior changed; the edits are a YAML comment and a README paragraph.

**Implementation Notes:**

**Verified at HEAD** — every cohort deliverable is present and matches its tasknote description:

| Deliverable | State at HEAD | Verdict |
|---|---|---|
| `package.json` pins | `next: ^15.5.23`, `eslint-config-next: ^15.5.23` | ✅ matches `.2` |
| `package.json` overrides | `postcss: ^8.5.26`, `sharp: ^0.35.3` | ✅ matches `.3` |
| Resolved tree | `next@15.5.23` → `postcss@8.5.26` → `nanoid@3.3.18`; `sharp@0.35.3` | ✅ overrides effective |
| `ci.yml` Audit step | last, after Build, `--omit=dev --audit-level=high` | ✅ matches `.3` |
| `.github/dependabot.yml` | npm / `/` / weekly / `patch-and-minor` group | ✅ matches `.4`, parses |
| `.gitleaks.toml`, `.pre-commit-config.yaml` | `diff -q` vs natabula HEAD → identical | ✅ unforked, refreshable |
| `.git/hooks/pre-commit` | present, executable, pre-commit-generated | ✅ armed |
| README arming block | present under "Install & Run" | ✅ matches `.5` |

**No regressions in cohort surfaces.** `npm audit --omit=dev --audit-level=high` → **0 vulnerabilities, exit 0** (the gate `.3` installed still passes, and the floor `.2` documented is still resolved rather than merely tolerated). `npm run typecheck` ✅ · `npm run lint` ✅ · `npm test` → **213 passed / 15 files** ✅. `.gitleaks.toml` allowlist paths still resolve (`.env.example` present, `.flowtron/tasknote/archive/` present).

**Findings.**

**F1 — cross-child coupling recorded only in the archive (fixed inline).** `.3` established that the `overrides` block must be **deleted** once the tree reaches Next 16 (which fixes `postcss`/`sharp` upstream), and `.4` established that Dependabot raises majors as individual PRs — so `next@16` will arrive as its own PR. Nothing on a live surface connected the two: `ci.yml`'s comment explained *why* the overrides exist but not *when to remove them*, and the instruction lived only inside an archived tasknote that no cold-start session reads. Whoever merges the `next@16` PR would leave the repo pinned to floors it no longer needs. Fixed by extending `.3`'s existing comment in `.github/workflows/ci.yml` by two lines, naming both the trigger and the originating task.

**F2 — Dependabot covers npm only (follow-up candidate).** `.github/dependabot.yml` declares one ecosystem. `.github/workflows/ci.yml` pins `actions/checkout@v4` and `actions/setup-node@v4`, which stay frozen and unmonitored — the exact condition BI-EPIC-034's theme ("Dependencies are frozen, not managed") set out to end. `.4` executed its PLAN line faithfully (the line named npm), so this is a cohort-level gap, not a child-level miss. Adding a `github-actions` ecosystem entry is a four-line change, but it raises PR volume, so it is the operator's call rather than an inline fix. → `/ft-file-followup` candidate.

**F3 — cumulative doc gap on the epic's own surface (fixed inline).** Each child individually justified "README: no change" because the repo has no CI or tooling section — individually correct, cumulatively wrong. By epic close, the repo had gained a CI audit gate, a Dependabot config, and two secret-scan deposits, and README documented exactly one of them. This is the slice-local staleness the fixed doc-drift line exists to surface. Fixed inline: a three-line paragraph in `README.md` under "Install & Run", beside `.5`'s arming block, naming the CI gate and Dependabot's grouping behavior.

**Observation — won't fix.** `.gitleaks.toml`'s header lists "the CI secret-scan step (`.github/workflows/ci.yml`, working tree)" among its consumers, which blastimage does not have (`.5` declined it deliberately, recording the `.next/` false-positive trap). The line is inaccurate *here*, but the file is a byte-identical fleet deposit and editing it would fork it out of `/natabula-layer-refresh`'s reach — precisely what `.5` avoided. Correctly left alone; `.5`'s recap already carries the context.

**Inline fixes applied.**

- `.github/workflows/ci.yml` (+2 lines, in the existing Audit comment) — "Drop those overrides when the tree moves to Next 16, which fixes them upstream — Dependabot surfaces that major as its own PR (BI-034.4)." Re-parsed as valid YAML after the edit.
- `README.md` (+4 lines, after the secret-scan arming block) — one paragraph naming the CI audit gate and Dependabot's weekly cadence / grouping split.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — the two edits are a YAML comment and README prose, so no test targets them. Full suite run anyway as a cohort regression check: **213 passed / 15 files** ✅.

- [x] Ran lint/type-check on changed code — `npm run typecheck` ✅ · `npm run lint` ✅ (neither reaches `.yml`/`.md`, run to confirm the cohort's surfaces are still clean). `.github/workflows/ci.yml` re-validated as parseable YAML after the comment edit. The cohort's own gate, `npm audit --omit=dev --audit-level=high`, exits `0`.

- [x] **Quality assertions** — no duplication, dead code, or public-surface growth; both edits are documentation that closes stale code-facing gaps rather than adding any.

- [x] (frontend) Asked the user for visual confirmation — N/A. No UI surface touched; the cohort is dependency/CI configuration only.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — walked every entry in `.flowtron/tasknote/README.md` §"AI-referenced docs":

  - `README.md` — **updated.** Gained a three-line dependency-tooling paragraph under "Install & Run" (finding F3). Each child correctly declared "no change" in isolation; cumulatively the repo had gained a CI audit gate, a Dependabot config, and two secret-scan deposits while README documented only the last. Everything else in the file stays accurate — the feature surface is untouched by this epic.
  - `AGENTS.md` — **no change.** Flowtron workflow prose and the paste-block only; it documents task lifecycle, not dependency policy or local tooling. Nothing the cohort shipped belongs in it.
  - `CLAUDE.md` — **no change.** "Next.js 15 (App Router, TypeScript)" remains accurate at `15.5.23`, and the `overrides` block moves a transitive pin rather than the framework. The Stack section covers framework / state / target API / dev port / testing; a CI gate and a commit hook are none of those, and README now carries both.
  - `.flowtron/PLAN.md` — **updated.** `BI-034.N` flipped to stub form with the `| audit` placeholder replaced by `| dependency-vuln-gate audit`; parent `BI-EPIC-034` flip + cohort move applied per the operator's answer at the 📦 gate.

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-08-09.`, kept nested beneath `BI-EPIC-034`, then tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] **Evidence-based recap** drafted — surfaces at the 📦 ready-to-commit gate

**Final Summary:**

Audited the complete `BI-EPIC-034` cohort (`.2` relax-next-pin, `.3` ci-audit-step, `.4` dependabot-config, `.5` secret-scan-decision). All four deliverables are present at HEAD and match their tasknotes; the epic's gate holds — `npm audit --omit=dev --audit-level=high` exits `0`, down from the 4 prod highs that opened the epic — and typecheck / lint / 213 tests are green. Style parity across the four micro-tasknotes is consistent, and the `.2 → .3` handoff (the epic's one real design dependency, where `.2` predicted a day-one CI failure and `.3` resolved it with `overrides` instead of a weakened threshold) landed cleanly.

Two gaps surfaced and were fixed inline, both cheap and both documentation rather than behavior. **F1:** `.3` established that the `overrides` block must be deleted once the tree reaches Next 16, and `.4` established that Dependabot raises majors as individual PRs — so the removal trigger will arrive as a PR, but the instruction to act on it lived only in an archived tasknote no cold-start session reads. Two lines added to `.3`'s existing comment in `ci.yml` connect them. **F2:** each child's individually-correct "README: no change" summed to a cumulative miss — three of four tooling additions were undocumented on the repo's own front page. Three lines added beside `.5`'s arming block. This is exactly the staleness class the fixed doc-drift line exists to catch, and it would not have been visible from inside any single child.

One finding left for the operator: `.github/dependabot.yml` declares the npm ecosystem only, while `ci.yml` pins `actions/checkout@v4` and `actions/setup-node@v4` — still frozen, still unmonitored, which is the condition the epic's theme set out to end. `.4` executed its PLAN line faithfully (the line named npm), so this is a cohort-level gap rather than a child-level miss, and the fix raises PR volume, so it is a filing decision rather than an inline one. Cited as a `/ft-file-followup` candidate.

One won't-fix recorded: `.gitleaks.toml`'s header claims a CI secret-scan consumer blastimage doesn't have. The line is inaccurate here but the file is a byte-identical fleet deposit, and editing it would fork it out of `/natabula-layer-refresh`'s reach — the exact outcome `.5` designed around. Left alone deliberately.

Changed: `.github/workflows/ci.yml` (+2), `README.md` (+4). No source touched, no behavior changed.

Maintainability effect: the epic's substantive work was already sound; the audit's contribution is that its two conditional handoffs — remove the overrides at Next 16, and the tooling the repo now runs — moved from archived tasknotes onto surfaces a future session actually reads.

**Archived:** 2026-08-09
