---
title: generate-comment-drift
status: completed
tags: []
created: 2026-08-08
due:
related-tasks: [BI-EPIC-031]
---

# BI-031.5 | generate-comment-drift

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[BI-EPIC-031]]

## 🎯 Goal

Rewrite the stale BI-013 live-test comments in `lib/generate.ts` so they narrate the present generation mode instead of the one-off historical test.

## ✅ Acceptance

- [ ] `lib/generate.ts`'s four F1-cited comment blocks (`GrokImagineProvider` JSDoc, `declare global` bridge comment, `generateBatch` JSDoc, no-provider block comment) no longer narrate the BI-013 one-off live test as the present operating mode, and no longer name `/ft-task`/an agent-driven-test artifact as the runtime actor
- [ ] No behavior change — comment text only; `generateBatch`/`resolveProvider`/`isGenerationAvailable` logic and the operator-facing throw message (BI-031.4) are untouched
- [ ] Tests, lint, and typecheck still pass

## 🧩 Subtasks

- [ ] Rewrite the `GrokImagineProvider` JSDoc (`:56-59`) — drop "For this live test"
- [ ] Rewrite the `declare global` bridge comment (`:64-66`) — drop "during BI-013 live test (and future ...)"
- [ ] Rewrite the `generateBatch` JSDoc (`:91-93`) — drop "during a pure-client test run"
- [ ] Rewrite the no-provider block comment (`:110-114`) — drop "this agent-driven live test" / "the /ft-task driver"
- [ ] Run targeted tests, lint, typecheck
- [ ] Doc-drift sweep + close

## 🔗 Related

- [[BI-EPIC-031]] — parent epic (generation-mode-clarity)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `lib/generate.ts` still carries all four cited comment blocks verbatim at HEAD; the finding that filed this task (BI-031.N's F1) is live and unaddressed.

- [x] Read relevant source files — `lib/generate.ts` in full

- [x] **Best Practices Review** — `N/A`, comment-only edit; no logic, responsibility, or dependency-direction change. Nothing to refactor or defer.

- [x] **Archive skim** — `archive/bi/BI-031.N.md` (the audit that filed this task) is the direct source: its F1 finding names these exact four spots and explicitly notes BI-031.4 "consciously scoped these out (its assumption 2: developer-facing architecture notes, surgical-edit convention) — a defensible per-task call" but left mode-confusion residue. `archive/bi/BI-031.4.md`'s own Discovery Notes confirm assumption 2 (surrounding architecture comments, including the BI-013 live-test driver note, were deliberately left untouched as "developer-facing", not user-visible error copy) and that it only rewrote the thrown `Error` message string — the comment block *above* the throw was out of its scope, which is exactly what this task now closes. `archive/bi/BI-013.md` is the original author of the live-test framing in all four spots.

- [x] **Drift check** — task description cites `:57`, `:64`, `:92`, `:110-114`; current file has the four blocks at `:56-59`, `:64-66`, `:91-93`, `:110-114` respectively (single-line citations in the PLAN description land inside the same JSDoc block the fuller BI-031.N F1 citation gives) — content matches exactly, no drift. The file-header comment (`:1-18`) and `isGenerationAvailable`'s JSDoc (`:76-85`) also mention BI-013/BI-031.2 but in historical/citation form ("BI-013 installs it before triggering generate"), not present-tense live-test narration — F1 doesn't cite them and they stay out of scope.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — No clarifications needed. Assumptions: (1) scope is exactly the four F1-cited blocks — the file-header history comment and `isGenerationAvailable`'s JSDoc are already historical-citation form and stay untouched, matching BI-031.4's surgical-edit precedent; (2) the no-provider block comment (`:110-114`) is rewritten to describe the standing operator-facing behavior (bridge absent → throw, per `docs/GROK-AGENT.md`) rather than an agent-driven-test procedure, mirroring BI-031.4's already-rewritten throw message one paragraph below it; (3) purely comment text changes — no test changes needed since no behavior changes, but the existing suite is re-run to confirm.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

This task is the direct, single-purpose output of BI-031.N's F1 finding — Discovery only confirms the finding still holds at HEAD (it does, verbatim) and delimits the exact rewrite scope. No new information changes the task's shape.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `N/A`, comment-only edit; no code shape to extend or justify. The rewritten comments follow the tone/voice BI-031.4 already set for the operator-facing throw message one paragraph below the last block (name the missing bridge, point at `docs/GROK-AGENT.md`).

- [x] **Minimal refactor gate** — no refactor; four comment blocks rewritten, nothing else touched.

- [x] Implemented the minimal solution — rewrote the `GrokImagineProvider` JSDoc, `declare global` bridge comment, `generateBatch` JSDoc, and the no-provider block comment in `lib/generate.ts` to describe the standing Grok-Build-session operating mode instead of the BI-013 one-off live test, and dropped the "/ft-task driver" workflow-artifact reference.

- [x] Updated/added tests for non-trivial behavior — `N/A`, comment-only change, no behavior to test. Existing suite re-run to confirm no regression.

**Implementation Notes:**

Four blocks rewritten in `lib/generate.ts`, all comment-text-only, no line outside them touched: `:56-59` (`GrokImagineProvider` JSDoc) drops "For this live test"; `:64-66` (`declare global` bridge comment) drops "during BI-013 live test (and future ...)"; `:91-93` (`generateBatch` JSDoc) drops "during a pure-client test run"; `:110-112` (no-provider block comment, was `:110-114`, now 3 lines) drops "this agent-driven live test" and "the /ft-task driver", replaced with a two-line pointer to the actual runtime condition (no Grok Build session/bridge) and `docs/GROK-AGENT.md`, mirroring the throw message immediately below it. The file-header history comment (`:1-18`) and `isGenerationAvailable`'s JSDoc (`:76-85`) were left untouched per Discovery assumption 1 — already historical-citation form, not present-tense live-test narration, and outside F1's scope.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npx vitest run lib/generate.test.ts` 11/11 passed

- [x] Ran lint/type-check on changed code — `npx tsc --noEmit` exit 0; `npm run lint` clean

- [x] **Quality assertions** — no duplication, dead code, or complexity introduced (four comment blocks rewritten, no code logic touched). No public-surface change (no export/signature edits). Documentation: this task's entire purpose was fixing stale code-facing documentation — the four F1-cited blocks now describe the standing operating mode instead of a one-off historical test.

- [x] (frontend) Asked the user for visual confirmation — **N/A.** Comment-only change in a non-exported-string source file; nothing rendered or user-visible changed.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep**
  - `README.md` — **no change.** No mention of `lib/generate.ts`'s internal comments or the BI-013 live-test framing.
  - `AGENTS.md` — **no change.** Flowtron paste-block only; names no generation-module internals.
  - `CLAUDE.md` — **no change.** Doesn't cite `lib/generate.ts`'s comment text; the `globalThis.__grokImagineProvider` bridge reference and two-mode paragraph are unaffected (comment-only edit, bridge contract unchanged).
  - `.flowtron/PLAN.md` — **updated.** BI-031.5 flipped to stub form, kept nested under the active [[BI-EPIC-031]].

- [x] Closed — PLAN.md line flipped to `Completed 2026-08-08.` and kept nested beneath the active parent epic (all five children now checked; parent flip is `/ft-close-epic`'s job, not this task's), then tasknote moved to `.flowtron/tasknote/archive/bi/`

- [x] **Evidence-based recap** drafted

**Final Summary:**

Rewrote the four `lib/generate.ts` comment blocks BI-031.N's audit (F1) flagged as still narrating the BI-013 one-off live test as the present operating mode. `GrokImagineProvider`'s JSDoc, the `declare global` bridge comment, `generateBatch`'s JSDoc, and the no-provider block comment above the throw all now describe the standing Grok-Build-session architecture instead of a historical test run — the last of these also stopped naming "the /ft-task driver" (a flowtron workflow artifact) as the runtime actor, and now points at `docs/GROK-AGENT.md` like the operator-facing throw message immediately below it (BI-031.4).

Verification: `npx vitest run lib/generate.test.ts` 11/11 passed, `npx tsc --noEmit` exit 0, `npm run lint` clean. Comment-only change — no logic, exports, or thrown-message text touched; the file-header history comment and `isGenerationAvailable`'s JSDoc were left alone since they're already historical-citation form, not live-test narration, and outside F1's cited scope. Documentation: three AI-referenced docs swept (all "no change"), PLAN.md updated. Maintainability: closes the one gap BI-031.4 knowingly left open — a future reader of `lib/generate.ts` no longer finds an internal-jargon "live test" / "/ft-task driver" narrative in the exact file BI-EPIC-031 was about clarifying.

**Archived:** 2026-08-08
