---
title: adopter-usage-guide
status: in-progress
tags: [docs]
created: 2026-06-11
due:
related-tasks: []
---

# BI-018 | adopter-usage-guide

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Write `docs/USAGE.md` covering prompt-writing conventions and reference image preparation so adopters can produce consistent, high-quality results without guessing the craft layer.

## ✅ Acceptance

- [ ] `docs/USAGE.md` exists with a base-prompt structure section
- [ ] Iteration prompt guidance covered (how to refine, what to carry forward)
- [ ] Reference image preparation covered: resolution, subject framing, naming, base64 size implications
- [ ] README.md Docs section links to `docs/USAGE.md`
- [ ] No features introduced; documentation only

## 🧩 Subtasks

- [ ] Survey README.md + existing docs to define scope and avoid duplication
- [ ] Write `docs/USAGE.md` (base prompt structure, iteration guidance, reference image prep)
- [ ] Add `docs/USAGE.md` entry to README.md Docs section
- [ ] Run type-check + lint (docs-only; confirm no code touched)

## 🔗 Related

- (none)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `docs/USAGE.md` does not exist. README.md covers the 6-step workflow at a surface level but says nothing about *how* to write effective prompts or *how* to prepare reference images. ADOPT.md covers git submodule setup; GROK-AGENT.md covers the technical integration seam. None fills the craft/operator guidance gap this task targets.

- [x] Read relevant source files

  - `README.md` — 6-step workflow overview; the Install & Run and Workflow sections are user-facing but don't cover prompt strategy.
  - `docs/GROK-AGENT.md` — agent-facing technical guide; no prompt-writing guidance.
  - `docs/ADOPT.md` — adoption mechanics; no usage guidance.
  - `lib/types.ts` — data model. Key constraints:
    - `RefImage.dataUrl` — images stored as base64 data URLs inline in localStorage.
    - `activeRefImageIds` — up to 3 active refs per task (`MAX_ACTIVE_REFS = 3` per `ReferenceLibrary.tsx`).
    - `BatchSize: 3 | 4 | 5` — 4 candidates per batch by default.
  - `components/ReferenceLibrary.tsx` — `MAX_REF_BYTES = 2 * 1024 * 1024` (2 MB hard cap per upload).
  - `lib/workspace.ts` (BI-017) — `GENERATED_QUOTA_WARN_BYTES = 4 * 1024 * 1024`; banner fires when accumulated generated data URLs exceed 4 MB; localStorage soft cliff ~5 MB.

- [x] **Archive skim** — BI-012: authored README.md (user-facing workflow overview, no prompt guidance). BI-016: refreshed docs for post-BI-013 reality; no prompt guidance added. BI-011: GROK-AGENT.md (technical only). BI-014: ADOPT.md (git submodule only). BI-017: surfaced exact size constants (MAX_REF_BYTES=2MB, GENERATED_QUOTA_WARN_BYTES=4MB, localStorage ~5MB cliff). No prior tasknote added anything resembling prompt-writing conventions or reference image prep guidance. Load-bearing constraint numbers confirmed as current.

- [x] **Drift check** — `MAX_REF_BYTES = 2 * 1024 * 1024` confirmed at `ReferenceLibrary.tsx:19`. `MAX_ACTIVE_REFS = 3` confirmed at `ReferenceLibrary.tsx:8` (JSDoc + usage at line 92). `GENERATED_QUOTA_WARN_BYTES = 4 * 1024 * 1024` confirmed in BI-017. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed. Explicit assumptions:
  1. USAGE.md is adopter-facing, not agent-facing — plain English, conversational tone matching README.md voice.
  2. No specific image-model recommendations (model choice is user/Grok-context dependent). Focus on universal craft principles that hold regardless of model.
  3. Reference image preparation covers what the *user* controls: resolution/size before upload, framing, naming. Not the technical data-URL plumbing (that's GROK-AGENT.md territory).
  4. Base64 size implications = practical guidance (keep refs under 2 MB, know accumulated generated images approach the 4 MB warning) — not raw encoding theory.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**
- Technical constraints grounding the guidance: 2 MB ref upload cap; 3 max active refs per task; 4 MB generated-image warning threshold; ~5 MB localStorage cliff.
- README.md covers what to do (the 6 steps); USAGE.md covers *how to do it well* — complementary, no duplication.
- No code files will be touched; `tsc --noEmit` and lint are trivially unaffected but should still be confirmed.

Discovery surfaced no significant deviation → skip 🛠️.

✅ Phase 1 Discovery complete; entering Phase 2 Execution.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — matched ADOPT.md's section-header + plain-English structure; GROK-AGENT.md's technical-fenced-code shape was rejected for a user-facing guide; new shape justified.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — docs-only; no tests applicable.

**Implementation Notes:**
- `docs/USAGE.md` (new, ~110 lines): two top-level sections — "Prompt writing" (base structure, iteration guidance, feedback notes) and "Reference image preparation" (resolution/size, localStorage budget table, subject framing, naming, active selection). Grounded in confirmed constants: `MAX_REF_BYTES=2MB`, `GENERATED_QUOTA_WARN_BYTES=4MB`, `MAX_ACTIVE_REFS=3`, localStorage ~5MB cliff.
- `README.md` — added `docs/USAGE.md` as first entry in the Docs section (most user-relevant; listed before adoption/integration guides).

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — docs-only; no tests applicable.

- [x] Ran lint/type-check on changed code — `npx tsc --noEmit` clean; `npm run lint` clean.

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask) — no frontend code changed; N/A.

**Testing Notes:**
tsc and eslint both exit clean. Only `docs/USAGE.md` (new) and `README.md` (one Docs-section line) changed; no source files touched.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep**:
  - `README.md` — **updated this task** (added `docs/USAGE.md` as first entry in the Docs section).
  - `AGENTS.md` — no change (no prompt/usage guidance referenced; grep clean).
  - `CLAUDE.md` — no change.
  - `.flowtron/PLAN.md` — updated (BI-018 flipped to stub + moved to `## Completed`).

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-06-11.` and tasknote moved to `.flowtron/tasknote/archive/bi/BI-018.md`

- [x] Recap drafted

**Final Summary:**
Wrote `docs/USAGE.md` — the prompt-writing and reference image preparation guide for adopters. Covers base prompt structure (subject/context/style pattern, concrete tips), iteration guidance (what to preserve vs. change, using feedback notes), and reference image preparation (2 MB cap, resize guidance, localStorage budget table, framing, naming, active selection strategy). Added `docs/USAGE.md` as the first entry in README.md's Docs section. Docs-only — no source files touched; tsc + lint clean.

**Archived:** 2026-06-11
